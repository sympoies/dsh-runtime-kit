diff --git a/packages/core/tools/src/index.ts b/packages/core/tools/src/index.ts
index 377015feb3..60d0a360a7 100644
--- a/packages/core/tools/src/index.ts
+++ b/packages/core/tools/src/index.ts
@@ -287,6 +287,23 @@ export interface ToolDefinition extends ToolSchema {
   presentResult?(args: unknown, result: ToolResult): ToolResultView | undefined
 }
 
+/**
+ * One execution-bound prerequisite installed during `tools/pre-execute`.
+ * Verification is side-effect free and may run more than once: before the
+ * around-dispatch waterfall and again at the exact body boundary. It returns
+ * the already verified context that belongs to this execution. `commit()` is
+ * only a durable cache-completion step after body, post-execute, cancellation,
+ * content finalization, and result materialization have all succeeded. A cache
+ * completion failure cannot turn an already completed mutation into a
+ * retryable tool failure; the verified context still accompanies the result.
+ */
+export interface ToolExecutionPrerequisite {
+  /** Side-effect-free last-mile verification and its bounded verified context. */
+  beforeBody(exec: ToolRunContext, phase: 'dispatch' | 'body'): Promise<UserMessage[] | void>
+  /** Idempotent durable cache completion after the accepted result linearizes. */
+  commit(exec: Readonly<ToolExecution>, result: Readonly<ToolExecutionSuccess>): Promise<void>
+}
+
 /** The completed outcome handed to {@link ToolDefinition.presentResult}. */
 export interface ToolResult {
   /** The final model-facing content (or the rendered error text on failure). */
@@ -763,6 +780,49 @@ interface ToolAskResolution {
 interface ToolCancellationState {
   readonly callerSignal: AbortSignal
   bodyInvoked: boolean
+  completionLinearized: boolean
+}
+
+type ToolExecutionStage =
+  | 'created'
+  | 'binding'
+  | 'admission'
+  | 'prepared-dispatch'
+  | 'prepared-post'
+  | 'prepared-final'
+  | 'dispatching'
+  | 'dispatched-post'
+  | 'dispatched-final'
+  | 'finalizing'
+  | 'finished'
+
+/** Immutable executable and output contract captured before policy listeners run. */
+interface ToolDefinitionSnapshot {
+  readonly definition: ToolDefinition
+  readonly name: string
+  readonly executeIdentity: ToolDefinition['execute']
+  readonly execute: ToolDefinition['execute']
+  readonly outputIdentity: ToolOutputDefinition
+  readonly outputSchemaJson: string
+  readonly output: ToolOutputDefinition
+  readonly renderIdentity: ToolOutputDefinition['render']
+  readonly presentationMetaIdentity: ToolOutputDefinition['presentationMeta']
+  readonly finalizeContentIdentity: ToolDefinition['finalizeContent']
+  readonly finalizeContent: ToolDefinition['finalizeContent']
+}
+
+/** Minimum immutable contract needed to normalize one successful result. */
+interface ToolSuccessContract {
+  readonly name: string
+  readonly output: ToolOutputDefinition
+}
+
+interface BoundToolPrerequisite {
+  readonly snapshot: ToolDefinitionSnapshot
+  readonly prerequisite: ToolExecutionPrerequisite
+  verifiedContexts: UserMessage[]
+  verifiedBeforeDispatch: boolean
+  completionEligible: boolean
 }
 
 /** One dispatch-scoped fused signal plus listener cleanup after the body settles. */
@@ -808,6 +868,14 @@ export class ToolRuntime extends Service {
   private cancellationStates = new WeakMap<ToolRunContext, ToolCancellationState>()
   /** Definition-owned final content transform snapshotted before policy begins. */
   private contentFinalizers = new WeakMap<ToolRunContext, ToolDefinition['finalizeContent']>()
+  /** Exact callable/output contract captured before extensible policy begins. */
+  private executionDefinitions = new WeakMap<ToolRunContext, ToolDefinitionSnapshot>()
+  /** Exact-definition prerequisite bound by policy to one registry-owned execution. */
+  private executionPrerequisites = new WeakMap<ToolRunContext, BoundToolPrerequisite>()
+  /** One-shot staged scheduler phase for every registry-owned execution. */
+  private executionStages = new WeakMap<ToolRunContext, ToolExecutionStage>()
+  /** Runtime attempts to mutate readonly execution identity before result publication. */
+  private executionIdentityViolations = new WeakSet<ToolRunContext>()
   private readonly layers = new ScopedLayers(
     scope => new ToolLayer(scope),
     () => { this.ctx.emit('tools/change') },
@@ -1115,6 +1183,46 @@ export class ToolRuntime extends Service {
     )
   }
 
+  /**
+   * Bind one exact visible definition to a prerequisite lifecycle for this
+   * execution. Policy calls this during `tools/pre-execute`; later definition
+   * substitution fails before the selected body. One execution accepts at
+   * most one binding so independent plugins cannot overwrite each other.
+   */
+  bindPrerequisite(
+    exec: ToolExecution,
+    definition: ToolDefinition,
+    prerequisite: ToolExecutionPrerequisite,
+  ): void {
+    const runtimeExec = exec as ToolRunContext
+    if (!this.cancellationStates.has(runtimeExec)) {
+      throw new Error('dsh-tools: prerequisite requires a registry-owned execution')
+    }
+    if (this.executionStages.get(runtimeExec) !== 'binding') {
+      throw new Error('dsh-tools: prerequisite binding is closed for this execution')
+    }
+    const snapshot = this.executionDefinitions.get(runtimeExec)
+    if (snapshot === undefined || snapshot.definition !== definition
+      || this.get(exec.name, exec.agent) !== definition || definition.name !== exec.name
+      || this.prerequisiteDefinitionMismatch(snapshot, runtimeExec) !== undefined) {
+      throw new Error('dsh-tools: prerequisite definition is not the exact visible tool')
+    }
+    if (this.executionPrerequisites.has(runtimeExec)) {
+      throw new Error('dsh-tools: execution already has a prerequisite binding')
+    }
+    if (typeof prerequisite?.beforeBody !== 'function'
+      || typeof prerequisite.commit !== 'function') {
+      throw new TypeError('dsh-tools: prerequisite must define beforeBody() and commit()')
+    }
+    this.executionPrerequisites.set(runtimeExec, {
+      snapshot,
+      prerequisite,
+      verifiedContexts: [],
+      verifiedBeforeDispatch: false,
+      completionEligible: true,
+    })
+  }
+
   /** First monotonic denial from the global then the scope chain's guard layers, farthest first. */
   private guardReason(exec: ToolExecution): string | undefined {
     const globalReason = this.layers.global.guardReason(exec)
@@ -1405,20 +1513,31 @@ export class ToolRuntime extends Service {
     // the same retained path), while the `UNKNOWN_TOOL` denial and the
     // invalid-args failure of a NON-ABORTED collapsed call drop it (the call
     // could never execute).
-    const capturedFinalizer = visible?.finalizeContent?.bind(visible)
-    const finalizerFor = (): ToolDefinition['finalizeContent'] | undefined =>
-      collapsed && !signal.aborted ? undefined : capturedFinalizer
+    let capturedDefinition: ToolDefinitionSnapshot | undefined
+    let capturedFinalizer: ToolDefinition['finalizeContent']
+    let finalizerFor: () => ToolDefinition['finalizeContent'] | undefined = () => undefined
     try {
+      capturedDefinition = visible === undefined
+        ? undefined
+        : this.snapshotExecutionDefinition(visible)
+      capturedFinalizer = capturedDefinition?.finalizeContent
+      finalizerFor = () => collapsed && !signal.aborted ? undefined : capturedFinalizer
       const detached = snapshotJsonValue(exec.arguments)
       if (detached === undefined) {
         throw new TypeError('tool execution arguments must be losslessly JSON-serializable')
       }
       const execution: MutableToolRunContext = { ...base, arguments: deepFreeze(detached) }
+      this.lockExecutionIdentity(execution)
+      this.executionStages.set(execution, 'created')
       this.deferredContexts.set(execution, deferredContexts)
       this.contentFinalizers.set(execution, finalizerFor())
+      if (capturedDefinition !== undefined) {
+        this.executionDefinitions.set(execution, capturedDefinition)
+      }
       this.cancellationStates.set(execution, {
         callerSignal: signal,
         bodyInvoked: false,
+        completionLinearized: false,
       })
       if (collapsed) {
         // The collapse denies the call before the policy pipeline, but a
@@ -1445,6 +1564,8 @@ export class ToolRuntime extends Service {
       return { kind: 'ready', exec: execution }
     } catch (error: unknown) {
       const execution: MutableToolRunContext = { ...base, arguments: undefined }
+      this.lockExecutionIdentity(execution)
+      this.executionStages.set(execution, 'created')
       this.contentFinalizers.set(execution, finalizerFor())
       return { kind: 'final-result', exec: execution, result: toolErrorResult(error) }
     }
@@ -1465,29 +1586,41 @@ export class ToolRuntime extends Service {
     next: (prepared: ScheduledToolPreparation) => T | PromiseLike<T>,
   ): Promise<T> {
     const created = this.createExecution(input)
-    if (created.kind !== 'ready') return next(created)
+    if (created.kind !== 'ready') {
+      this.markPreparedExecution(created.exec, created.kind)
+      return next(created)
+    }
     const exec = created.exec
     if (this.callerCancelled(exec)) {
-      return next({ kind: 'final-result', exec, result: toolAbortedBeforeDispatchResult() })
+      const prepared = { kind: 'final-result' as const, exec, result: toolAbortedBeforeDispatchResult() }
+      this.markPreparedExecution(exec, prepared.kind)
+      return next(prepared)
     }
+    this.transitionExecution(exec, ['created'], 'binding', 'prepare')
+    let prepared: ScheduledToolPreparation
     try {
       const carrier = scopeTarget(this, exec.agent)
       const gate = await this.ctx.waterfall(
         carrier, 'tools/pre-execute', exec,
         () => Promise.resolve<PreToolDecision>({ kind: 'allow' }),
       )
+      // The ordered policy waterfall is the sole prerequisite-binding window.
+      // Close it before an approval request can suspend admission, otherwise a
+      // listener-retained execution handle could attach new authority while a
+      // human decision is already pending.
+      this.transitionExecution(exec, ['binding'], 'admission', 'await approval')
       const askResolution: ToolAskResolution = gate.kind === 'ask'
         ? await this.serviceAsk(exec, gate)
         : { decision: gate, approvalCancelled: false }
       const { decision } = askResolution
       if (this.callerCancelled(exec) && askResolution.approvalCancelled) {
-        return await next({ kind: 'post-result', exec, result: toolAbortedBeforeDispatchResult() })
-      }
-      const denialReason = decision.kind === 'allow'
-        ? this.guardReason(exec)
-        : decision.reason
-      if (denialReason !== undefined) {
-        return await next({
+        prepared = { kind: 'post-result', exec, result: toolAbortedBeforeDispatchResult() }
+      } else {
+        const denialReason = decision.kind === 'allow'
+          ? this.guardReason(exec)
+          : decision.reason
+        if (denialReason !== undefined) {
+          prepared = {
           kind: 'post-result',
           exec,
           result: this.materializeFinalResult({
@@ -1495,15 +1628,43 @@ export class ToolRuntime extends Service {
             isError: true,
             error: { message: denialReason },
           }),
-        })
-      }
-      if (this.callerCancelled(exec)) {
-        return await next({ kind: 'post-result', exec, result: toolAbortedBeforeDispatchResult() })
+          }
+        } else if (this.callerCancelled(exec)) {
+          prepared = { kind: 'post-result', exec, result: toolAbortedBeforeDispatchResult() }
+        } else {
+          prepared = { kind: 'dispatch', exec }
+        }
       }
-      return await next({ kind: 'dispatch', exec })
     } catch (error: unknown) {
-      return next({ kind: 'final-result', exec, result: toolErrorResult(error) })
+      prepared = { kind: 'final-result', exec, result: toolErrorResult(error) }
+    }
+    this.markPreparedExecution(exec, prepared.kind)
+    return next(prepared)
+  }
+
+  /** Atomically move one registry execution through its private scheduler state machine. */
+  private transitionExecution(
+    exec: ToolRunContext,
+    expected: ToolExecutionStage[],
+    next: ToolExecutionStage,
+    operation: string,
+  ): void {
+    const current = this.executionStages.get(exec)
+    if (current === undefined || !expected.includes(current)) {
+      throw new Error(`dsh-tools: scheduler execution cannot ${operation} from its current stage`)
     }
+    this.executionStages.set(exec, next)
+  }
+
+  /** Close prerequisite binding and publish the sole scheduler continuation. */
+  private markPreparedExecution(
+    exec: ToolRunContext,
+    kind: ScheduledToolPreparation['kind'],
+  ): void {
+    const next = kind === 'dispatch' ? 'prepared-dispatch'
+      : kind === 'post-result' ? 'prepared-post'
+        : 'prepared-final'
+    this.transitionExecution(exec, ['created', 'binding', 'admission'], next, 'complete preparation')
   }
 
   /** Whether the original caller signal is currently aborted. */
@@ -1511,7 +1672,7 @@ export class ToolRuntime extends Service {
     const state = this.cancellationStates.get(exec)
     /* v8 ignore next -- only registry-minted executions reach the staged scheduler methods */
     if (state === undefined) throw new Error('tool registry scheduler invariant violated: missing cancellation state')
-    return state.callerSignal.aborted
+    return state.callerSignal.aborted && !state.completionLinearized
   }
 
   /** Canonical cancellation outcome selected by whether the tool body started. */
@@ -1524,6 +1685,142 @@ export class ToolRuntime extends Service {
       : toolAbortedBeforeDispatchResult(prior)
   }
 
+  /**
+   * Make readonly execution fields tamper-evident while preserving `signal` as
+   * the sole around-wrapper mutation channel. A caught assignment still marks
+   * the execution invalid, so mutate-then-restore cannot hide substitution.
+   */
+  private lockExecutionIdentity(exec: MutableToolRunContext): void {
+    for (const key of Reflect.ownKeys(exec)) {
+      if (key === 'signal') continue
+      const descriptor = Object.getOwnPropertyDescriptor(exec, key)
+      /* v8 ignore next -- every spread-created execution field is an own data property */
+      if (descriptor === undefined || !('value' in descriptor)) continue
+      const value = descriptor.value
+      Object.defineProperty(exec, key, {
+        configurable: false,
+        enumerable: descriptor.enumerable === true,
+        get: () => value,
+        set: () => { this.executionIdentityViolations.add(exec) },
+      })
+    }
+  }
+
+  /** Reject any attempted substitution of the public readonly identity. */
+  private assertExecutionIdentity(exec: ToolRunContext): void {
+    if (this.executionIdentityViolations.has(exec)) {
+      throw new Error('dsh-tools: readonly execution identity mutation attempted')
+    }
+  }
+
+  /** Capture every behavior that may run after policy before listeners can substitute it. */
+  private snapshotExecutionDefinition(definition: ToolDefinition): ToolDefinitionSnapshot {
+    const outputSchema = snapshotJsonValue(definition.output.schema)
+    if (outputSchema === undefined) {
+      throw new TypeError(`tool "${definition.name}" output schema must be losslessly JSON-serializable`)
+    }
+    const executeIdentity = definition.execute
+    const outputIdentity = definition.output
+    const renderIdentity = outputIdentity.render
+    const presentationMetaIdentity = outputIdentity.presentationMeta
+    const finalizeContentIdentity = definition.finalizeContent
+    return Object.freeze({
+      definition,
+      name: definition.name,
+      executeIdentity,
+      execute: executeIdentity.bind(definition),
+      outputIdentity,
+      outputSchemaJson: JSON.stringify(outputSchema),
+      output: Object.freeze({
+        schema: deepFreeze(outputSchema) as unknown as JsonSchemaNode,
+        render: renderIdentity.bind(outputIdentity),
+        ...presentationMetaIdentity === undefined
+          ? {}
+          : { presentationMeta: presentationMetaIdentity.bind(outputIdentity) },
+      }),
+      renderIdentity,
+      presentationMetaIdentity,
+      finalizeContentIdentity,
+      finalizeContent: finalizeContentIdentity?.bind(definition),
+    })
+  }
+
+  /** Describe definition or in-place callable/output drift without executing it. */
+  private prerequisiteDefinitionMismatch(
+    snapshot: ToolDefinitionSnapshot,
+    exec: ToolRunContext,
+  ): string | undefined {
+    const current = this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
+    if (current !== snapshot.definition) return 'exact tool definition changed'
+    try {
+      const schema = snapshotJsonValue(current.output.schema)
+      if (current.name !== snapshot.name
+        || current.execute !== snapshot.executeIdentity
+        || current.output !== snapshot.outputIdentity
+        || current.output.render !== snapshot.renderIdentity
+        || current.output.presentationMeta !== snapshot.presentationMetaIdentity
+        || current.finalizeContent !== snapshot.finalizeContentIdentity
+        || schema === undefined
+        || JSON.stringify(schema) !== snapshot.outputSchemaJson) {
+        return 'exact tool definition contract changed'
+      }
+    } catch {
+      return 'exact tool definition contract changed'
+    }
+    return undefined
+  }
+
+  /** Require the exact definition and callable/output contract bound by policy to remain visible. */
+  private assertPrerequisiteDefinition(exec: ToolRunContext, phase: string): BoundToolPrerequisite | undefined {
+    const bound = this.executionPrerequisites.get(exec)
+    if (bound === undefined) return undefined
+    const mismatch = this.prerequisiteDefinitionMismatch(bound.snapshot, exec)
+    if (mismatch !== undefined) {
+      throw new Error(`dsh-tools: prerequisite ${mismatch} before ${phase}`)
+    }
+    return bound
+  }
+
+  /** Reverify one bound prerequisite and retain only its latest verified context. */
+  private async verifyExecutionPrerequisite(
+    exec: ToolRunContext,
+    phase: 'dispatch' | 'body',
+  ): Promise<void> {
+    const boundary = phase === 'dispatch' ? 'execute dispatch' : 'tool body'
+    this.assertExecutionIdentity(exec)
+    const bound = this.assertPrerequisiteDefinition(exec, boundary)
+    if (bound === undefined) return
+    const contexts = await bound.prerequisite.beforeBody(exec, phase)
+    if (this.callerCancelled(exec)) {
+      throw new Error('dsh-tools: prerequisite verification cancelled')
+    }
+    this.assertExecutionIdentity(exec)
+    this.assertPrerequisiteDefinition(exec, boundary)
+    if (contexts !== undefined && !Array.isArray(contexts)) {
+      throw new TypeError('dsh-tools: prerequisite verification must return contexts or undefined')
+    }
+    if (contexts === undefined) {
+      bound.verifiedContexts = []
+      bound.verifiedBeforeDispatch = true
+      return
+    }
+    const detached = snapshotJsonValue(contexts)
+    if (!Array.isArray(detached)) {
+      throw new TypeError('dsh-tools: prerequisite contexts must be losslessly JSON-serializable')
+    }
+    bound.verifiedContexts = deepFreeze(detached) as unknown as UserMessage[]
+    bound.verifiedBeforeDispatch = true
+  }
+
+  /** A completion warning must never alter an already materialized success. */
+  private warnPrerequisiteCompletion(exec: ToolExecution, reason: string): void {
+    try {
+      this.ctx.logger.warn(`tool "${exec.name}" (${exec.callId}): prerequisite cache completion skipped: ${reason}`)
+    } catch {
+      // Logging is observational and cannot reopen an accepted mutation.
+    }
+  }
+
   /**
    * Dispatch the registered body with the original caller signal fused back
    * into any around-wrapper replacement. Cancellation never abandons the body:
@@ -1543,7 +1840,12 @@ export class ToolRuntime extends Service {
     }
     exec.signal = signal
     try {
-      const tool = this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
+      this.assertExecutionIdentity(exec)
+      await this.verifyExecutionPrerequisite(exec, 'body')
+      if (isAborted(signal)) return toolAbortedBeforeDispatchResult()
+      const bound = this.assertPrerequisiteDefinition(exec, 'tool body dispatch')
+      const tool = bound?.snapshot
+        ?? this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
       if (!tool) throw new ToolNotFoundError(exec.name)
       state.bodyInvoked = true
       const returned = await tool.execute(exec.arguments, exec)
@@ -1552,7 +1854,9 @@ export class ToolRuntime extends Service {
         ? toolAbortedResult(result)
         : result
     } catch (error: unknown) {
-      return toolErrorResult(error)
+      return !state.bodyInvoked && (isAborted(signal) || this.callerCancelled(exec))
+        ? this.cancellationResult(exec)
+        : toolErrorResult(error)
     } finally {
       fused.dispose()
       exec.signal = wrapperSignal
@@ -1567,14 +1871,53 @@ export class ToolRuntime extends Service {
    * @internal
    */
   private async dispatchScheduledExecution(exec: ToolRunContext): Promise<ScheduledToolDispatch> {
+    this.transitionExecution(exec, ['prepared-dispatch'], 'dispatching', 'dispatch')
+    const outcome = await this.runScheduledDispatch(exec)
+    this.transitionExecution(
+      exec,
+      ['dispatching'],
+      outcome.kind === 'post-result' ? 'dispatched-post' : 'dispatched-final',
+      'complete dispatch',
+    )
+    return outcome
+  }
+
+  /** Execute one already-claimed dispatch stage. */
+  private async runScheduledDispatch(exec: ToolRunContext): Promise<ScheduledToolDispatch> {
     try {
       const mutableExec = exec as MutableToolRunContext
       const carrier = scopeTarget(this, exec.agent)
+      this.assertExecutionIdentity(exec)
+      try {
+        await this.verifyExecutionPrerequisite(exec, 'dispatch')
+      } catch (error: unknown) {
+        if (this.callerCancelled(exec)) {
+          return { kind: 'post-result', result: this.cancellationResult(exec) }
+        }
+        return { kind: 'post-result', result: toolErrorResult(error) }
+      }
+      if (this.callerCancelled(exec)) {
+        return { kind: 'post-result', result: toolAbortedBeforeDispatchResult() }
+      }
       const result = await this.ctx.waterfall(
         carrier, 'tools/execute', mutableExec,
         () => this.dispatchToolBody(mutableExec),
       )
       const normalized = this.normalizeDispatchResult(exec, result)
+      const state = this.cancellationStates.get(exec)
+      /* v8 ignore next -- dispatch only receives registry-minted executions */
+      if (state === undefined) throw new Error('tool registry scheduler invariant violated: missing cancellation state')
+      if (!state.bodyInvoked && !normalized.isError) {
+        const bound = this.executionPrerequisites.get(exec)
+        if (state.callerSignal.aborted && bound !== undefined) {
+          bound.completionEligible = false
+          this.warnPrerequisiteCompletion(exec, 'caller cancellation arrived after successful execute short circuit')
+        }
+        // The pre-wrapper verification is the authorization linearization point.
+        // A wrapper-reported success may already represent a mutation, so no
+        // asynchronous verification or caller abort may make it retryable.
+        state.completionLinearized = true
+      }
       const deferredContexts = this.deferredContexts.get(exec)
       /* v8 ignore next -- dispatch only receives executions minted by this registry's prepare stage */
       if (deferredContexts === undefined) throw new Error('tool registry scheduler invariant violated: unprepared execution')
@@ -1607,17 +1950,74 @@ export class ToolRuntime extends Service {
    * @internal
    */
   private async finalizeScheduledExecution(exec: ToolRunContext, result: ToolExecutionResult): Promise<ToolExecutionResult> {
+    this.transitionExecution(
+      exec,
+      ['prepared-post', 'dispatched-post'],
+      'finalizing',
+      'finalize',
+    )
     try {
-      const postResult = await this.postExecute(exec, result)
-      return this.finishScheduledExecution(
-        exec,
-        this.callerCancelled(exec) && !postResult.isError
-          ? this.cancellationResult(exec, postResult)
-          : postResult,
-      )
+      return await this.finalizeScheduledResult(exec, result)
+    } finally {
+      this.transitionExecution(exec, ['finalizing'], 'finished', 'complete finalization')
+    }
+  }
+
+  /** Finalize one already-claimed post-result stage. */
+  private async finalizeScheduledResult(exec: ToolRunContext, result: ToolExecutionResult): Promise<ToolExecutionResult> {
+    let postResult: ToolExecutionResult
+    try {
+      postResult = await this.postExecute(exec, result)
     } catch (error: unknown) {
-      return this.finishScheduledExecution(exec, toolErrorResult(error))
+      return this.finishScheduledResult(exec, toolErrorResult(error))
+    }
+
+    const candidate = this.callerCancelled(exec) && !postResult.isError
+      ? this.cancellationResult(exec, postResult)
+      : postResult
+    let finalResult = this.prepareScheduledResult(exec, candidate)
+    const bound = this.executionPrerequisites.get(exec)
+    if (bound === undefined || finalResult.isError || this.callerCancelled(exec)) {
+      this.notifyResult(exec, finalResult)
+      return finalResult
     }
+
+    if (bound.verifiedContexts.length > 0) {
+      finalResult = this.materializeFinalResult({
+        ...finalResult,
+        additionalContexts: [
+          ...finalResult.additionalContexts ?? [],
+          ...bound.verifiedContexts,
+        ],
+      })
+    }
+    if (this.callerCancelled(exec)) {
+      finalResult = this.materializeFinalResult(this.cancellationResult(exec, finalResult))
+      this.notifyResult(exec, finalResult)
+      return finalResult
+    }
+
+    const state = this.cancellationStates.get(exec)
+    /* v8 ignore next -- only registry-minted executions reach finalization */
+    if (state === undefined) throw new Error('tool registry scheduler invariant violated: missing cancellation state')
+    state.completionLinearized = true
+    const mismatch = this.prerequisiteDefinitionMismatch(bound.snapshot, exec)
+    if (!bound.verifiedBeforeDispatch) {
+      this.warnPrerequisiteCompletion(exec, 'execution prerequisite was never verified before dispatch')
+    } else if (!bound.completionEligible) {
+      // A successful execute-wrapper short circuit may already have mutated.
+      // Its failed post-dispatch recheck cannot turn that success retryable.
+    } else if (mismatch !== undefined) {
+      this.warnPrerequisiteCompletion(exec, `${mismatch} after execution`)
+    } else {
+      try {
+        await bound.prerequisite.commit(exec, finalResult as ToolExecutionSuccess)
+      } catch (error: unknown) {
+        this.warnPrerequisiteCompletion(exec, errorMessage(error))
+      }
+    }
+    this.notifyResult(exec, finalResult)
+    return finalResult
   }
 
   /**
@@ -1629,6 +2029,28 @@ export class ToolRuntime extends Service {
    * @internal
    */
   private finishScheduledExecution(exec: ToolRunContext, result: ToolExecutionResult): ToolExecutionResult {
+    this.transitionExecution(
+      exec,
+      ['prepared-final', 'dispatched-final'],
+      'finalizing',
+      'finish',
+    )
+    try {
+      return this.finishScheduledResult(exec, result)
+    } finally {
+      this.transitionExecution(exec, ['finalizing'], 'finished', 'complete finalization')
+    }
+  }
+
+  /** Finish one already-claimed final-result stage. */
+  private finishScheduledResult(exec: ToolRunContext, result: ToolExecutionResult): ToolExecutionResult {
+    const finalResult = this.prepareScheduledResult(exec, result)
+    this.notifyResult(exec, finalResult)
+    return finalResult
+  }
+
+  /** Materialize and finalize once without publishing the result yet. */
+  private prepareScheduledResult(exec: ToolRunContext, result: ToolExecutionResult): ToolExecutionResult {
     let materializedResult: ToolExecutionResult
     try {
       materializedResult = this.materializeFinalResult(result)
@@ -1641,7 +2063,6 @@ export class ToolRuntime extends Service {
     } catch (error: unknown) {
       finalResult = this.materializeFinalResult(toolErrorResult(error))
     }
-    this.notifyResult(exec, finalResult)
     return finalResult
   }
 
@@ -1765,8 +2186,7 @@ export class ToolRuntime extends Service {
       if (result.isError) {
         throw new TypeError('tools/post-execute cannot replace the value of a failed result')
       }
-      const tool = this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
-      if (tool === undefined) throw new ToolNotFoundError(exec.name)
+      const tool = this.successContract(exec)
       const replaced = this.createSuccessResult(exec, tool, decision.value)
       return this.markCanonical(exec, {
         ...replaced,
@@ -1783,6 +2203,14 @@ export class ToolRuntime extends Service {
   /** Registry-normalized results and the exact dispatch that validated each value. */
   private readonly canonicalResults = new WeakMap<object, ToolExecutionToken>()
 
+  /** Stable output contract selected for success normalization in this dispatch. */
+  private successContract(exec: ToolExecution): ToolSuccessContract {
+    const tool = this.executionPrerequisites.get(exec as ToolRunContext)?.snapshot
+      ?? this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
+    if (tool === undefined) throw new ToolNotFoundError(exec.name)
+    return tool
+  }
+
   /** Mark one registry-normalized result as canonical only for its owning dispatch. */
   private markCanonical<T extends ToolExecutionResult>(exec: ToolExecution, result: T): T {
     this.canonicalResults.set(result, exec.token)
@@ -1790,7 +2218,7 @@ export class ToolRuntime extends Service {
   }
 
   /** Snapshot, validate, render, and optionally project one successful body value. */
-  private createSuccessResult(exec: ToolExecution, tool: ToolDefinition, candidate: unknown): ToolExecutionSuccess {
+  private createSuccessResult(exec: ToolExecution, tool: ToolSuccessContract, candidate: unknown): ToolExecutionSuccess {
     const detached = snapshotToolValue(tool.name, candidate)
     const violations = validateJsonSchemaValue(tool.output.schema, detached, 'value')
     if (violations.length > 0) throw new ToolOutputError(tool.name, violations)
@@ -1834,8 +2262,7 @@ export class ToolRuntime extends Service {
         ...result.additionalContexts !== undefined ? { additionalContexts: result.additionalContexts } : {},
       })
     }
-    const tool = this.resolveExecution(exec.name, exec.agent, exec.parent !== undefined)
-    if (tool === undefined) throw new ToolNotFoundError(exec.name)
+    const tool = this.successContract(exec)
     const normalized = this.createSuccessResult(exec, tool, result.value)
     return this.markCanonical(exec, {
       ...normalized,
diff --git a/packages/core/tools/tests/tools.spec.ts b/packages/core/tools/tests/tools.spec.ts
index ec770592a6..2d3baae02b 100644
--- a/packages/core/tools/tests/tools.spec.ts
+++ b/packages/core/tools/tests/tools.spec.ts
@@ -4,9 +4,10 @@ import { createUserMessage, CallId, HarnessError, type ContentBlock  } from '@de
 import SystemPrompt from '@deepseek-ai/dsh-system-prompt'
 import type { Agent } from '@deepseek-ai/dsh-agent'
 import ApprovalService, { type ApprovalOutcome, type ApprovalRequest } from '@deepseek-ai/dsh-user-approval'
+import { createScope } from '@deepseek-ai/dsh-scope'
 import ToolRuntime, {
   defineContentToolFixture, defineTool, JsonSchemaError, parameterSchemaSpecToJsonSchema, validateArgs, ToolArgsError, ToolNotFoundError,
-  TOOL_ABORTED, TOOL_ABORTED_BEFORE_DISPATCH,
+  TOOL_ABORTED, TOOL_ABORTED_BEFORE_DISPATCH, TOOL_RUNTIME_SCHEDULER,
   type InferArgs, type JsonValue, type ParameterSchemaSpec, type PreToolDecision, type PostToolDecision,
   type JsonSchemaNode, type ToolDefinition, type ToolDispatchExecution, type ToolExecutionResult, type ToolExecutionToken,
 } from '@deepseek-ai/dsh-tools'
@@ -93,6 +94,696 @@ describe('ToolRuntime', () => {
     expect(observed).toEqual(result)
   })
 
+  it('binds an exact execution prerequisite across body and post completion', async () => {
+    const ctx = await setup()
+    const order: string[] = []
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-order',
+      async execute() { order.push('body'); return 'ok' },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody(_exec, phase) {
+          order.push(`before-${phase}`)
+          return [createUserMessage({
+            content: [{ type: 'text', text: 'prerequisite context' }],
+            source: { kind: 'plugin', plugin: 'prerequisite-test' },
+          })]
+        },
+        async commit() {
+          order.push('commit')
+        },
+      })
+      return next()
+    })
+    ctx.on('tools/post-execute', async (_exec, _result, next) => {
+      order.push('post-enter')
+      const decision = await next()
+      order.push('post-exit')
+      return decision
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-order'),
+      name: tool.name,
+      arguments: {},
+    })
+
+    expect(order).toEqual(['before-dispatch', 'before-body', 'body', 'post-enter', 'post-exit', 'commit'])
+    expect(result).toMatchObject({
+      isError: false,
+      value: 'ok',
+      additionalContexts: [{ content: [{ type: 'text', text: 'prerequisite context' }] }],
+    })
+  })
+
+  it('binds a prerequisite to a definition owned by the exact agent scope', async () => {
+    const ctx = await setup()
+    const agent = { id: 'scoped-prerequisite-agent' } as unknown as Agent
+    let scoped!: Context
+    await ctx.plugin(Object.assign((inner: Context) => {
+      scoped = createScope(inner, agent).ctx
+    }, { inject: ['tools'] }))
+    ;(agent as { ctx?: Context }).ctx = scoped
+    let bodies = 0
+    let commits = 0
+    const tool = {
+      ...echoTool,
+      name: 'scoped-prerequisite',
+      async execute() { bodies += 1; return 'scoped' },
+    }
+    scoped.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('scoped-prerequisite'),
+      name: tool.name,
+      arguments: {},
+      agent,
+    })
+
+    expect(result).toMatchObject({ isError: false, value: 'scoped' })
+    expect({ bodies, commits }).toEqual({ bodies: 1, commits: 1 })
+  })
+
+  it('rejects exact-definition substitution before prerequisite verification or body', async () => {
+    const ctx = await setup()
+    let bodies = 0
+    let verifications = 0
+    let commits = 0
+    const original = {
+      ...echoTool,
+      name: 'prerequisite-replaced',
+      async execute() { bodies += 1; return 'old' },
+    }
+    const replacement = {
+      ...original,
+      async execute() { bodies += 1; return 'new' },
+    }
+    const dispose = ctx.tools.register(original)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, original, {
+        async beforeBody() { verifications += 1 },
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async (_exec, next) => {
+      dispose()
+      ctx.tools.register(replacement)
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-replaced'),
+      name: original.name,
+      arguments: {},
+    })
+
+    expect(result.isError).toBe(true)
+    expect(result.error?.message).toContain('prerequisite exact tool definition changed')
+    expect({ bodies, verifications, commits }).toEqual({ bodies: 0, verifications: 1, commits: 0 })
+  })
+
+  it.each(['execute', 'render', 'schema'] as const)(
+    'rejects mutate-then-restore substitution of the bound %s contract',
+    async (surface) => {
+      const ctx = await setup()
+      let bodies = 0
+      let substitutedCallbacks = 0
+      let commits = 0
+      const tool = {
+        ...echoTool,
+        name: `prerequisite-contract-${surface}`,
+        output: { ...echoTool.output },
+        async execute() { bodies += 1; return 'original' },
+      }
+      ctx.tools.register(tool)
+      ctx.on('tools/pre-execute', async (exec, next) => {
+        ctx.tools.bindPrerequisite(exec, tool, {
+          async beforeBody() {},
+          async commit() { commits += 1 },
+        })
+        return next()
+      })
+      ctx.on('tools/execute', async (_exec, next) => {
+        const target = surface === 'execute' ? tool : tool.output
+        const key = surface === 'schema' ? 'schema' : surface
+        const original = Reflect.get(target, key)
+        const replacement = surface === 'schema'
+          ? { type: 'number' }
+          : async () => { substitutedCallbacks += 1; return 'substituted' }
+        Reflect.set(target, key, replacement)
+        try {
+          return await next()
+        } finally {
+          Reflect.set(target, key, original)
+        }
+      })
+
+      const result = await ctx.tools.execute({
+        signal: testToolSignal,
+        callId: CallId(`prerequisite-contract-${surface}`),
+        name: tool.name,
+        arguments: {},
+      })
+
+      expect(result).toMatchObject({
+        isError: true,
+        error: { message: expect.stringContaining('prerequisite exact tool definition contract changed') },
+      })
+      expect({ bodies, substitutedCallbacks, commits }).toEqual({
+        bodies: 0,
+        substitutedCallbacks: 0,
+        commits: 0,
+      })
+    },
+  )
+
+  it('verifies and commits a successful tools execute short circuit without the original body', async () => {
+    const ctx = await setup()
+    let bodies = 0
+    let verifications = 0
+    let commits = 0
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-short-circuit',
+      async execute() { bodies += 1; return 'body' },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() { verifications += 1 },
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async () => ({
+      isError: false,
+      value: 'wrapper',
+      content: [{ type: 'text', text: 'wrapper' }],
+    }))
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-short-circuit'),
+      name: tool.name,
+      arguments: {},
+    })
+
+    expect(result).toMatchObject({ isError: false, value: 'wrapper' })
+    expect({ bodies, verifications, commits }).toEqual({ bodies: 0, verifications: 1, commits: 1 })
+  })
+
+  it('preserves a successful mutating short circuit when post-dispatch verification becomes uncertain', async () => {
+    const ctx = await setup()
+    let bodies = 0
+    let mutations = 0
+    let verifications = 0
+    let commits = 0
+    const original = {
+      ...echoTool,
+      name: 'prerequisite-mutating-short-circuit',
+      async execute() { bodies += 1; return 'body' },
+    }
+    const replacement = { ...original, async execute() { return 'replacement' } }
+    const dispose = ctx.tools.register(original)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, original, {
+        async beforeBody() {
+          verifications += 1
+          return [createUserMessage({
+            content: [{ type: 'text', text: 'initial verified context' }],
+            source: { kind: 'plugin', plugin: 'prerequisite-test' },
+          })]
+        },
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async () => {
+      mutations += 1
+      dispose()
+      ctx.tools.register(replacement)
+      return {
+        isError: false,
+        value: 'wrapper mutation complete',
+        content: [{ type: 'text', text: 'wrapper mutation complete' }],
+      }
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-mutating-short-circuit'),
+      name: original.name,
+      arguments: {},
+    })
+
+    expect(result).toMatchObject({
+      isError: false,
+      value: 'wrapper mutation complete',
+      additionalContexts: [{ content: [{ type: 'text', text: 'initial verified context' }] }],
+    })
+    expect({ bodies, mutations, verifications, commits }).toEqual({
+      bodies: 0,
+      mutations: 1,
+      verifications: 1,
+      commits: 0,
+    })
+  })
+
+  it('preserves successful short-circuit mutation and context when caller cancellation arrives after it', async () => {
+    const ctx = await setup()
+    const controller = new AbortController()
+    let mutations = 0
+    let verifications = 0
+    let commits = 0
+    const tool = { ...echoTool, name: 'prerequisite-short-circuit-late-abort' }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {
+          verifications += 1
+          return [createUserMessage({
+            content: [{ type: 'text', text: 'last verified context' }],
+            source: { kind: 'plugin', plugin: 'prerequisite-test' },
+          })]
+        },
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async () => {
+      mutations += 1
+      controller.abort('caller stopped after mutation')
+      return {
+        isError: false,
+        value: 'wrapper mutation complete',
+        content: [{ type: 'text', text: 'wrapper mutation complete' }],
+      }
+    })
+
+    const result = await ctx.tools.execute({
+      signal: controller.signal,
+      callId: CallId('prerequisite-short-circuit-late-abort'),
+      name: tool.name,
+      arguments: {},
+    })
+
+    expect(result).toMatchObject({
+      isError: false,
+      value: 'wrapper mutation complete',
+      additionalContexts: [{ content: [{ type: 'text', text: 'last verified context' }] }],
+    })
+    expect({ mutations, verifications, commits }).toEqual({ mutations: 1, verifications: 1, commits: 0 })
+  })
+
+  it.each(['cyclic', 'bigint'] as const)(
+    'rejects %s prerequisite context before the tool body',
+    async (kind) => {
+      const ctx = await setup()
+      let bodies = 0
+      let commits = 0
+      const tool = {
+        ...echoTool,
+        name: `prerequisite-invalid-context-${kind}`,
+        async execute() { bodies += 1; return 'unexpected' },
+      }
+      ctx.tools.register(tool)
+      ctx.on('tools/pre-execute', async (exec, next) => {
+        ctx.tools.bindPrerequisite(exec, tool, {
+          async beforeBody() {
+            if (kind === 'bigint') return [{ invalid: 1n }] as unknown as never[]
+            const cyclic = [] as unknown[]
+            cyclic.push(cyclic)
+            return cyclic as never[]
+          },
+          async commit() { commits += 1 },
+        })
+        return next()
+      })
+
+      const result = await ctx.tools.execute({
+        signal: testToolSignal,
+        callId: CallId(`prerequisite-invalid-context-${kind}`),
+        name: tool.name,
+        arguments: {},
+      })
+
+      expect(result).toMatchObject({
+        isError: true,
+        error: { message: expect.stringContaining('prerequisite contexts must be losslessly JSON-serializable') },
+      })
+      expect({ bodies, commits }).toEqual({ bodies: 0, commits: 0 })
+    },
+  )
+
+  it('publishes a post-execute failure when a prerequisite rejects before dispatch', async () => {
+    const ctx = await setup()
+    let bodies = 0
+    let postFailures = 0
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-pre-dispatch-failure',
+      async execute() { bodies += 1; return 'unexpected' },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() { throw new Error('prerequisite denied before dispatch') },
+        async commit() {},
+      })
+      return next()
+    })
+    ctx.on('tools/post-execute', async (_exec, result, next) => {
+      if (result.isError) postFailures += 1
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-pre-dispatch-failure'),
+      name: tool.name,
+      arguments: {},
+    })
+
+    expect(result).toMatchObject({
+      isError: true,
+      error: { message: 'prerequisite denied before dispatch' },
+    })
+    expect({ bodies, postFailures }).toEqual({ bodies: 0, postFailures: 1 })
+  })
+
+  it('does not commit after a post-execute failure or caller cancellation', async () => {
+    for (const failure of ['post', 'cancel'] as const) {
+      const ctx = await setup()
+      const controller = new AbortController()
+      let commits = 0
+      const tool = {
+        ...echoTool,
+        name: `prerequisite-${failure}`,
+        async execute() {
+          if (failure === 'cancel') controller.abort()
+          return 'ok'
+        },
+      }
+      ctx.tools.register(tool)
+      ctx.on('tools/pre-execute', async (exec, next) => {
+        ctx.tools.bindPrerequisite(exec, tool, {
+          async beforeBody() {},
+          async commit() { commits += 1 },
+        })
+        return next()
+      })
+      if (failure === 'post') {
+        ctx.on('tools/post-execute', async () => { throw new Error('post failed') })
+      }
+
+      const result = await ctx.tools.execute({
+        signal: controller.signal,
+        callId: CallId(`prerequisite-${failure}`),
+        name: tool.name,
+        arguments: {},
+      })
+
+      expect(result.isError).toBe(true)
+      expect(commits).toBe(0)
+    }
+  })
+
+  it('linearizes accepted completion before cancellation during prerequisite commit', async () => {
+    const ctx = await setup()
+    const controller = new AbortController()
+    const tool = { ...echoTool, name: 'prerequisite-linearized' }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {},
+        async commit() {
+          controller.abort()
+        },
+      })
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: controller.signal,
+      callId: CallId('prerequisite-linearized'),
+      name: tool.name,
+      arguments: { text: 'ok' },
+    })
+
+    expect(result).toMatchObject({ isError: false, value: 'ok' })
+  })
+
+  it('blocks caught mutate-then-restore attempts on readonly execution identity', async () => {
+    const ctx = await setup()
+    let bodies = 0
+    let commits = 0
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-identity',
+      async execute() { bodies += 1; return 'original' },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async (exec, next) => {
+      const originalArguments = exec.arguments
+      const originalAgent = exec.agent
+      Reflect.set(exec, 'arguments', { text: 'substituted' })
+      Reflect.set(exec, 'agent', { id: 'substituted' } as unknown as Agent)
+      Reflect.set(exec, 'arguments', originalArguments)
+      Reflect.set(exec, 'agent', originalAgent)
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-identity'),
+      name: tool.name,
+      arguments: { text: 'original' },
+    })
+
+    expect(result).toMatchObject({
+      isError: true,
+      error: { message: 'dsh-tools: readonly execution identity mutation attempted' },
+    })
+    expect({ bodies, commits }).toEqual({ bodies: 0, commits: 0 })
+  })
+
+  it('maps caller abort during asynchronous prerequisite verification to pre-dispatch cancellation', async () => {
+    const ctx = await setup()
+    const entered = Promise.withResolvers<undefined>()
+    const release = Promise.withResolvers<undefined>()
+    const controller = new AbortController()
+    let bodies = 0
+    let commits = 0
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-verification-abort',
+      async execute() { bodies += 1; return 'unexpected' },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {
+          entered.resolve(undefined)
+          await release.promise
+          throw new Error('verification transport cancelled')
+        },
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+
+    const pending = ctx.tools.execute({
+      signal: controller.signal,
+      callId: CallId('prerequisite-verification-abort'),
+      name: tool.name,
+      arguments: {},
+    })
+    await entered.promise
+    controller.abort('cancel verification')
+    release.resolve(undefined)
+
+    await expect(pending).resolves.toMatchObject({
+      isError: true,
+      error: { info: { name: 'AbortError', code: TOOL_ABORTED_BEFORE_DISPATCH } },
+    })
+    expect({ bodies, commits }).toEqual({ bodies: 0, commits: 0 })
+  })
+
+  it('does not commit when definition-owned finalization fails', async () => {
+    const ctx = await setup()
+    let commits = 0
+    const tool = {
+      ...echoTool,
+      name: 'prerequisite-finalizer-failure',
+      finalizeContent() { throw new Error('finalizer failed') },
+    }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-finalizer-failure'),
+      name: tool.name,
+      arguments: { text: 'ok' },
+    })
+
+    expect(result).toMatchObject({ isError: true, error: { message: 'finalizer failed' } })
+    expect(commits).toBe(0)
+  })
+
+  it('preserves a finalized mutation and verified context when cache completion is uncertain', async () => {
+    const ctx = await setup()
+    const tool = { ...echoTool, name: 'prerequisite-cache-uncertain' }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {
+          return [createUserMessage({
+            content: [{ type: 'text', text: 'verified prerequisite context' }],
+            source: { kind: 'plugin', plugin: 'prerequisite-test' },
+          })]
+        },
+        async commit() { throw new Error('cache response lost') },
+      })
+      return next()
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-cache-uncertain'),
+      name: tool.name,
+      arguments: { text: 'ok' },
+    })
+
+    expect(result).toMatchObject({
+      isError: false,
+      value: 'ok',
+      additionalContexts: [{ content: [{ type: 'text', text: 'verified prerequisite context' }] }],
+    })
+  })
+
+  it('preserves a completed body but skips cache commit after post-body HMR substitution', async () => {
+    const ctx = await setup()
+    let commits = 0
+    const original = { ...echoTool, name: 'prerequisite-post-body-hmr' }
+    const replacement = { ...original, async execute() { return 'replacement' } }
+    const dispose = ctx.tools.register(original)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, original, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async (_exec, next) => {
+      const result = await next()
+      dispose()
+      ctx.tools.register(replacement)
+      return result
+    })
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-post-body-hmr'),
+      name: original.name,
+      arguments: { text: 'original' },
+    })
+
+    expect(result).toMatchObject({ isError: false, value: 'original' })
+    expect(commits).toBe(0)
+  })
+
+  it('normalizes post-execute value replacement through the snapshotted output after HMR', async () => {
+    const ctx = await setup()
+    let originalRenders = 0
+    let replacementRenders = 0
+    let commits = 0
+    const original = {
+      ...echoTool,
+      name: 'prerequisite-post-value-hmr',
+      output: {
+        schema: { type: 'string' } as const,
+        render(_args: unknown, value: JsonValue) {
+          originalRenders += 1
+          return [{ type: 'text' as const, text: `original:${value}` }]
+        },
+      },
+    }
+    const replacement = {
+      ...original,
+      output: {
+        ...original.output,
+        render(_args: unknown, value: JsonValue) {
+          replacementRenders += 1
+          return [{ type: 'text' as const, text: `replacement:${value}` }]
+        },
+      },
+    }
+    const dispose = ctx.tools.register(original)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, original, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/execute', async (_exec, next) => {
+      const result = await next()
+      dispose()
+      ctx.tools.register(replacement)
+      return result
+    })
+    ctx.on('tools/post-execute', async () => ({
+      kind: 'accept',
+      value: 'post replacement',
+    }))
+
+    const result = await ctx.tools.execute({
+      signal: testToolSignal,
+      callId: CallId('prerequisite-post-value-hmr'),
+      name: original.name,
+      arguments: { text: 'body value' },
+    })
+
+    expect(result).toMatchObject({
+      isError: false,
+      value: 'post replacement',
+      content: [{ type: 'text', text: 'original:post replacement' }],
+    })
+    expect({ originalRenders, replacementRenders, commits }).toEqual({
+      originalRenders: 2,
+      replacementRenders: 0,
+      commits: 0,
+    })
+  })
+
   it('projects presentation metadata from the canonical value', async () => {
     const ctx = await setup()
     ctx.tools.register({
@@ -758,6 +1449,45 @@ describe('ToolRuntime', () => {
       expect(seen[0]?.signal).toBe(controller.signal)
     })
 
+    it('closes prerequisite binding before an approval request can suspend admission', async () => {
+      const ctx = await approvalSetup()
+      const entered = Promise.withResolvers<undefined>()
+      const release = Promise.withResolvers<ApprovalOutcome>()
+      let retained: Parameters<ToolRuntime['bindPrerequisite']>[0] | undefined
+      let verifications = 0
+      let commits = 0
+      ctx.on('approval/request', () => {
+        entered.resolve(undefined)
+        return release.promise
+      })
+      ctx.on('tools/pre-execute', async (exec): Promise<PreToolDecision> => {
+        retained = exec
+        return { kind: 'ask' }
+      })
+
+      const pending = ctx.tools.execute({
+        signal: testToolSignal,
+        callId: CallId('approval-closes-prerequisite-binding'),
+        name: 'echo',
+        arguments: { text: 'approved' },
+        agent: fakeAgent(),
+      })
+
+      await entered.promise
+      expect(retained).toBeDefined()
+      expect(() => ctx.tools.bindPrerequisite(retained!, echoTool, {
+        async beforeBody() { verifications += 1 },
+        async commit() { commits += 1 },
+      })).toThrow('prerequisite binding is closed')
+
+      release.resolve('allowed-once')
+      await expect(pending).resolves.toMatchObject({
+        isError: false,
+        content: [{ type: 'text', text: 'approved' }],
+      })
+      expect({ verifications, commits }).toEqual({ verifications: 0, commits: 0 })
+    })
+
     it('denies with the user-rejection reason on rejected', async () => {
       const ctx = await approvalSetup()
       ctx.on('approval/request', () => Promise.resolve<ApprovalOutcome>('rejected'))
@@ -1256,7 +1986,7 @@ describe('ToolRuntime', () => {
     expect(dispatched).toBe(0)
   })
 
-  it('uses ABORTED_BEFORE_DISPATCH when cancellation overtakes a wrapper short-circuit', async () => {
+  it('preserves a successful wrapper short-circuit when cancellation overtakes it', async () => {
     const ctx = await setup()
     let dispatched = 0
     ctx.tools.register({
@@ -1292,14 +2022,127 @@ describe('ToolRuntime', () => {
     release.resolve(undefined)
 
     await expect(pending).resolves.toMatchObject({
-      content: [{ type: 'text', text: 'Error: tool call aborted before dispatch' }],
-      isError: true,
-      error: { info: { name: 'AbortError', code: TOOL_ABORTED_BEFORE_DISPATCH } },
+      content: [{ type: 'text', text: 'wrapper success' }],
+      isError: false,
+      value: 'wrapper success',
       additionalContexts: [{ source: { kind: 'plugin', plugin: 'wrapper' } }],
     })
     expect(dispatched).toBe(0)
   })
 
+  it.each(['sequential', 'concurrent'] as const)(
+    'rejects %s replay of one scheduler dispatch before a second body',
+    async (mode) => {
+      const ctx = await setup()
+      const entered = Promise.withResolvers<undefined>()
+      const release = Promise.withResolvers<undefined>()
+      let bodies = 0
+      ctx.tools.register({
+        ...echoTool,
+        name: `scheduler-dispatch-${mode}`,
+        async execute() {
+          bodies += 1
+          entered.resolve(undefined)
+          if (mode === 'concurrent') await release.promise
+          return 'once'
+        },
+      })
+      const scheduler = ctx.tools[TOOL_RUNTIME_SCHEDULER]
+      const prepared = await scheduler.prepare({
+        signal: testToolSignal,
+        callId: CallId(`scheduler-dispatch-${mode}`),
+        name: `scheduler-dispatch-${mode}`,
+        arguments: {},
+      })
+      expect(prepared.kind).toBe('dispatch')
+      if (prepared.kind !== 'dispatch') throw new Error('expected dispatch preparation')
+
+      const first = scheduler.dispatch(prepared.exec)
+      await entered.promise
+      await expect(scheduler.dispatch(prepared.exec))
+        .rejects.toThrow('scheduler execution cannot dispatch from its current stage')
+      release.resolve(undefined)
+      const outcome = await first
+      expect(bodies).toBe(1)
+      if (outcome.kind === 'post-result') {
+        await scheduler.finalize(prepared.exec, outcome.result)
+      } else {
+        scheduler.finish(prepared.exec, outcome.result)
+      }
+    },
+  )
+
+  it('rejects double finalization without repeating result notification or prerequisite commit', async () => {
+    const ctx = await setup()
+    let commits = 0
+    let notifications = 0
+    const tool = { ...echoTool, name: 'scheduler-double-finalize' }
+    ctx.tools.register(tool)
+    ctx.on('tools/pre-execute', async (exec, next) => {
+      ctx.tools.bindPrerequisite(exec, tool, {
+        async beforeBody() {},
+        async commit() { commits += 1 },
+      })
+      return next()
+    })
+    ctx.on('tools/result', () => { notifications += 1 })
+    const scheduler = ctx.tools[TOOL_RUNTIME_SCHEDULER]
+    const prepared = await scheduler.prepare({
+      signal: testToolSignal,
+      callId: CallId('scheduler-double-finalize'),
+      name: tool.name,
+      arguments: { text: 'once' },
+    })
+    if (prepared.kind !== 'dispatch') throw new Error('expected dispatch preparation')
+    const outcome = await scheduler.dispatch(prepared.exec)
+    if (outcome.kind !== 'post-result') throw new Error('expected post result')
+
+    const first = await scheduler.finalize(prepared.exec, outcome.result)
+    await expect(scheduler.finalize(prepared.exec, outcome.result))
+      .rejects.toThrow('scheduler execution cannot finalize from its current stage')
+
+    expect(first).toMatchObject({ isError: false, value: 'once' })
+    expect({ commits, notifications }).toEqual({ commits: 1, notifications: 1 })
+  })
+
+  it.each(['execute', 'post'] as const)(
+    'rejects prerequisite binding from the tools/%s phase after ordered admission',
+    async (phase) => {
+      const ctx = await setup()
+      let verifications = 0
+      let commits = 0
+      const tool = { ...echoTool, name: `prerequisite-late-${phase}` }
+      ctx.tools.register(tool)
+      const attempt = (exec: Parameters<ToolRuntime['bindPrerequisite']>[0]) => {
+        ctx.tools.bindPrerequisite(exec, tool, {
+          async beforeBody() { verifications += 1 },
+          async commit() { commits += 1 },
+        })
+      }
+      if (phase === 'execute') {
+        ctx.on('tools/execute', async (exec) => {
+          expect(() => attempt(exec)).toThrow('prerequisite binding is closed')
+          return { isError: false, value: 'wrapper', content: [{ type: 'text', text: 'wrapper' }] }
+        })
+      } else {
+        ctx.on('tools/post-execute', async (exec) => {
+          expect(() => attempt(exec)).toThrow('prerequisite binding is closed')
+          return { kind: 'accept' as const }
+        })
+      }
+
+      const result = await ctx.tools.execute({
+        signal: testToolSignal,
+        callId: CallId(`prerequisite-late-${phase}`),
+        name: tool.name,
+        arguments: { text: 'body' },
+      })
+
+      expect(result.isError).toBe(false)
+      expect({ verifications, commits }).toEqual({ verifications: 0, commits: 0 })
+    },
+  )
+
   it('replaces a late wrapper success with ABORTED and preserves deferred contexts', async () => {
     const ctx = await setup()
     ctx.tools.register({
diff --git a/packages/fs/tool-fs/src/sandbox.ts b/packages/fs/tool-fs/src/sandbox.ts
index 695a9fdfc9..73fabfcc2f 100644
--- a/packages/fs/tool-fs/src/sandbox.ts
+++ b/packages/fs/tool-fs/src/sandbox.ts
@@ -13,7 +13,7 @@
 import type { Context } from '@deepseek-ai/cordis'
 import type { ToolExecution } from '@deepseek-ai/dsh-tools'
 import type { SandboxExecutionPolicy, SandboxMode } from '@deepseek-ai/dsh-sandbox'
-import { ESCALATION_TARGETS, approveEscalation, escalationHintMarker, sandboxDenialMarker, validateEscalationArgs } from '@deepseek-ai/dsh-sandbox'
+import { ESCALATION_TARGETS, approveEscalation, escalationHintMarker, normalizeEscalationArgs, sandboxDenialMarker } from '@deepseek-ai/dsh-sandbox'
 import type { SandboxPolicyService } from '@deepseek-ai/dsh-sandbox-policy'
 import { FsError } from '@deepseek-ai/dsh-fs'
 
@@ -85,9 +85,13 @@ export class FsSandboxController {
    *   unsandboxed backend.
    */
   async resolvePolicy(toolName: string, args: FsEscalationArgs, exec: ToolExecution): Promise<SandboxExecutionPolicy | undefined> {
-    validateEscalationArgs(args.sandbox_permissions, args.justification)
     const standingPolicy = this.policy?.resolve({ ...exec.agent ? { session: exec.agent.session } : {} })
-    if (args.sandbox_permissions === undefined || args.justification === undefined) {
+    const escalation = normalizeEscalationArgs(
+      args.sandbox_permissions,
+      args.justification,
+      standingPolicy?.mode,
+    )
+    if (escalation === undefined) {
       return standingPolicy
     }
     if (this.escalationModes.length === 0) {
@@ -95,7 +99,7 @@ export class FsSandboxController {
     }
     const policy = standingPolicy as SandboxExecutionPolicy
     const approvedMode = await approveEscalation(
-      { requestedMode: args.sandbox_permissions, justification: args.justification, effectiveMode: policy.mode, subject: 'operation' },
+      { requestedMode: escalation.sandboxPermissions, justification: escalation.justification, effectiveMode: policy.mode, subject: 'operation' },
       {
         approver: this.ctx.get('approval'),
         agent: exec.agent,
diff --git a/packages/fs/tool-fs/tests/tools.spec.ts b/packages/fs/tool-fs/tests/tools.spec.ts
index cad6531766..a688524614 100644
--- a/packages/fs/tool-fs/tests/tools.spec.ts
+++ b/packages/fs/tool-fs/tests/tools.spec.ts
@@ -857,6 +857,25 @@ describe('sandbox escalation API (write/edit)', () => {
     expect(fs.stamped).toEqual([{ mode: 'read-only', workspaceRoot: resolve('/session-project') }])
   })
 
+  it('treats the schema-default mode with a blank reason as ordinary under full access', async () => {
+    const { ctx, fs } = await setupConfining()
+    const agent = escalationAgent([{ type: 'sandbox/mode', data: { mode: 'danger-full-access' } }])
+    fs.files.set('key:a.txt', 'before')
+    await call(ctx, 'read', { file_path: 'a.txt' }, agent)
+
+    const result = await call(ctx, 'edit', {
+      file_path: 'a.txt',
+      old_string: 'before',
+      new_string: 'after',
+      sandbox_permissions: 'workspace-write',
+      justification: '',
+    }, agent)
+
+    expect(result.isError).toBe(false)
+    expect(fs.files.get('key:a.txt')).toBe('after')
+    expect(fs.stamped).toEqual([{ mode: 'danger-full-access', workspaceRoot: resolve('/session-project') }])
+  })
+
   it('a denied write maps to the shared marker plus the escalation hint (isError)', async () => {
     const { ctx, fs } = await setupConfining()
     fs.rejectWith = new FsError('denied', 'FS_SANDBOX_DENIED')
diff --git a/packages/goal/goal/src/index.ts b/packages/goal/goal/src/index.ts
index 1aedfe7185..b1047e9fe0 100644
--- a/packages/goal/goal/src/index.ts
+++ b/packages/goal/goal/src/index.ts
@@ -59,9 +59,16 @@ export { decodeGoalChange, foldGoal, goalChangeRef } from './fold.ts'
 declare module '@deepseek-ai/cordis' {
   interface Context {
     goals: GoalService
+    /** Optional deployment-owned synchronous completion evaluator. */
+    dshAcceptance?: DshAcceptanceService
   }
 }
 
+/** Optional service seam consulted before GoalService mutates completion state. */
+export interface DshAcceptanceService {
+  assertGoalCompletion(agent: Agent, ref: GoalRef): void
+}
+
 /** Wire payload schema of the `goal` projection (whole current goal or pre-create/cleared null). */
 const goalProjectionSchema: ZodType<GoalProjection | null> = zod.union([
   zod.object({
@@ -335,6 +342,7 @@ export class GoalService extends TypertRemoteService {
    */
   @Remote('complete')
   complete(agent: Agent, ref: GoalRef): GoalView {
+    this.ctx.get('dshAcceptance')?.assertGoalCompletion(agent, ref)
     return this.transition(
       agent,
       ref,
diff --git a/packages/goal/goal/tests/goal.spec.ts b/packages/goal/goal/tests/goal.spec.ts
index e94e0754a0..fdd267bb9a 100644
--- a/packages/goal/goal/tests/goal.spec.ts
+++ b/packages/goal/goal/tests/goal.spec.ts
@@ -297,6 +297,44 @@ describe('GoalService mutations', () => {
     expect(() => ctx.goals.resume(agent, goal)).toThrow(expect.objectContaining({ code: 'GOAL_INVALID_TRANSITION' }))
   })
 
+  it('does not mutate goal state when acceptance denies', async () => {
+    const { ctx, agent, session } = await harness()
+    const goal = ctx.goals.create(agent, { objective: 'require authoritative acceptance' })
+    const eventsBefore = session.events.length
+    let assertions = 0
+    ctx.provide('dshAcceptance', {
+      assertGoalCompletion(candidate, ref) {
+        assertions += 1
+        expect(candidate).toBe(agent)
+        expect(ref).toBe(goal)
+        throw new Error('acceptance denied')
+      },
+    })
+
+    expect(() => ctx.goals.complete(agent, goal)).toThrow('acceptance denied')
+    expect(assertions).toBe(1)
+    expect(session.events).toHaveLength(eventsBefore)
+    expect(ctx.goals.get(agent)).toMatchObject({
+      id: goal.id,
+      revision: goal.revision,
+      phase: 'active',
+      activation: 'armed',
+    })
+  })
+
+  it('preserves completion when no acceptance provider is installed', async () => {
+    const { ctx, agent } = await harness()
+    const goal = ctx.goals.create(agent, { objective: 'remain ungoverned' })
+
+    expect(ctx.get('dshAcceptance')).toBeUndefined()
+    expect(ctx.goals.complete(agent, goal)).toMatchObject({
+      id: goal.id,
+      revision: goal.revision + 1,
+      phase: 'complete',
+      activation: 'disarmed',
+    })
+  })
+
   it('allows completion from every stopped phase and replacement only after completion', async () => {
     const phases = ['paused', 'blocked'] as const
     for (const phase of phases) {
diff --git a/packages/llm/llm/src/index.ts b/packages/llm/llm/src/index.ts
index e87c428d06..61d90a7432 100644
--- a/packages/llm/llm/src/index.ts
+++ b/packages/llm/llm/src/index.ts
@@ -171,6 +171,12 @@ export interface PreparedLlmCall {
   stream(options: GenerateOptions): AsyncIterable<StreamChunk>
 }
 
+/**
+ * Monotonic asynchronous admission before the `llm/stream` waterfall.
+ * Returning a stable reason denies the call; `undefined` leaves it unchanged.
+ */
+export type LlmGuard = (options: GenerateOptions) => string | undefined | Promise<string | undefined>
+
 /**
  * Provider-wire adapter for the harness message and stream vocabulary. Register implementations
  * with `ctx.llm.registerAdapter(providers, adapter)`. Every provider HTTP request must include
@@ -284,6 +290,7 @@ export interface DirectoryRegistrationHandle {
 export class LlmRuntime extends Service {
   private adapters = new Map<string, AdapterRegistration>()
   private directory = new Map<string, LlmConfigurableProvider>()
+  private guards = new Map<symbol, LlmGuard>()
   private discoveries = new Map<
     string,
     (request: LlmModelDiscoveryRequest) => Promise<readonly LlmDiscoveredModel[]>
@@ -293,6 +300,33 @@ export class LlmRuntime extends Service {
     super(ctx, 'llm')
   }
 
+  /**
+   * Register one effect-owned monotonic model-call guard. Guards run before
+   * cooperative `llm/stream` middleware so a short-circuit, cache, or routing
+   * listener cannot bypass package-owned admission.
+   */
+  guard(guard: LlmGuard): () => void {
+    if (typeof guard !== 'function') throw new TypeError('llm.guard() requires a function')
+    const key = Symbol()
+    return this.ctx.effect(() => {
+      this.guards.set(key, guard)
+      return () => { this.guards.delete(key) }
+    }, 'llm.guard()')
+  }
+
+  /** First monotonic denial before model-call middleware. */
+  private async guardReason(options: GenerateOptions): Promise<string | undefined> {
+    for (const guard of this.guards.values()) {
+      const reason = await guard(options)
+      if (reason === undefined) continue
+      if (typeof reason !== 'string' || reason.length === 0) {
+        throw new TypeError('llm.guard() must return a non-empty reason or undefined')
+      }
+      return reason
+    }
+    return undefined
+  }
+
   /** Notify topology observers without letting one broken listener veto the commit. */
   private emitAdaptersUpdated(): void {
     // Cordis emit uses Array.map: one synchronous throw starves later
@@ -914,4 +948,20 @@ export class LlmRuntime extends Service {
     return this.streamWithRegistration(options)
   }
 
+  private async * guardedStream(
+    options: GenerateOptions,
+    dispatch: () => AsyncIterable<StreamChunk>,
+  ): AsyncGenerator<StreamChunk> {
+    try {
+      const denialReason = await this.guardReason(options)
+      if (denialReason !== undefined) {
+        throw new LlmError('LLM call denied by a monotonic guard', denialReason)
+      }
+    } catch (error: unknown) {
+      yield adapterFailureChunk(error, options.signal)
+      return
+    }
+    yield* dispatch()
+  }
+
   private streamWithRegistration(
@@ -920,9 +970,9 @@ export class LlmRuntime extends Service {
   ): AsyncIterable<StreamChunk> {
-    return this.ctx.waterfall(
+    return this.guardedStream(options, () => this.ctx.waterfall(
       this,
       'llm/stream',
       options,
       () => this.adapterStream(options, prepared),
-    )
+    ))
   }
 }
 
diff --git a/packages/llm/llm/tests/service.spec.ts b/packages/llm/llm/tests/service.spec.ts
index ed7d8d9bf8..8ce3578dca 100644
--- a/packages/llm/llm/tests/service.spec.ts
+++ b/packages/llm/llm/tests/service.spec.ts
@@ -457,6 +457,87 @@ describe('LlmRuntime', () => {
     })()).rejects.toBe(consumerFailure)
   })
 
+  it('runs effect-owned async guards before cooperative middleware and adapter dispatch', async () => {
+    let adapterCalls = 0
+    let guardCalls = 0
+    const ctx = new Context()
+    await ctx.plugin(LlmRuntime)
+    ctx.llm.registerAdapter(['test'], new class extends ScriptedAdapter {
+      override async * stream(options: GenerateOptions): AsyncIterable<StreamChunk> {
+        adapterCalls += 1
+        yield* super.stream(options)
+      }
+    }(SCRIPT))
+    const guardFiber = ctx.plugin({
+      inject: ['llm'],
+      apply(guardCtx) {
+        guardCtx.llm.guard(async (options) => {
+          guardCalls += 1
+          await Promise.resolve(options.sessionId)
+          return 'HEALTH_BLOCKED'
+        })
+      },
+    })
+    await guardFiber
+    ctx.on('llm/stream', (_options, next) => next(), { prepend: true })
+
+    const blocked = await collect(ctx.llm.stream({
+      provider: 'test',
+      model: 'test',
+      messages: [],
+    }))
+    expect(blocked.at(-1)).toMatchObject({
+      type: 'finish',
+      reason: { kind: 'error', failure: { code: 'HEALTH_BLOCKED' } },
+    })
+    expect({ adapterCalls, guardCalls }).toEqual({ adapterCalls: 0, guardCalls: 1 })
+
+    await guardFiber.dispose()
+    await collect(ctx.llm.stream({ provider: 'test', model: 'test', messages: [] }))
+    expect({ adapterCalls, guardCalls }).toEqual({ adapterCalls: 1, guardCalls: 1 })
+  })
+
+  it('denies before middleware can short-circuit a guarded model call', async () => {
+    let adapterCalls = 0
+    let guardCalls = 0
+    let middlewareCalls = 0
+    const ctx = new Context()
+    await ctx.plugin(LlmRuntime)
+    ctx.llm.registerAdapter(['test'], new class extends ScriptedAdapter {
+      override async * stream(options: GenerateOptions): AsyncIterable<StreamChunk> {
+        adapterCalls += 1
+        yield* super.stream(options)
+      }
+    }(SCRIPT))
+    ctx.llm.guard(() => { guardCalls += 1; return 'HEALTH_BLOCKED' })
+    ctx.on('llm/stream', () => {
+      middlewareCalls += 1
+      return (async function* () { yield* SCRIPT })()
+    }, { prepend: true })
+
+    const blocked = await collect(ctx.llm.stream({ provider: 'test', model: 'test', messages: [] }))
+    expect(blocked.at(-1)).toMatchObject({
+      type: 'finish',
+      reason: { kind: 'error', failure: { code: 'HEALTH_BLOCKED' } },
+    })
+    expect({ adapterCalls, guardCalls, middlewareCalls })
+      .toEqual({ adapterCalls: 0, guardCalls: 1, middlewareCalls: 0 })
+  })
+
+  it('preserves NO_ADAPTER after an allowing guard', async () => {
+    const ctx = new Context()
+    let guardCalls = 0
+    await ctx.plugin(LlmRuntime)
+    ctx.llm.guard(() => { guardCalls += 1 })
+
+    const result = await collect(ctx.llm.stream({ provider: 'missing', model: 'test', messages: [] }))
+    expect(result.at(-1)).toMatchObject({
+      type: 'finish',
+      reason: { kind: 'error', failure: { code: 'NO_ADAPTER' } },
+    })
+    expect(guardCalls).toBe(1)
+  })
+
   it('awaits adapter cleanup on downstream close and leaves cleanup failure thrown', async () => {
     const cleanup = new Error('cleanup failed')
     let cleanupCalls = 0
diff --git a/packages/sandbox/sandbox/src/escalation.ts b/packages/sandbox/sandbox/src/escalation.ts
index e0b9a2ce63..a9731e90fd 100644
--- a/packages/sandbox/sandbox/src/escalation.ts
+++ b/packages/sandbox/sandbox/src/escalation.ts
@@ -60,6 +60,45 @@ export function validateEscalationArgs(sandboxPermissions: string | undefined, j
   }
 }
 
+/** A validated escalation pair, ready for the strictly-wider approval path. */
+export interface EscalationArgs {
+  sandboxPermissions: string
+  justification: string
+}
+
+/** Classify the only schema echoes that cannot widen the effective mode. */
+export function isNonWideningSandboxEcho(
+  sandboxPermissions: string | undefined,
+  effectiveMode: SandboxMode | undefined,
+): boolean {
+  return effectiveMode !== undefined
+    && (sandboxPermissions === effectiveMode
+      || (sandboxPermissions === 'workspace-write' && effectiveMode === 'danger-full-access'))
+}
+
+/**
+ * Normalize model/tool-protocol echoes that cannot widen the current call.
+ * Some callers materialize optional schema fields as the effective mode, or
+ * as the schema's `workspace-write` floor under `danger-full-access`, together
+ * with a concretized blank optional reason. Those shapes request no additional
+ * authority, so treat them as an ordinary call. Every other shape retains the
+ * strict pairing validation and approval choreography.
+ */
+export function normalizeEscalationArgs(
+  sandboxPermissions: string | undefined,
+  justification: string | undefined,
+  effectiveMode: SandboxMode | undefined,
+): EscalationArgs | undefined {
+  if (isNonWideningSandboxEcho(sandboxPermissions, effectiveMode)
+    && justification !== undefined
+    && justification.trim().length === 0) {
+    return undefined
+  }
+  validateEscalationArgs(sandboxPermissions, justification)
+  if (sandboxPermissions === undefined || justification === undefined) return undefined
+  return { sandboxPermissions, justification }
+}
+
 /**
  * The model-facing denial marker — the one vocabulary both enforcing families
  * teach and report, so the model recognizes a policy denial identically
diff --git a/packages/sandbox/sandbox/src/index.ts b/packages/sandbox/sandbox/src/index.ts
index 2d9ee5b91b..28282b03e9 100644
--- a/packages/sandbox/sandbox/src/index.ts
+++ b/packages/sandbox/sandbox/src/index.ts
@@ -14,10 +14,12 @@ export {
   WIDER_MODES,
   approveEscalation,
   escalationHintMarker,
+  isNonWideningSandboxEcho,
+  normalizeEscalationArgs,
   sandboxDenialMarker,
   validateEscalationArgs,
 } from './escalation.ts'
-export type { EscalationApproval, EscalationApprover, EscalationOutcome, EscalationRequest } from './escalation.ts'
+export type { EscalationApproval, EscalationApprover, EscalationArgs, EscalationOutcome, EscalationRequest } from './escalation.ts'
 export { canonicalPath, writableRoots } from './roots.ts'
 
 /**
diff --git a/packages/sandbox/sandbox/tests/escalation.spec.ts b/packages/sandbox/sandbox/tests/escalation.spec.ts
index 15810d09d5..6073180efc 100644
--- a/packages/sandbox/sandbox/tests/escalation.spec.ts
+++ b/packages/sandbox/sandbox/tests/escalation.spec.ts
@@ -12,6 +12,8 @@ import {
   WIDER_MODES,
   approveEscalation,
   escalationHintMarker,
+  isNonWideningSandboxEcho,
+  normalizeEscalationArgs,
   sandboxDenialMarker,
   validateEscalationArgs,
 } from '@deepseek-ai/dsh-sandbox'
@@ -42,6 +44,23 @@ describe('validateEscalationArgs', () => {
   })
 })
 
+describe('normalizeEscalationArgs', () => {
+  it('normalizes only explicit blank echoes that cannot widen full access', () => {
+    expect(isNonWideningSandboxEcho('danger-full-access', 'danger-full-access')).toBe(true)
+    expect(isNonWideningSandboxEcho('workspace-write', 'danger-full-access')).toBe(true)
+    expect(isNonWideningSandboxEcho('danger-full-access', 'workspace-write')).toBe(false)
+    expect(normalizeEscalationArgs('danger-full-access', '', 'danger-full-access')).toBeUndefined()
+    expect(normalizeEscalationArgs('workspace-write', ' ', 'danger-full-access')).toBeUndefined()
+  })
+
+  it('preserves pairing and nonblank non-widening validation', () => {
+    expect(() => normalizeEscalationArgs('workspace-write', undefined, 'danger-full-access'))
+      .toThrow(/requires a justification/)
+    expect(normalizeEscalationArgs('workspace-write', 'requested explicitly', 'danger-full-access'))
+      .toEqual({ sandboxPermissions: 'workspace-write', justification: 'requested explicitly' })
+  })
+})
+
 describe('the model-facing markers', () => {
   it('the denial marker names the mode', () => {
     expect(sandboxDenialMarker('read-only')).toBe('[sandbox: file access denied under read-only mode]')
diff --git a/packages/shell/tool-bash/src/index.ts b/packages/shell/tool-bash/src/index.ts
index 37c4a762e3..f149209c6f 100644
--- a/packages/shell/tool-bash/src/index.ts
+++ b/packages/shell/tool-bash/src/index.ts
@@ -20,7 +20,7 @@ import type {} from '@deepseek-ai/dsh-jobs'
 import type {} from '@deepseek-ai/dsh-user-approval'
 import type {} from '@deepseek-ai/dsh-shell-env'
 import type { SandboxExecutionPolicy, SandboxMode } from '@deepseek-ai/dsh-sandbox'
-import { ESCALATION_TARGETS, approveEscalation, canonicalPath, validateEscalationArgs } from '@deepseek-ai/dsh-sandbox'
+import { ESCALATION_TARGETS, approveEscalation, canonicalPath, normalizeEscalationArgs } from '@deepseek-ai/dsh-sandbox'
 import type { SandboxPolicyService } from '@deepseek-ai/dsh-sandbox-policy'
 import { DSH_ENV_PREFIX } from '@deepseek-ai/dsh-shell'
 import type { ShellRunResult } from '@deepseek-ai/dsh-shell'
@@ -62,9 +62,6 @@ function validateBashArgs(args: BashToolArgs): void {
   if (args.timeoutMs !== undefined && (!Number.isFinite(args.timeoutMs) || args.timeoutMs <= 0)) {
     throw new Error(`invalid timeoutMs: expected a positive number, got ${JSON.stringify(args.timeoutMs)}`)
   }
-  // The escalation pairing (sandbox_permissions ⇔ justification, non-empty) is
-  // the shared rule both enforcing families validate identically.
-  validateEscalationArgs(args.sandbox_permissions, args.justification)
 }
 
 function bashDescription(backgroundEnabled: boolean, escalationModes: readonly SandboxMode[]): string {
@@ -331,8 +328,13 @@ export function apply(ctx: Context, config: Config = {}): void {
       validateBashArgs(args)
       // Description is display metadata; workdir defaults to the caller's session.
       const standingPolicy = resolveSandboxPolicy(exec)
-      const approvedMode = args.sandbox_permissions !== undefined && args.justification !== undefined
-        ? await approveBashEscalation(args.sandbox_permissions, args.justification, exec, standingPolicy)
+      const escalation = normalizeEscalationArgs(
+        args.sandbox_permissions,
+        args.justification,
+        standingPolicy?.mode,
+      )
+      const approvedMode = escalation !== undefined
+        ? await approveBashEscalation(escalation.sandboxPermissions, escalation.justification, exec, standingPolicy)
         : undefined
       const policy = approvedMode === undefined
         ? standingPolicy
diff --git a/packages/shell/tool-bash/tests/tools.spec.ts b/packages/shell/tool-bash/tests/tools.spec.ts
index 0f2e4d5a20..aaceb87775 100644
--- a/packages/shell/tool-bash/tests/tools.spec.ts
+++ b/packages/shell/tool-bash/tests/tools.spec.ts
@@ -620,6 +620,19 @@ describe('sandbox escalation through the generic task producer', () => {
     expect(text(await call(ctx, 'bash', escalate, malformed))).toContain('not strictly wider')
   })
 
+  it('treats the schema-default mode with a blank reason as ordinary under full access', async () => {
+    const { ctx, bash } = await setupSandboxed()
+    const result = await call(ctx, 'bash', {
+      command: 'true',
+      description: 'run ordinary full-access command',
+      sandbox_permissions: 'workspace-write',
+      justification: '',
+    }, sandboxAgent('danger-full-access'))
+
+    expect(result.isError).toBe(false)
+    expect(bash.modes).toEqual(['danger-full-access'])
+  })
+
   it('fails closed when approval cannot be routed', async () => {
     const withoutService = await setupSandboxed()
     expect(text(await call(withoutService.ctx, 'bash', escalate, sandboxAgent()))).toContain('no approval service')
diff --git a/packages/subagent/subagent/src/child-agent.ts b/packages/subagent/subagent/src/child-agent.ts
index 7582338858..93fc2894d2 100644
--- a/packages/subagent/subagent/src/child-agent.ts
+++ b/packages/subagent/subagent/src/child-agent.ts
@@ -103,11 +103,14 @@ export function childSessionMeta(
   parent: Agent,
   childDepth: number,
   lineageSeedLength: number,
+  workspaceCwd?: string,
 ): NonNullable<CreateAgentOptions['meta']> {
   const parentHeader = parent.session.header
   const agentPreset = parent.ctx.get('agentPresets')?.composedPreset(parent.ctx)
   return {
-    ...parentHeader.cwd !== undefined ? { cwd: parentHeader.cwd } : {},
+    ...workspaceCwd !== undefined
+      ? { cwd: workspaceCwd }
+      : parentHeader.cwd !== undefined ? { cwd: parentHeader.cwd } : {},
     ...agentPreset === undefined ? {} : { agentPreset },
     parentSession: parentHeader.id,
     // Navigation classification only; the descriptor remains the authority
diff --git a/packages/subagent/subagent/src/continuation.ts b/packages/subagent/subagent/src/continuation.ts
index d826efbfdb..3ec4739c02 100644
--- a/packages/subagent/subagent/src/continuation.ts
+++ b/packages/subagent/subagent/src/continuation.ts
@@ -49,7 +49,17 @@ import {
 import type { DelegatedPolicyOverrides } from './child-agent.ts'
 import { assertSubagentMaxDepth } from './depth.ts'
 import { seedDescriptorTurn } from './descriptor-seed.ts'
-import type { ContinuableCreateRequest, ContinuableCreateSpec, SubagentResult, SubagentStartRequest } from './types.ts'
+import type {
+  ContinuableCreateRequest,
+  ContinuableCreateSpec,
+  ContinuableWorkspaceActivationRequest,
+  ContinuableWorkspaceDescriptor,
+  ContinuableWorkspacePrepareRequest,
+  ContinuableWorkspacePrepared,
+  ContinuableWorkspaceSelection,
+  SubagentResult,
+  SubagentStartRequest,
+} from './types.ts'
 import type { ActivationObserver, ActivationTerminal } from './lifecycle.ts'
 import { SubagentError } from './error.ts'
 import type SubagentActivationSetupRegistry from './activation-setup-registry.ts'
@@ -121,6 +131,8 @@ export interface ContinuableStartSpec {
   readonly request: Omit<SubagentStartRequest, 'label' | 'signal' | 'outputSchema'>
   /** Caller cancellation, owning the operation only until inbox acceptance. */
   readonly signal: AbortSignal
+  /** Trusted host-only workspace selection. Never part of model tool input. */
+  readonly workspace?: ContinuableWorkspaceSelection
 }
 
 /** Identities returned once a continuable child accepted its initial prompt. */
@@ -173,6 +185,9 @@ interface ContinuationHost {
    * @returns the provider's detached creation spec.
    */
   prepareContinuable(name: string, request: ContinuableCreateRequest): Promise<ContinuableCreateSpec>
+  snapshotWorkspace(selection: ContinuableWorkspaceSelection, parent: Agent): ContinuableWorkspaceDescriptor
+  prepareWorkspace(request: ContinuableWorkspacePrepareRequest): Promise<ContinuableWorkspacePrepared>
+  activateWorkspace(request: ContinuableWorkspaceActivationRequest): Promise<void>
   /**
    * Build the lifecycle observer for one Activation's residency epoch.
    * @param provider - the provider name recorded in the durable descriptor.
@@ -257,6 +272,7 @@ interface MaterializeInputs {
   }
   agentOptions: AgentOptions
   composition: { persona?: string | undefined; toolFilter?: ToolRestriction | undefined }
+  workspace?: ContinuableWorkspaceDescriptor
   signal: AbortSignal
 }
 
@@ -410,2 +426,5 @@ export class SubagentContinuationManager {
     const childDepth = resolveChildDepth(parent, request.maxDepth)
+    const workspace = spec.workspace === undefined
+      ? undefined
+      : this.host.snapshotWorkspace(spec.workspace, parent)
     // Snapshot before any await: invalid descriptor JSON rejects the call
@@ -420,11 +439,21 @@ export class SubagentContinuationManager {
       ...agentModel !== undefined ? { agentModel } : {},
       ...request.persona !== undefined ? { persona: request.persona } : {},
       ...request.toolFilter !== undefined ? { toolFilter: request.toolFilter } : {},
+      ...workspace !== undefined ? { workspace } : {},
     })
     // Capture before the first await: a later parent switch belongs to the
     // parent's future, not to this child.
     const delegatedPolicies = captureDelegatedPolicyOverrides(parent)
 
+    const preparedWorkspace = workspace === undefined
+      ? undefined
+      : await this.host.prepareWorkspace({
+        sessionId: childId,
+        parent,
+        signal: spec.signal,
+        descriptor: workspace,
+        ref: spec.workspace?.ref,
+      })
     const prepared = await this.host.prepareContinuable(spec.provider, {
       sessionId: childId,
       parent,
@@ -440,9 +469,14 @@ export class SubagentContinuationManager {
         childId,
         provider: spec.provider,
         parent,
-        create: { seed, meta: childSessionMeta(parent, childDepth, lineageSeedLength), delegatedPolicies },
+        create: {
+          seed,
+          meta: childSessionMeta(parent, childDepth, lineageSeedLength, preparedWorkspace?.cwd),
+          delegatedPolicies,
+        },
         agentOptions: resolveChildAgentOptions(parent, request.agentOptions, childDepth),
         composition: { persona: request.persona, toolFilter: request.toolFilter },
+        ...workspace === undefined ? {} : { workspace },
         signal: spec.signal,
       })
       return this.submitMaterialized(
@@ -652,6 +686,22 @@ export class SubagentContinuationManager {
     return message.id
   }
 
+  /**
+   * Terminally release one resident continuable child and its live descendants.
+   * A cold child owns no activation authority and is therefore an accepted no-op.
+   */
+  async close(parent: Agent, childId: SessionId, signal: AbortSignal): Promise<void> {
+    signal.throwIfAborted()
+    this.assertAdmitting(parent)
+    await this.locks.run(childId, async () => {
+      signal.throwIfAborted()
+      const activation = this.activations.get(childId)
+      if (activation === undefined) return
+      this.authorizeLineage(parent, childId, activation.parentSession)
+      await this.dispose(activation)
+    })
+  }
+
   /**
    * Perform one waking send to a parent, accounted against that parent's own
    * Activation when it has one. Registering the id before the send is what
@@ -910,6 +960,32 @@ export class SubagentContinuationManager {
         'NOT_RESUMABLE',
       )
     }
+    if (descriptor.workspace !== undefined) {
+      let prepared: ContinuableWorkspacePrepared
+      try {
+        prepared = await this.host.prepareWorkspace({
+          sessionId: childId,
+          parent,
+          signal: options.signal,
+          descriptor: descriptor.workspace,
+          ...loaded.meta.cwd === undefined ? {} : { persistedCwd: loaded.meta.cwd },
+        })
+      } catch (error: unknown) {
+        options.signal.throwIfAborted()
+        if (error instanceof SubagentError) throw error
+        throw new SubagentError(
+          `subagent "${childId}" workspace is unavailable`,
+          'NOT_RESUMABLE',
+          { cause: error },
+        )
+      }
+      if (loaded.meta.cwd === undefined || prepared.cwd !== loaded.meta.cwd) {
+        throw new SubagentError(
+          `subagent "${childId}" workspace identity does not match its durable session`,
+          'NOT_RESUMABLE',
+        )
+      }
+    }
     let activation: Activation
     try {
       activation = await this.materialize({
@@ -921,6 +997,7 @@ export class SubagentContinuationManager {
           ...descriptor.agentModel !== undefined ? { model: descriptor.agentModel } : {},
         },
         composition: { persona: descriptor.persona, toolFilter: descriptor.toolFilter },
+        ...descriptor.workspace === undefined ? {} : { workspace: descriptor.workspace },
         signal: options.signal,
       })
     } catch (error: unknown) {
@@ -1022,6 +1099,19 @@ export class SubagentContinuationManager {
         setup,
       })
 
+    if (inputs.workspace !== undefined) {
+      try {
+        await this.host.activateWorkspace({
+          agent: handle.agent,
+          descriptor: inputs.workspace,
+          signal: inputs.signal,
+        })
+      } catch (error: unknown) {
+        await handle.dispose().catch(() => undefined)
+        throw error
+      }
+    }
+
     const activation: Activation = {
       childId,
       // The durable lineage, not merely the caller: creation stamps this same
diff --git a/packages/subagent/subagent/src/descriptor.ts b/packages/subagent/subagent/src/descriptor.ts
index 6d9dedee75..ff5624fbff 100644
--- a/packages/subagent/subagent/src/descriptor.ts
+++ b/packages/subagent/subagent/src/descriptor.ts
@@ -24,6 +24,7 @@
 import { snapshotJsonValue } from '@deepseek-ai/dsh-session'
 import type { SessionEvent } from '@deepseek-ai/dsh-session'
 import type { ToolRestriction } from '@deepseek-ai/dsh-tools'
+import type { ContinuableWorkspaceDescriptor } from './types.ts'
 
 declare module '@deepseek-ai/dsh-session/types' {
   interface SessionEventMap {
@@ -44,7 +45,8 @@ declare module '@deepseek-ai/dsh-session/types' {
  * Supporting another composition input is a deliberate version change, never
  * an implicit extra field.
  */
-export const SUBAGENT_DESCRIPTOR_VERSION = 2
+export const SUBAGENT_DESCRIPTOR_VERSION = 3
+const LEGACY_SUBAGENT_DESCRIPTOR_VERSION = 2
 
 /** Fields shared by every supported `subagent/descriptor` payload. */
 interface SubagentDescriptorBase {
@@ -80,6 +82,8 @@ export interface ContinuableSubagentDescriptorData extends SubagentDescriptorBas
   readonly persona?: string
   /** Child tool scoping reapplied on resume. */
   readonly toolFilter?: ToolRestriction
+  /** Host-only non-bearer identity needed to reauthenticate on cold resume. */
+  readonly workspace?: ContinuableWorkspaceDescriptor
 }
 
 /** The supported durable subagent identity and optional continuation composition. */
@@ -115,6 +119,8 @@ export interface ContinuableSubagentDescriptorInput extends SubagentDescriptorIn
   readonly persona?: string
   /** Requested child tool scoping. */
   readonly toolFilter?: ToolRestriction
+  /** Trusted host workspace identity; opaque live refs are never persisted. */
+  readonly workspace?: ContinuableWorkspaceDescriptor
 }
 
 /** Inputs {@link snapshotSubagentDescriptor} validates and detaches. */
@@ -135,7 +141,9 @@ const CONTINUABLE_DESCRIPTOR_KEYS = new Set([
   'agentModel',
   'persona',
   'toolFilter',
+  'workspace',
 ])
+const WORKSPACE_DESCRIPTOR_KEYS = new Set(['provider', 'version'])
 const TOOL_FILTER_KEYS = new Set(['allow', 'deny'])
 
 /** Whether a persisted JSON value is an object record. */
@@ -192,6 +200,19 @@ function parseToolFilter(value: unknown): ToolRestriction {
   }
 }
 
+/** Validate the non-bearer durable identity of one host workspace provider. */
+function parseWorkspace(value: unknown): ContinuableWorkspaceDescriptor {
+  if (!isRecord(value)) throw new Error('persisted subagent descriptor workspace must be an object')
+  assertKnownKeys(value, WORKSPACE_DESCRIPTOR_KEYS, 'workspace')
+  if (typeof value['provider'] !== 'string' || value['provider'].length === 0) {
+    throw new Error('persisted subagent descriptor workspace.provider must be a non-empty string')
+  }
+  if (!Number.isSafeInteger(value['version']) || (value['version'] as number) <= 0) {
+    throw new Error('persisted subagent descriptor workspace.version must be a positive safe integer')
+  }
+  return { provider: value['provider'], version: value['version'] as number }
+}
+
 /** Validate one persisted descriptor payload for the current runtime. */
 function parseSubagentDescriptor(value: unknown): SubagentDescriptorData | undefined {
   if (!isRecord(value)) {
@@ -201,7 +222,7 @@ function parseSubagentDescriptor(value: unknown): SubagentDescriptorData | undef
   if (typeof version !== 'number') {
     throw new Error('persisted subagent descriptor version must be a number')
   }
-  if (version !== SUBAGENT_DESCRIPTOR_VERSION) return undefined
+  if (version !== SUBAGENT_DESCRIPTOR_VERSION && version !== LEGACY_SUBAGENT_DESCRIPTOR_VERSION) return undefined
 
   const mode = value['mode']
   if (mode !== 'one-shot' && mode !== 'continuable') {
@@ -209,7 +230,11 @@ function parseSubagentDescriptor(value: unknown): SubagentDescriptorData | undef
   }
   assertKnownKeys(
     value,
-    mode === 'one-shot' ? ONE_SHOT_DESCRIPTOR_KEYS : CONTINUABLE_DESCRIPTOR_KEYS,
+    mode === 'one-shot'
+      ? ONE_SHOT_DESCRIPTOR_KEYS
+      : version === LEGACY_SUBAGENT_DESCRIPTOR_VERSION
+        ? new Set([...CONTINUABLE_DESCRIPTOR_KEYS].filter(key => key !== 'workspace'))
+        : CONTINUABLE_DESCRIPTOR_KEYS,
     'payload',
   )
   const provider = value['provider']
@@ -235,6 +260,9 @@ function parseSubagentDescriptor(value: unknown): SubagentDescriptorData | undef
   const toolFilter = Object.hasOwn(value, 'toolFilter')
     ? parseToolFilter(value['toolFilter'])
     : undefined
+  const workspace = Object.hasOwn(value, 'workspace')
+    ? parseWorkspace(value['workspace'])
+    : undefined
   return {
     version: SUBAGENT_DESCRIPTOR_VERSION,
     mode,
@@ -244,6 +272,7 @@ function parseSubagentDescriptor(value: unknown): SubagentDescriptorData | undef
     ...agentModel !== undefined ? { agentModel } : {},
     ...persona !== undefined ? { persona } : {},
     ...toolFilter !== undefined ? { toolFilter } : {},
+    ...workspace !== undefined ? { workspace } : {},
   }
 }
 
@@ -271,13 +300,15 @@ export function snapshotSubagentDescriptor(
 export function snapshotSubagentDescriptor(input: SubagentDescriptorInput): SubagentDescriptorData {
   const candidate: SubagentDescriptorData = input.mode === 'one-shot'
     ? {
-      version: SUBAGENT_DESCRIPTOR_VERSION,
+      version: LEGACY_SUBAGENT_DESCRIPTOR_VERSION,
       mode: input.mode,
       provider: input.provider,
       ...input.label !== undefined ? { label: input.label } : {},
     }
     : {
-      version: SUBAGENT_DESCRIPTOR_VERSION,
+      version: input.workspace === undefined
+        ? LEGACY_SUBAGENT_DESCRIPTOR_VERSION
+        : SUBAGENT_DESCRIPTOR_VERSION,
       mode: input.mode,
       provider: input.provider,
       label: input.label,
@@ -285,6 +316,7 @@ export function snapshotSubagentDescriptor(input: SubagentDescriptorInput): Suba
       ...input.agentModel !== undefined ? { agentModel: input.agentModel } : {},
       ...input.persona !== undefined ? { persona: input.persona } : {},
       ...input.toolFilter !== undefined ? { toolFilter: input.toolFilter } : {},
+      ...input.workspace !== undefined ? { workspace: input.workspace } : {},
     }
   const snapshot = snapshotJsonValue(candidate)
   if (snapshot === undefined) {
diff --git a/packages/subagent/subagent/src/index.ts b/packages/subagent/subagent/src/index.ts
index fe270f1b4c..447b5682bb 100644
--- a/packages/subagent/subagent/src/index.ts
+++ b/packages/subagent/subagent/src/index.ts
@@ -41,6 +41,12 @@ import type { SessionId } from '@deepseek-ai/dsh-session'
 import type {
   ContinuableCreateRequest,
   ContinuableCreateSpec,
+  ContinuableWorkspaceActivationRequest,
+  ContinuableWorkspaceDescriptor,
+  ContinuableWorkspacePrepareRequest,
+  ContinuableWorkspacePrepared,
+  ContinuableWorkspaceProvider,
+  ContinuableWorkspaceSelection,
   ResolvedSubagentStartRequest,
   SubagentCapabilities,
   SubagentProvider,
@@ -74,6 +80,12 @@ export { SubagentRunId } from './types.ts'
 export type {
   ContinuableCreateRequest,
   ContinuableCreateSpec,
+  ContinuableWorkspaceActivationRequest,
+  ContinuableWorkspaceDescriptor,
+  ContinuableWorkspacePrepareRequest,
+  ContinuableWorkspacePrepared,
+  ContinuableWorkspaceProvider,
+  ContinuableWorkspaceSelection,
   ResolvedSubagentStartRequest,
   SubagentCapabilities,
   SubagentProvider,
@@ -170,6 +182,7 @@ declare module '@deepseek-ai/cordis' {
 /** Named provider registry with one-shot runs, durable discovery, and continuable-child operations. */
 export class SubagentRuntime extends Service {
   private providers = new Map<string, SubagentProvider>()
+  private workspaceProviders = new Map<string, ContinuableWorkspaceProvider>()
   private continuations: SubagentContinuationManager | undefined
   /** Deployment contributions composed into unpublished continuable children. */
   private readonly setupRegistry = new SubagentActivationSetupRegistry()
@@ -186,6 +199,9 @@ export class SubagentRuntime extends Service {
     ctx.inject(['agents'], (childCtx: Context) => {
       const manager = new SubagentContinuationManager(childCtx, {
         prepareContinuable: (name, request) => this.prepareContinuable(name, request),
+        snapshotWorkspace: (selection, parent) => this.snapshotWorkspace(selection, parent),
+        prepareWorkspace: request => this.prepareWorkspace(request),
+        activateWorkspace: request => this.activateWorkspace(request),
         observeActivation: (provider, childId, parent) => this.observeActivation(provider, childId, parent),
       }, this.setupRegistry)
       this.continuations = manager
@@ -237,6 +253,13 @@ export class SubagentRuntime extends Service {
     return this.requireContinuations().followup(parent, childId, content, options)
   }
 
+  /** Terminally release one resident continuable child and its descendants. */
+  async closeContinuable(parent: Agent, childId: SessionId, signal: AbortSignal): Promise<void> {
+    const manager = this.continuations
+    if (manager === undefined) return
+    await manager.close(parent, childId, signal)
+  }
+
   /**
    * Interrupt one live continuable child's current turn under a human parent
    * address or an exact live ancestor Agent. Fire-and-return: the cancel
@@ -291,6 +314,56 @@ export class SubagentRuntime extends Service {
     )
   }
 
+  /** Register one trusted provider for host-issued continuable workspaces. */
+  registerContinuableWorkspaceProvider(provider: ContinuableWorkspaceProvider): () => void {
+    if (provider.name.length === 0 || !Number.isSafeInteger(provider.version) || provider.version <= 0) {
+      throw new SubagentError('invalid continuable workspace provider identity', 'INVALID_REQUEST')
+    }
+    return this.ctx.effect(() => {
+      if (this.workspaceProviders.has(provider.name)) {
+        throw new SubagentError(
+          `a continuable workspace provider named "${provider.name}" is already registered`,
+          'DUPLICATE_PROVIDER',
+        )
+      }
+      this.workspaceProviders.set(provider.name, provider)
+      return () => { this.workspaceProviders.delete(provider.name) }
+    }, 'subagents.registerContinuableWorkspaceProvider()')
+  }
+
+  private snapshotWorkspace(
+    selection: ContinuableWorkspaceSelection,
+    parent: Agent,
+  ): ContinuableWorkspaceDescriptor {
+    const provider = this.workspaceProviders.get(selection.provider)
+    if (provider === undefined) {
+      throw new SubagentError(
+        `continuable workspace provider "${selection.provider}" is not registered`,
+        'UNSUPPORTED_CAPABILITY',
+      )
+    }
+    provider.validate(selection.ref, parent)
+    return { provider: provider.name, version: provider.version }
+  }
+
+  private async prepareWorkspace(
+    request: ContinuableWorkspacePrepareRequest,
+  ): Promise<ContinuableWorkspacePrepared> {
+    const provider = this.workspaceProviders.get(request.descriptor.provider)
+    if (provider === undefined || provider.version !== request.descriptor.version) {
+      throw new SubagentError('continuable workspace capability is unavailable', 'UNSUPPORTED_CAPABILITY')
+    }
+    return provider.prepare(request)
+  }
+
+  private async activateWorkspace(request: ContinuableWorkspaceActivationRequest): Promise<void> {
+    const provider = this.workspaceProviders.get(request.descriptor.provider)
+    if (provider === undefined || provider.version !== request.descriptor.version) {
+      throw new SubagentError('continuable workspace capability is unavailable', 'UNSUPPORTED_CAPABILITY')
+    }
+    await provider.activate(request)
+  }
+
   /**
    * Close continuable admission below exact live parent Agents, stop only their
    * visible descendant Activations synchronously, then await admitted scoped
diff --git a/packages/subagent/subagent/src/types.ts b/packages/subagent/subagent/src/types.ts
index 2c5209765f..5d26a6f4ae 100644
--- a/packages/subagent/subagent/src/types.ts
+++ b/packages/subagent/subagent/src/types.ts
@@ -191,6 +191,56 @@ export interface ContinuableCreateSpec {
   readonly seed?: readonly SessionEvent[]
 }
 
+/** Host-only selector for one managed continuable workspace. */
+export interface ContinuableWorkspaceSelection {
+  /** Registered trusted workspace provider. */
+  readonly provider: string
+  /** Opaque same-process reference. It is never persisted or exposed to tools. */
+  readonly ref: unknown
+}
+
+/** Non-bearer workspace identity retained in a continuable descriptor. */
+export interface ContinuableWorkspaceDescriptor {
+  readonly provider: string
+  readonly version: number
+}
+
+/** Inputs a trusted workspace provider receives before child materialization. */
+export interface ContinuableWorkspacePrepareRequest {
+  readonly sessionId: SessionId
+  readonly parent: Agent
+  readonly signal: AbortSignal
+  readonly descriptor: ContinuableWorkspaceDescriptor
+  /** Present only for a fresh host-selected child. */
+  readonly ref?: unknown
+  /** Persisted immutable cwd on cold resume. */
+  readonly persistedCwd?: string
+}
+
+/** Exact session metadata resolved from one host-issued workspace. */
+export interface ContinuableWorkspacePrepared {
+  readonly cwd: string
+}
+
+/** Inputs delivered after Agent creation but before prompt admission. */
+export interface ContinuableWorkspaceActivationRequest {
+  readonly agent: Agent
+  readonly descriptor: ContinuableWorkspaceDescriptor
+  readonly signal: AbortSignal
+}
+
+/** Trusted same-process provider for host-issued continuable workspaces. */
+export interface ContinuableWorkspaceProvider {
+  readonly name: string
+  readonly version: number
+  /** Synchronously authenticate a live opaque ref before the first await. */
+  validate(ref: unknown, parent: Agent): void
+  /** Resolve fresh or durable identity before the child Agent is created. */
+  prepare(request: ContinuableWorkspacePrepareRequest): Promise<ContinuableWorkspacePrepared>
+  /** Prove runtime authority before the first inbox message can be accepted. */
+  activate(request: ContinuableWorkspaceActivationRequest): Promise<void>
+}
+
 /**
  * Why a subagent run ended. Merge-extensible (a backend may add variants);
  * consumers branch on the known cases and fall through `default`. The known
diff --git a/packages/subagent/subagent/tests/continuation.spec.ts b/packages/subagent/subagent/tests/continuation.spec.ts
index 07f497628d..800220d78f 100644
--- a/packages/subagent/subagent/tests/continuation.spec.ts
+++ b/packages/subagent/subagent/tests/continuation.spec.ts
@@ -168,6 +168,143 @@ function observeCancel(agent: Agent, callback: () => void): void {
 }
 
 describe('SubagentRuntime.startContinuable', () => {
+  it('binds and cold-resumes one host-issued workspace without exposing its opaque ref', async () => {
+    const { ctx, parent } = await setup([textResponse('first'), textResponse('second')])
+    parkParent(ctx, parent)
+    const ref = Object.freeze(Object.create(null))
+    const calls: unknown[] = []
+    ctx.subagents.registerContinuableWorkspaceProvider({
+      name: 'managed',
+      version: 1,
+      validate(candidate, caller) {
+        expect(candidate).toBe(ref)
+        expect(caller).toBe(parent)
+      },
+      async prepare(request) {
+        calls.push(request)
+        expect(request.descriptor).toEqual({ provider: 'managed', version: 1 })
+        return { cwd: '/managed/worktree' }
+      },
+      async activate({ agent, descriptor }) {
+        expect(agent.session.header.cwd).toBe('/managed/worktree')
+        expect(descriptor).toEqual({ provider: 'managed', version: 1 })
+      },
+    })
+
+    const started = await ctx.subagents.startContinuable({
+      ...startSpec(parent),
+      workspace: { provider: 'managed', ref },
+    })
+    await waitNoActivation(ctx, started.childId)
+    const loaded = await ctx.sessionPersistence.load(started.childId)
+    const descriptor = loaded.events.find(event => event.type === 'subagent/descriptor')
+    expect(descriptor?.data).toMatchObject({
+      version: SUBAGENT_DESCRIPTOR_VERSION,
+      mode: 'continuable',
+      workspace: { provider: 'managed', version: 1 },
+    })
+    expect((descriptor?.data as { workspace?: unknown }).workspace).toEqual({ provider: 'managed', version: 1 })
+    expect((descriptor?.data as { workspace?: unknown }).workspace).not.toHaveProperty('ref')
+
+    await followup(ctx, parent, started.childId, message('again'))
+    await waitNoActivation(ctx, started.childId)
+    expect(calls).toHaveLength(2)
+    expect(calls[1]).toMatchObject({ persistedCwd: '/managed/worktree' })
+    expect(calls[1]).not.toHaveProperty('ref')
+  })
+
+  it('keeps ordinary continuable descriptors on the rollback-readable v2 format', async () => {
+    const { ctx, parent } = await setup([textResponse('ordinary')])
+    parkParent(ctx, parent)
+    const started = await ctx.subagents.startContinuable(startSpec(parent))
+    await waitNoActivation(ctx, started.childId)
+    const loaded = await ctx.sessionPersistence.load(started.childId)
+    const descriptor = loaded.events.find(event => event.type === 'subagent/descriptor')
+    expect(descriptor?.data).toMatchObject({ version: 2, mode: 'continuable' })
+    expect(descriptor?.data).not.toHaveProperty('workspace')
+  })
+
+  it('fails before child creation when a host workspace provider is unavailable', async () => {
+    const { ctx, parent } = await setup([])
+    await expect(ctx.subagents.startContinuable({
+      ...startSpec(parent),
+      workspace: { provider: 'missing', ref: Object.freeze(Object.create(null)) },
+    })).rejects.toMatchObject({ code: 'UNSUPPORTED_CAPABILITY' })
+    expect(ctx.agents.list().map(agent => agent.id)).toEqual([SessionId('parent')])
+  })
+
+  it('rolls back the child when host workspace activation refuses authority', async () => {
+    const { ctx, parent } = await setup([textResponse('unused')])
+    const ref = Object.freeze(Object.create(null))
+    ctx.subagents.registerContinuableWorkspaceProvider({
+      name: 'refusing',
+      version: 1,
+      validate() {},
+      async prepare() { return { cwd: '/managed/worktree' } },
+      async activate() { throw new Error('foreign lease') },
+    })
+    await expect(ctx.subagents.startContinuable({
+      ...startSpec(parent),
+      workspace: { provider: 'refusing', ref },
+    })).rejects.toThrow(/foreign lease/)
+    expect(ctx.agents.list().map(agent => agent.id)).toEqual([SessionId('parent')])
+  })
+
+  it('returns a typed cold-resume refusal when workspace reauthentication fails', async () => {
+    const { ctx, parent } = await setup([textResponse('first')])
+    parkParent(ctx, parent)
+    const ref = Object.freeze(Object.create(null))
+    let preparations = 0
+    ctx.subagents.registerContinuableWorkspaceProvider({
+      name: 'unstable',
+      version: 1,
+      validate() {},
+      async prepare() {
+        preparations += 1
+        if (preparations > 1) throw new Error('lease unavailable')
+        return { cwd: '/managed/worktree' }
+      },
+      async activate() {},
+    })
+    const started = await ctx.subagents.startContinuable({
+      ...startSpec(parent),
+      workspace: { provider: 'unstable', ref },
+    })
+    await waitNoActivation(ctx, started.childId)
+
+    await expect(followup(ctx, parent, started.childId, message('again')))
+      .rejects.toMatchObject({ code: 'NOT_RESUMABLE' })
+    expect(ctx.agents.list().map(agent => agent.id)).toEqual([SessionId('parent')])
+  })
+
+  it('terminally closes one exact child and its descendants', async () => {
+    const releaseChild = Promise.withResolvers<undefined>()
+    const releaseGrandchild = Promise.withResolvers<undefined>()
+    const adapter = new GatedAdapter([
+      { chunks: textResponse('child'), gate: releaseChild.promise },
+      { chunks: textResponse('grandchild'), gate: releaseGrandchild.promise },
+    ])
+    const { ctx, parent } = await setupWith(adapter)
+    const started = await ctx.subagents.startContinuable(startSpec(parent))
+    await vi.waitFor(() => { expect(adapter.requests).toHaveLength(1) })
+    const child = ctx.agents.get(started.childId)!
+    const grandchild = await ctx.subagents.startContinuable(startSpec(child))
+    await vi.waitFor(() => { expect(adapter.requests).toHaveLength(2) })
+    const childCancel = vi.spyOn(child, 'cancel')
+    const grandchildCancel = vi.spyOn(ctx.agents.get(grandchild.childId)!, 'cancel')
+
+    const closed = ctx.subagents.closeContinuable(parent, started.childId, testSignal)
+    await vi.waitFor(() => {
+      expect(childCancel).toHaveBeenCalledWith({ kind: 'parent' })
+      expect(grandchildCancel).toHaveBeenCalledWith({ kind: 'parent' })
+    })
+    releaseChild.resolve(undefined)
+    releaseGrandchild.resolve(undefined)
+    await closed
+    expect(ctx.agents.get(grandchild.childId)).toBeUndefined()
+    expect(ctx.agents.get(started.childId)).toBeUndefined()
+  })
+
   it('returns both identities at inbox acceptance, without waiting for the turn or the log', async () => {
     const { ctx, parent, adapter } = await setup([textResponse('first answer')])
     const enqueued: { id: MessageId; loggedYet: boolean }[] = []
@@ -225,7 +362,7 @@ describe('SubagentRuntime.startContinuable', () => {
     expect(descriptorIndex).toBeLessThan(turnStartIndex)
     const descriptor = loaded.events[descriptorIndex] as SessionEvent<'subagent/descriptor'>
     expect(descriptor.data).toEqual({
-      version: SUBAGENT_DESCRIPTOR_VERSION,
+      version: 2,
       mode: 'continuable',
       provider: 'spawn',
       label: 'child task',
@@ -321,7 +458,7 @@ describe('SubagentRuntime.startContinuable', () => {
     const descriptor = child.session.events.find(event => event.type === 'subagent/descriptor')
 
     expect(descriptor?.data).toEqual({
-      version: SUBAGENT_DESCRIPTOR_VERSION,
+      version: 2,
       mode: 'continuable',
       provider: 'spawn',
       label: 'child task',
@@ -355,7 +492,7 @@ describe('SubagentRuntime.startContinuable', () => {
 
     expect(child.session.events.find(event => event.type === 'subagent/descriptor')?.data)
       .toEqual({
-        version: SUBAGENT_DESCRIPTOR_VERSION,
+        version: 2,
         mode: 'continuable',
         provider: 'spawn',
         label: 'child task',
diff --git a/packages/subprocess/subprocess-local/src/index.ts b/packages/subprocess/subprocess-local/src/index.ts
index 261f614fba..2686bec756 100644
--- a/packages/subprocess/subprocess-local/src/index.ts
+++ b/packages/subprocess/subprocess-local/src/index.ts
@@ -35,6 +35,11 @@ import { LocalTerminalHandle } from './terminal.ts'
  * JavaScript-observable host exit.
  */
 export class LocalSubprocessRuntime extends SubprocessRuntime {
+  override readonly descriptorSpawnMode = process.platform === 'linux'
+    ? 'atomic-descriptor' as const
+    : process.platform === 'darwin'
+      ? 'verified-transient' as const
+      : 'unsupported' as const
   /** Live handles retained for normal disposal and synchronous host-exit finalization. */
   private live = new Set<LocalSubprocessHandle>()
   /** Live terminals retained through normal quiescence or host-exit finalization. */
@@ -144,7 +149,14 @@ export class LocalSubprocessRuntime extends SubprocessRuntime {
   }
 
   spawn(spec: SubprocessSpawnSpec): SubprocessHandle {
-    const handle = spawnSubprocess(spec, this.internals)
+    return this.own(spawnSubprocess(spec, this.internals))
+  }
+
+  override spawnDescriptor(spec: SubprocessSpawnSpec, executableFd: number): SubprocessHandle {
+    return this.own(spawnSubprocess(spec, this.internals, executableFd))
+  }
+
+  private own(handle: LocalSubprocessHandle): SubprocessHandle {
     this.live.add(handle)
     // Release ownership only once the whole TREE is gone, not at direct-child
     // settlement — a TERM-trapping helper that outlives the leader must stay
diff --git a/packages/subprocess/subprocess-local/src/spawn.ts b/packages/subprocess/subprocess-local/src/spawn.ts
index 433ba01791..b7d93c2973 100644
--- a/packages/subprocess/subprocess-local/src/spawn.ts
+++ b/packages/subprocess/subprocess-local/src/spawn.ts
@@ -7,12 +7,27 @@
  * @module dsh-subprocess-local/spawn
  */
 
-import { type ChildProcess, spawn, spawnSync } from 'node:child_process'
+import { type ChildProcess, type SpawnOptions, spawn, spawnSync } from 'node:child_process'
 import type { Readable } from 'node:stream'
-import { randomBytes } from 'node:crypto'
-import { closeSync, mkdtempSync, openSync, unlinkSync, writeSync } from 'node:fs'
+import { createHash, randomBytes } from 'node:crypto'
+import {
+  chmodSync,
+  closeSync,
+  constants,
+  fchmodSync,
+  fstatSync,
+  fsyncSync,
+  lstatSync,
+  mkdtempSync,
+  openSync,
+  readSync,
+  realpathSync,
+  rmdirSync,
+  unlinkSync,
+  writeSync,
+} from 'node:fs'
 import { tmpdir } from 'node:os'
-import { join } from 'node:path'
+import { dirname, join, parse } from 'node:path'
 import { setTimeout as sleepMs } from 'node:timers/promises'
 import { scrubbedParentEnv } from '@deepseek-ai/dsh-subprocess'
 import { MAX_TIMER_DELAY_MS } from '@deepseek-ai/dsh-timeout'
@@ -27,15 +42,19 @@ import type {
 import { linuxProcessGroupHasLiveMembers } from './process-inspector.ts'
 
 /**
- * Build a child environment: explicit caller entries override the scrubbed
- * parent base using the target platform's environment-key semantics. A string
- * deliberately restores or overrides an entry; an explicit `undefined`
- * tombstone removes an ordinary ambient entry.
- * @param extra - explicit caller entries and tombstones, merged after the scrub.
+ * Build a child environment: danger-full-access inherits the complete host
+ * environment, while every other permission mode keeps the upstream scrubbed
+ * base. Explicit caller entries then override the selected base using the
+ * target platform's environment-key semantics. A string deliberately restores
+ * or overrides an entry; an explicit `undefined` tombstone removes an ordinary
+ * ambient entry.
+ * @param extra - explicit caller entries and tombstones, merged after the base.
  * @returns the environment to hand to `spawn` for the child process.
  */
 export function childEnv(extra?: Readonly<NodeJS.ProcessEnv>): NodeJS.ProcessEnv {
-  const env = scrubbedParentEnv()
+  const env = process.env.DSH_PERMISSION_MODE === 'danger-full-access'
+    ? { ...process.env }
+    : scrubbedParentEnv()
   if (process.platform !== 'win32') return { ...env, ...extra }
   let entries: [string, string | undefined][] = Object.entries(env)
   for (const [key, value] of Object.entries(extra ?? {})) {
@@ -54,10 +73,17 @@ export interface SpawnInternals {
   taskkill?: (pid: number) => void
   /** Host platform override for signalling decisions. */
   platform?: NodeJS.Platform
+  /** Parent for private Darwin descriptor copies (defaults to the OS temp dir). */
+  descriptorExecDir?: string
+  /** Process spawn seam used only by descriptor-materialization tests. */
+  spawnProcess?: SpawnProcess
   /** Linux process-group member probe (defaults to `/proc` inspection). */
   linuxProcessGroupHasLiveMembers?: (processGroupId: number) => boolean | undefined
 }
 
+type SpawnProcess = (command: string, args: readonly string[], options: SpawnOptions) => ChildProcess
+const MAX_DESCRIPTOR_EXECUTABLE_BYTES = 256 * 1024 * 1024
+
 /**
  * Local-only synchronous final termination used by the owning service during
  * host exit and as the last fallback after failed normal disposal. It is
@@ -314,6 +340,190 @@ function signalTree(
   }
 }
 
+interface TransientExecutable {
+  readonly path: string
+  verify(): void
+  cleanup(): void
+}
+
+interface TransientIdentity {
+  readonly dev: number
+  readonly ino: number
+}
+
+/** Reject a descriptor-exec parent whose ancestry permits cross-UID replacement. */
+function trustedDescriptorExecParent(parent: string): string {
+  const canonical = realpathSync(parent)
+  const filesystemRoot = parse(canonical).root
+  for (let cursor = canonical;; cursor = dirname(cursor)) {
+    const metadata = lstatSync(cursor)
+    const ownerTrusted = metadata.uid === 0
+      || (typeof process.getuid === 'function' && metadata.uid === process.getuid())
+    const stickyTrusted = (metadata.mode & 0o1000) !== 0
+      && ownerTrusted
+    if (!metadata.isDirectory() || !ownerTrusted
+      || ((metadata.mode & 0o022) !== 0 && !stickyTrusted)) {
+      throw new Error('subprocess descriptor execution parent is not trusted')
+    }
+    if (cursor === filesystemRoot) break
+  }
+  return canonical
+}
+
+/** Remove only the materialized inode; preserve replacements and cleanup failures. */
+function cleanupTransientExecutable(
+  path: string,
+  directory: string,
+  identity: TransientIdentity | undefined,
+): void {
+  try {
+    const current = lstatSync(path)
+    if (identity !== undefined && current.dev === identity.dev && current.ino === identity.ino) {
+      unlinkSync(path)
+    }
+  } catch (error: unknown) {
+    if ((error as NodeJS.ErrnoException).code !== 'ENOENT') {
+      process.emitWarning(error instanceof Error ? error : String(error))
+    }
+  }
+  try {
+    rmdirSync(directory)
+  } catch (error: unknown) {
+    if ((error as NodeJS.ErrnoException).code !== 'ENOENT') {
+      process.emitWarning(error instanceof Error ? error : String(error))
+    }
+  }
+}
+
+/** Read exactly one descriptor range without changing the caller's file offset. */
+function readDescriptor(
+  fd: number,
+  size: number,
+  consume: (chunk: Buffer) => void,
+): void {
+  const chunk = Buffer.allocUnsafe(Math.min(size, 64 * 1024))
+  let position = 0
+  while (position < size) {
+    const read = readSync(fd, chunk, 0, Math.min(chunk.length, size - position), position)
+    if (read === 0) throw new Error('subprocess executable descriptor changed while reading')
+    consume(chunk.subarray(0, read))
+    position += read
+  }
+}
+
+/**
+ * Darwin has no supported descriptor-only exec primitive. Materialize exact
+ * descriptor bytes beneath a fresh private directory for the synchronous
+ * native spawn call, then retain that pathname until the whole child process
+ * tree settles so executable self-inspection remains valid.
+ */
+function materializeDarwinExecutable(executableFd: number, parent: string): TransientExecutable {
+  const sourceBefore = fstatSync(executableFd)
+  if (!sourceBefore.isFile() || !Number.isSafeInteger(sourceBefore.size)
+    || sourceBefore.size <= 0 || sourceBefore.size > MAX_DESCRIPTOR_EXECUTABLE_BYTES) {
+    throw new Error(`subprocess executable descriptor must reference one non-empty regular file no larger than ${MAX_DESCRIPTOR_EXECUTABLE_BYTES} bytes`)
+  }
+
+  const directory = mkdtempSync(join(trustedDescriptorExecParent(parent), 'dsh-descriptor-exec-'))
+  chmodSync(directory, 0o700)
+  const directoryStat = lstatSync(directory)
+  if (!directoryStat.isDirectory() || (directoryStat.mode & 0o777) !== 0o700) {
+    try { rmdirSync(directory) } catch { /* preserve the identity failure */ }
+    throw new Error('subprocess transient executable directory identity mismatch')
+  }
+  const directoryIdentity = { dev: directoryStat.dev, ino: directoryStat.ino }
+  const path = join(directory, 'executable')
+  let outputFd: number | undefined
+  let outputIdentity: TransientIdentity | undefined
+  try {
+    outputFd = openSync(
+      path,
+      constants.O_WRONLY | constants.O_CREAT | constants.O_EXCL | constants.O_NOFOLLOW,
+      0o500,
+    )
+    const createdStat = fstatSync(outputFd)
+    outputIdentity = { dev: createdStat.dev, ino: createdStat.ino }
+    const sourceDigest = createHash('sha256')
+    readDescriptor(executableFd, sourceBefore.size, (chunk) => {
+      sourceDigest.update(chunk)
+      let written = 0
+      while (written < chunk.length) {
+        const count = writeSync(outputFd!, chunk, written, chunk.length - written)
+        if (count <= 0) throw new Error('subprocess transient executable write made no progress')
+        written += count
+      }
+    })
+    fchmodSync(outputFd, 0o500)
+    fsyncSync(outputFd)
+    const outputStat = fstatSync(outputFd)
+    closeSync(outputFd)
+    outputFd = undefined
+
+    const sourceAfter = fstatSync(executableFd)
+    if (sourceAfter.dev !== sourceBefore.dev || sourceAfter.ino !== sourceBefore.ino
+      || sourceAfter.size !== sourceBefore.size || sourceAfter.mtimeMs !== sourceBefore.mtimeMs
+      || sourceAfter.ctimeMs !== sourceBefore.ctimeMs) {
+      throw new Error('subprocess executable descriptor changed while materializing')
+    }
+    if (outputStat.size !== sourceBefore.size || (outputStat.mode & 0o777) !== 0o500) {
+      throw new Error('subprocess transient executable metadata mismatch')
+    }
+
+    const verifyFd = openSync(path, constants.O_RDONLY | constants.O_NOFOLLOW)
+    try {
+      const verifyBefore = fstatSync(verifyFd)
+      if (verifyBefore.dev !== outputStat.dev || verifyBefore.ino !== outputStat.ino
+        || verifyBefore.size !== outputStat.size || (verifyBefore.mode & 0o777) !== 0o500) {
+        throw new Error('subprocess transient executable identity mismatch')
+      }
+      const outputDigest = createHash('sha256')
+      readDescriptor(verifyFd, verifyBefore.size, chunk => { outputDigest.update(chunk) })
+      const verifyAfter = fstatSync(verifyFd)
+      if (verifyAfter.dev !== verifyBefore.dev || verifyAfter.ino !== verifyBefore.ino
+        || verifyAfter.size !== verifyBefore.size || verifyAfter.mtimeMs !== verifyBefore.mtimeMs
+        || verifyAfter.ctimeMs !== verifyBefore.ctimeMs) {
+        throw new Error('subprocess transient executable changed while verifying')
+      }
+      if (outputDigest.digest('hex') !== sourceDigest.digest('hex')) {
+        throw new Error('subprocess transient executable digest mismatch')
+      }
+    } finally {
+      closeSync(verifyFd)
+    }
+
+    let cleaned = false
+    return {
+      path,
+      verify(): void {
+        const currentDirectory = lstatSync(directory)
+        const currentFile = lstatSync(path)
+        if (!currentDirectory.isDirectory()
+          || currentDirectory.dev !== directoryIdentity.dev
+          || currentDirectory.ino !== directoryIdentity.ino
+          || (currentDirectory.mode & 0o777) !== 0o700
+          || !currentFile.isFile()
+          || currentFile.dev !== outputStat.dev
+          || currentFile.ino !== outputStat.ino
+          || currentFile.size !== outputStat.size
+          || (currentFile.mode & 0o777) !== 0o500) {
+          throw new Error('subprocess transient executable pre-spawn identity mismatch')
+        }
+      },
+      cleanup(): void {
+        if (cleaned) return
+        cleaned = true
+        cleanupTransientExecutable(path, directory, outputIdentity)
+      },
+    }
+  } catch (error: unknown) {
+    if (outputFd !== undefined) {
+      try { closeSync(outputFd) } catch { /* preserve the original materialization failure */ }
+    }
+    cleanupTransientExecutable(path, directory, outputIdentity)
+    throw error
+  }
+}
+
 /**
  * Spawn one isolated detached process tree with the spec's per-stream stdio
  * dispositions. Runtime exits resolve `done` as {@link SubprocessOutcome};
@@ -323,7 +533,11 @@ function signalTree(
  * @returns live subprocess handle.
  * @throws when `graceMs` cannot be represented by one Node timer.
  */
-export function spawnSubprocess(spec: SubprocessSpawnSpec, internals: SpawnInternals = {}): LocalSubprocessHandle {
+export function spawnSubprocess(
+  spec: SubprocessSpawnSpec,
+  internals: SpawnInternals = {},
+  executableFd?: number,
+): LocalSubprocessHandle {
   if (!Number.isFinite(spec.graceMs) || spec.graceMs <= 0 || spec.graceMs > MAX_TIMER_DELAY_MS) {
     throw new Error(`subprocess graceMs must be a positive finite number no greater than ${MAX_TIMER_DELAY_MS}`)
   }
@@ -332,6 +546,13 @@ export function spawnSubprocess(spec: SubprocessSpawnSpec, internals: SpawnInter
   const taskkill = internals.taskkill ?? taskkillProcessTree
   const linuxGroupHasLiveMembers = internals.linuxProcessGroupHasLiveMembers ?? linuxProcessGroupHasLiveMembers
 
+  const spawnProcess = internals.spawnProcess ?? spawn
+
+  if (executableFd !== undefined
+    && (!Number.isSafeInteger(executableFd) || executableFd < 0)) {
+    throw new Error('subprocess executable descriptor must be a non-negative safe integer')
+  }
+
   if (spec.signal?.aborted) {
     throw new Error(`aborted before spawn: ${String(spec.signal.reason ?? 'aborted')}`)
   }
@@ -347,18 +568,42 @@ export function spawnSubprocess(spec: SubprocessSpawnSpec, internals: SpawnInter
   const stdinMode = spec.stdio.stdin
 
   const env = childEnv(spec.env)
-  const child = spawn(program, args, {
+  const childExecutableFd = 3
+  let executable = program
+  let inheritedExecutableFd: number | undefined
+  let transientExecutable: TransientExecutable | undefined
+  if (executableFd !== undefined) {
+    if (platform === 'linux') {
+      executable = `/proc/self/fd/${childExecutableFd}`
+      inheritedExecutableFd = executableFd
+    } else if (platform === 'darwin') {
+      transientExecutable = materializeDarwinExecutable(executableFd, internals.descriptorExecDir ?? tmpdir())
+      executable = transientExecutable.path
+    }
+    else throw new Error('subprocess descriptor-bound execution is unsupported on this platform')
+  }
+  const spawnOptions: SpawnOptions = {
+    ...(executableFd === undefined ? {} : { argv0: program }),
     cwd: spec.cwd,
     env,
     stdio: [
       stdinMode === 'ignore' ? 'ignore' : 'pipe',
       outMode === 'inherit' ? 'inherit' : 'pipe',
       errMode === 'inherit' ? 'inherit' : 'pipe',
+      ...(inheritedExecutableFd === undefined ? [] : [inheritedExecutableFd]),
     ],
     // `detached` gives teardown a tree root on POSIX (its own process group);
     // Windows terminates by root pid through taskkill /T instead.
     detached: platform !== 'win32',
-  })
+  }
+  let child: ChildProcess
+  try {
+    transientExecutable?.verify()
+    child = spawnProcess(executable, args, spawnOptions)
+  } catch (error: unknown) {
+    transientExecutable?.cleanup()
+    throw error
+  }
 
   const collectStream = (mode: SubprocessOutputMode, stream: Readable | null, label: string): OutputCollector | undefined => {
     if (!isCollect(mode) || stream === null) return undefined
@@ -424,6 +669,10 @@ export function spawnSubprocess(spec: SubprocessSpawnSpec, internals: SpawnInter
     return treeExitObservation
   }
 
+  if (transientExecutable !== undefined) {
+    void observeTreeExit().then(() => { transientExecutable?.cleanup() })
+  }
+
   // The escalation's tier primitive (not on the handle — terminate() is the
   // only consumer-facing termination verb). Guards on TREE liveness, not
   // outcome settlement: a TERM-trapping helper can outlive the settled direct
@@ -485,6 +734,7 @@ export function spawnSubprocess(spec: SubprocessSpawnSpec, internals: SpawnInter
       // No meaningful close outcome follows a spawn failure.
       settled = true
       cleanup()
+      transientExecutable?.cleanup()
       reject(error)
     })
     child.on('exit', (exitCode, signal) => {
diff --git a/packages/subprocess/subprocess-local/tests/spawn.spec.ts b/packages/subprocess/subprocess-local/tests/spawn.spec.ts
index 1f87fd67cc..d22ace2e8f 100644
--- a/packages/subprocess/subprocess-local/tests/spawn.spec.ts
+++ b/packages/subprocess/subprocess-local/tests/spawn.spec.ts
@@ -1,4 +1,5 @@
-import { mkdtempSync, readFileSync, statSync, unlinkSync } from 'node:fs'
+import { spawn as spawnChild } from 'node:child_process'
+import { chmodSync, closeSync, existsSync, ftruncateSync, mkdtempSync, openSync, readFileSync, readdirSync, realpathSync, rmdirSync, statSync, unlinkSync } from 'node:fs'
 import { tmpdir } from 'node:os'
 import { dirname, join } from 'node:path'
 import { describe, expect, it, vi } from 'vitest'
@@ -10,4 +11,5 @@ import {
 } from '../src/spawn.ts'
 import type { SubprocessHandle, SubprocessOutputReader } from '@deepseek-ai/dsh-subprocess'
 import { MAX_TIMER_DELAY_MS } from '@deepseek-ai/dsh-timeout'
+import * as spawnModule from '../src/spawn.ts'
 
@@ -14,4 +16,7 @@ import { MAX_TIMER_DELAY_MS } from '@deepseek-ai/dsh-timeout'
-const { failNextClose, failNextUnlink } = vi.hoisted(() => ({
+const { failNextClose, failNextUnlink, failUnlinkPath, foreignOwnerPath, replaceTransientOnClose } = vi.hoisted(() => ({
   failNextClose: { value: false },
   failNextUnlink: { value: false },
+  failUnlinkPath: { value: undefined as string | undefined },
+  foreignOwnerPath: { value: undefined as string | undefined },
+  replaceTransientOnClose: { parent: undefined as string | undefined, path: undefined as string | undefined },
 }))
@@ -18,5 +23,15 @@ const { failNextClose, failNextUnlink } = vi.hoisted(() => ({
 vi.mock('node:fs', async (importOriginal) => {
   const actual = await importOriginal<typeof import('node:fs')>()
   return {
     ...actual,
+    lstatSync(path: Parameters<typeof actual.lstatSync>[0], options?: Parameters<typeof actual.lstatSync>[1]) {
+      const metadata = actual.lstatSync(path, options as never)
+      if (String(path) !== foreignOwnerPath.value) return metadata
+      return new Proxy(metadata, {
+        get(target, property, receiver) {
+          if (property === 'uid') return (typeof process.getuid === 'function' ? process.getuid() : 0) + 1
+          return Reflect.get(target, property, receiver)
+        },
+      })
+    },
     closeSync(fd: number): void {
@@ -23,6 +38,14 @@ vi.mock('node:fs', async (importOriginal) => {
       if (failNextClose.value) {
         failNextClose.value = false
+        if (replaceTransientOnClose.parent !== undefined) {
+          const [directory] = actual.readdirSync(replaceTransientOnClose.parent)
+          const path = `${replaceTransientOnClose.parent}/${directory}/executable`
+          actual.closeSync(fd)
+          actual.renameSync(path, `${path}.original`)
+          actual.writeFileSync(path, 'foreign replacement', { mode: 0o500 })
+          replaceTransientOnClose.path = path
+        }
         throw Object.assign(new Error('simulated EIO on close'), { code: 'EIO' })
       }
       actual.closeSync(fd)
     },
@@ -29,4 +52,8 @@ vi.mock('node:fs', async (importOriginal) => {
     unlinkSync(path: Parameters<typeof actual.unlinkSync>[0]): void {
+      if (String(path) === failUnlinkPath.value) {
+        failUnlinkPath.value = undefined
+        throw Object.assign(new Error('simulated EIO on unlink'), { code: 'EIO' })
+      }
       if (failNextUnlink.value) {
         failNextUnlink.value = false
         throw Object.assign(new Error('simulated EIO on unlink'), { code: 'EIO' })
@@ -135,6 +162,294 @@ describe('spawnSubprocess', () => {
     expect(result.stderr.text).toBe('')
   })
 
+  it('inherits the ambient host environment in danger-full-access mode', () => {
+    const previousMode = process.env.DSH_PERMISSION_MODE
+    const previousCanary = process.env.DSH_HOST_ENV_TOKEN
+    try {
+      process.env.DSH_PERMISSION_MODE = 'danger-full-access'
+      process.env.DSH_HOST_ENV_TOKEN = 'ambient-canary'
+      expect(spawnModule.childEnv().DSH_HOST_ENV_TOKEN).toBe('ambient-canary')
+      expect(spawnModule.childEnv({ DSH_HOST_ENV_TOKEN: 'explicit-override' }).DSH_HOST_ENV_TOKEN)
+        .toBe('explicit-override')
+      expect(spawnModule.childEnv({ DSH_HOST_ENV_TOKEN: undefined }).DSH_HOST_ENV_TOKEN).toBeUndefined()
+    } finally {
+      if (previousMode === undefined) delete process.env.DSH_PERMISSION_MODE
+      else process.env.DSH_PERMISSION_MODE = previousMode
+      if (previousCanary === undefined) delete process.env.DSH_HOST_ENV_TOKEN
+      else process.env.DSH_HOST_ENV_TOKEN = previousCanary
+    }
+  })
+
+  it('keeps scrubbing ambient credentials outside danger-full-access mode', () => {
+    const previousMode = process.env.DSH_PERMISSION_MODE
+    const previousCanary = process.env.DSH_HOST_ENV_TOKEN
+    try {
+      process.env.DSH_PERMISSION_MODE = 'workspace-write'
+      process.env.DSH_HOST_ENV_TOKEN = 'ambient-canary'
+      expect(spawnModule.childEnv().DSH_HOST_ENV_TOKEN).toBeUndefined()
+    } finally {
+      if (previousMode === undefined) delete process.env.DSH_PERMISSION_MODE
+      else process.env.DSH_PERMISSION_MODE = previousMode
+      if (previousCanary === undefined) delete process.env.DSH_HOST_ENV_TOKEN
+      else process.env.DSH_HOST_ENV_TOKEN = previousCanary
+    }
+  })
+
+  it.runIf(process.platform === 'linux')(
+    'executes Linux descriptor bytes when the display pathname does not exist',
+    async () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      try {
+        const result = await finish(spawnSubprocess({
+          ...spec('printf descriptor-bound'),
+          argv: ['/definitely/replaced/bash', '-c', 'printf descriptor-bound'],
+        }, { platform: 'linux' }, executableFd))
+        expect(result.exitCode).toBe(0)
+        expect(result.stdout.text).toBe('descriptor-bound')
+      } finally {
+        closeSync(executableFd)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'executes descriptor bytes through a private transient file on Darwin',
+    async () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-spec-'))
+      try {
+        const handle = spawnSubprocess({
+          ...spec('printf descriptor-bound'),
+          argv: ['/definitely/replaced/bash', '-c', 'printf descriptor-bound'],
+        }, {
+          platform: 'darwin',
+          descriptorExecDir,
+          spawnProcess(program, args, options) {
+            expect(readFileSync(program)).toEqual(readFileSync('/bin/bash'))
+            expect(statSync(program).mode & 0o777).toBe(0o500)
+            return spawnChild(program, args, options)
+          },
+        }, executableFd)
+        const result = await finish(handle)
+        expect(await handle.waitForExit()).toBe(true)
+        expect(result.exitCode).toBe(0)
+        expect(result.stdout.text).toBe('descriptor-bound')
+        expect(readdirSync(descriptorExecDir)).toEqual([])
+      } finally {
+        closeSync(executableFd)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'keeps Darwin materialization linked while the child resolves its own executable',
+    async () => {
+      const executableFd = openSync(process.execPath, 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-self-spec-'))
+      try {
+        const script = `setTimeout(() => {
+          const { realpathSync } = require('node:fs')
+          process.stdout.write(realpathSync(process.execPath))
+        }, 50)`
+        const handle = spawnSubprocess({
+          ...spec('unused'),
+          argv: [process.execPath, '-e', script],
+        }, {
+          platform: 'darwin',
+          descriptorExecDir,
+        }, executableFd)
+        const result = await finish(handle)
+        expect(await handle.waitForExit()).toBe(true)
+        expect(result.exitCode).toBe(0)
+        expect(result.stdout.text).toContain('/executable')
+        expect(readdirSync(descriptorExecDir)).toEqual([])
+      } finally {
+        closeSync(executableFd)
+        if (existsSync(descriptorExecDir)) rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'keeps Darwin materialization linked until a descendant resolves its executable',
+    async () => {
+      const executableFd = openSync(process.execPath, 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-tree-spec-'))
+      const marker = join(descriptorExecDir, 'descendant-result')
+      try {
+        const descendantScript = `setTimeout(() => {
+          const { realpathSync, writeFileSync } = require('node:fs')
+          writeFileSync(${JSON.stringify(marker)}, realpathSync(process.execPath))
+        }, 100)`
+        const leaderScript = `const { spawn } = require('node:child_process')
+          spawn(process.execPath, ['-e', ${JSON.stringify(descendantScript)}], {
+            stdio: 'ignore',
+          }).unref()`
+        const handle = spawnSubprocess({
+          ...spec('unused'),
+          argv: [process.execPath, '-e', leaderScript],
+        }, {
+          platform: 'darwin',
+          descriptorExecDir,
+        }, executableFd)
+        const direct = await handle.done
+        expect(direct.exitCode).toBe(0)
+        expect(readdirSync(descriptorExecDir)).not.toEqual([])
+        expect(await handle.waitForExit()).toBe(true)
+        expect(readFileSync(marker, 'utf8')).toContain('/executable')
+        expect(readdirSync(descriptorExecDir)).toEqual(['descendant-result'])
+      } finally {
+        closeSync(executableFd)
+        if (existsSync(marker)) unlinkSync(marker)
+        if (existsSync(descriptorExecDir)) rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'removes Darwin descriptor materialization when spawn throws',
+    () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-failure-spec-'))
+      try {
+        expect(() => spawnSubprocess(spec('true'), {
+          platform: 'darwin',
+          descriptorExecDir,
+          spawnProcess() { throw new Error('simulated spawn failure') },
+        }, executableFd)).toThrow('simulated spawn failure')
+        expect(readdirSync(descriptorExecDir)).toEqual([])
+      } finally {
+        closeSync(executableFd)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'rejects a cross-UID-writable Darwin descriptor parent before materialization',
+    () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-untrusted-spec-'))
+      chmodSync(descriptorExecDir, 0o777)
+      try {
+        expect(() => spawnSubprocess(spec('true'), {
+          platform: 'darwin',
+          descriptorExecDir,
+        }, executableFd)).toThrow('descriptor execution parent is not trusted')
+        expect(readdirSync(descriptorExecDir)).toEqual([])
+      } finally {
+        chmodSync(descriptorExecDir, 0o700)
+        closeSync(executableFd)
+        rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'rejects a foreign-owned non-writable Darwin descriptor parent before materialization',
+    () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-foreign-owner-spec-'))
+      chmodSync(descriptorExecDir, 0o755)
+      foreignOwnerPath.value = realpathSync(descriptorExecDir)
+      try {
+        expect(() => spawnSubprocess(spec('true'), {
+          platform: 'darwin',
+          descriptorExecDir,
+        }, executableFd)).toThrow('descriptor execution parent is not trusted')
+        expect(readdirSync(descriptorExecDir)).toEqual([])
+      } finally {
+        foreignOwnerPath.value = undefined
+        closeSync(executableFd)
+        rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'preserves a foreign replacement when Darwin materialization fails',
+    () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-replacement-spec-'))
+      const warning = vi.spyOn(process, 'emitWarning').mockImplementation(() => {})
+      replaceTransientOnClose.parent = descriptorExecDir
+      failNextClose.value = true
+      try {
+        expect(() => spawnSubprocess(spec('true'), {
+          platform: 'darwin',
+          descriptorExecDir,
+        }, executableFd)).toThrow('simulated EIO on close')
+        expect(replaceTransientOnClose.path).toBeDefined()
+        expect(readFileSync(replaceTransientOnClose.path!, 'utf8')).toBe('foreign replacement')
+      } finally {
+        warning.mockRestore()
+        closeSync(executableFd)
+        const path = replaceTransientOnClose.path
+        replaceTransientOnClose.parent = undefined
+        replaceTransientOnClose.path = undefined
+        if (path !== undefined) {
+          if (existsSync(path)) unlinkSync(path)
+          if (existsSync(`${path}.original`)) unlinkSync(`${path}.original`)
+          if (existsSync(dirname(path))) rmdirSync(dirname(path))
+        }
+        if (existsSync(descriptorExecDir)) rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'warns and retains the private Darwin materialization when unlink fails',
+    async () => {
+      const executableFd = openSync('/bin/bash', 'r')
+      const descriptorExecDir = mkdtempSync(join(tmpdir(), 'dsh-descriptor-cleanup-spec-'))
+      const warning = vi.spyOn(process, 'emitWarning').mockImplementation(() => {})
+      let materializedPath: string | undefined
+      try {
+        const handle = spawnSubprocess(spec('true'), {
+          platform: 'darwin',
+          descriptorExecDir,
+          spawnProcess(program, args, options) {
+            materializedPath = program
+            failUnlinkPath.value = program
+            return spawnChild(program, args, options)
+          },
+        }, executableFd)
+        const result = await finish(handle)
+        expect(await handle.waitForExit()).toBe(true)
+        expect(result.exitCode).toBe(0)
+        expect(failUnlinkPath.value).toBeUndefined()
+        expect(materializedPath).toBeDefined()
+        expect(readFileSync(materializedPath!)).toEqual(readFileSync('/bin/bash'))
+        expect(warning).toHaveBeenCalled()
+      } finally {
+        failUnlinkPath.value = undefined
+        warning.mockRestore()
+        closeSync(executableFd)
+        if (materializedPath !== undefined && existsSync(materializedPath)) {
+          unlinkSync(materializedPath)
+          rmdirSync(dirname(materializedPath))
+        }
+        if (existsSync(descriptorExecDir)) rmdirSync(descriptorExecDir)
+      }
+    },
+  )
+
+  it.skipIf(process.platform === 'win32')(
+    'rejects an oversized Darwin executable descriptor before materialization',
+    () => {
+      const oversized = join(mkdtempSync(join(tmpdir(), 'dsh-descriptor-oversize-spec-')), 'oversized')
+      const executableFd = openSync(oversized, 'w+')
+      try {
+        ftruncateSync(executableFd, 256 * 1024 * 1024 + 1)
+        expect(() => spawnSubprocess(spec('true'), { platform: 'darwin' }, executableFd))
+          .toThrow('no larger than 268435456 bytes')
+      } finally {
+        closeSync(executableFd)
+        unlinkSync(oversized)
+        rmdirSync(dirname(oversized))
+      }
+    },
+  )
+
   it('captures stderr separately', async () => {
     const result = await finish(spawnSubprocess(spec('echo oops >&2')))
     expect(result.exitCode).toBe(0)
diff --git a/packages/subprocess/subprocess/src/index.ts b/packages/subprocess/subprocess/src/index.ts
index a7413159f9..dc41ea2b2b 100644
--- a/packages/subprocess/subprocess/src/index.ts
+++ b/packages/subprocess/subprocess/src/index.ts
@@ -100,6 +100,14 @@ declare module '@deepseek-ai/cordis' {
  *   when the top-level process exits.
  */
 export abstract class SubprocessRuntime extends Service {
+  /** Execution guarantee supplied by `spawnDescriptor`. */
+  readonly descriptorSpawnMode: 'unsupported' | 'atomic-descriptor' | 'verified-transient' = 'unsupported'
+
+  /** Compatibility projection for callers that only need support detection. */
+  get descriptorSpawnSupported(): boolean {
+    return this.descriptorSpawnMode !== 'unsupported'
+  }
+
   constructor(ctx: Context) {
     super(ctx, 'subprocess')
   }
@@ -129,6 +137,20 @@ export abstract class SubprocessRuntime extends Service {
    */
   abstract spawn(spec: SubprocessSpawnSpec): SubprocessHandle
 
+  /**
+   * Start one managed child from an already-open executable descriptor. An
+   * `atomic-descriptor` provider makes the descriptor the kernel execution
+   * authority. A `verified-transient` provider copies and verifies descriptor
+   * bytes through a documented non-atomic platform transition. `argv[0]`
+   * remains display identity only. The default fails closed so providers
+   * cannot silently fall back to an undeclared ordinary pathname.
+   * @param spec - fully specified child process request.
+   * @param executableFd - caller-owned readable executable descriptor retained through spawn.
+   */
+  spawnDescriptor(_spec: SubprocessSpawnSpec, _executableFd: number): SubprocessHandle {
+    throw new Error('subprocess: descriptor-bound execution is unsupported by this provider')
+  }
+
   /**
    * Allocate a real terminal and start one owned process session. This is the
    * only non-pipe process primitive: implementations own terminal byte I/O,
