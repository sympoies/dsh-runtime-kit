# Development log - 2026-08

## 2026-08-30 - Classify repeated Stop policy outcomes without content

**Result**

- The runtime service now exposes only the current Agent-and-turn Stop policy
  outcome as one bounded enum: allow, context, policy denied, capability
  unavailable, transport failed, provider failed, or cancelled.
- When the authoritative canary observes a repeated completed Stop waterfall,
  it maps that enum to an exact allowlisted deadline cause code. It retains no
  provider reason, rule ID, policy context, prompt, path, or subprocess output.
- Fail-closed Stop behavior and same-turn retry authority are unchanged.

**Why / context**

- Two exact hosted Gate 4 attempts reached the same repeated Stop listener tail
  after authenticated acquisition and isolation. Read-only process observation
  also showed repeated policy dispatches during the same DSH turn, disproving
  the earlier automatic goal-round hypothesis and narrowing the unresolved
  branch to policy denial or policy transport/provider failure.
- Treating a later Stop as allowed would bypass a real policy decision. A
  content-free outcome classification provides the missing diagnostic boundary
  without weakening enforcement or disclosing policy material.

**Evidence**

- Test-first validation failed because the bounded outcome service and repeated
  policy-denial cause code did not exist. The focused runtime and canary
  contracts, including the acceptance parser, now pass 155/155.
- The complete package test suite and strict typechecking pass. The regression
  covers all seven outcome classes through real policy paths, exact session-and-
  turn scoping, repeated-stop cause-code projection, and allowlist parsing.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [First exact hosted failure](https://github.com/serenvia/sympoies-infra/actions/runs/33296635314)
- [Exact hosted rerun](https://github.com/serenvia/sympoies-infra/actions/runs/33297509846)
- [Outcome-classification suggestion](https://github.com/sympoies/dsh-runtime-kit/issues/66#issuecomment-5466177737)

## 2026-08-30 - Converge provider review publication on nils-cli 1.27.27

**Result**

- DSH review workflows now require the nils-cli 1.27.27 provider-review
  contract: one canonical five-column App report, native actionable diff
  threads, and metadata-only personal provenance bound to the reviewed head.
- The compatibility manifest, hosted workflow, operations guidance, and
  contract tests pin the same released source, archives, and companion binary
  identities. DSH retains review semantics and no local renderer fork.

**Why / context**

- Earlier consumers could publish semantically equivalent findings through
  different body shapes, and a personal identity could repeat the App report.
  Centralizing rendering and native-thread transport in nils-cli makes the
  provider result uniform while DSH continues to own reviewer selection and
  finding semantics.

**Evidence**

- The complete package suite passes 723/723 and strict typechecking passes.
- The authenticated release archive passed the keyless end-to-end smoke on a
  pristine DSH `0.1.0-rc.7` checkout after patch apply and rebuild. Patch
  reversal, pristine rebuild, CLI startup, and source-cleanliness checks also
  passed.
- Focused compatibility and coexistence coverage passes 21/21 with exact
  release and binary digest assertions.

**Links**

- [nils-cli provider-review contract PR #1567](https://github.com/sympoies/nils-cli/pull/1567)
- [nils-cli v1.27.27 release](https://github.com/sympoies/nils-cli/releases/tag/v1.27.27)
## 2026-08-30 - Disarm the self-driven acceptance goal before idle settlement

**Result**

- Positive and candidate-upgrade canaries now disarm their selected DSH goal
  before submitting the one canary-driven turn. This removes automatic
  goal-round continuation authority while preserving the active goal and its
  exact revision for the pre-evidence denial and post-evidence manual
  completion checks.
- The helper fails closed unless DSH returns the same goal identity, revision,
  and phase with `activation: disarmed`. The authoritative matrix now also
  requires exactly one completed stop and zero goal-round followups for the
  positive leg, and zero goal-round followups after candidate upgrade. Runtime
  policy, acceptance verdicts, deadlines, and authenticated DSH patch bytes
  are unchanged.

**Why / context**

- Hosted Gate 4 run `33291423415` reached the repeated canary-owned listener
  tail before its deadline. That milestone is the last completed boundary, so
  it proved the runtime listeners had finished rather than identifying a
  listener-order failure.
- DSH creates goals armed by default. Its public goal-round driver may enqueue
  another round whenever an armed active goal reaches idle, while this canary
  deliberately waits for idle before manually completing the goal after the
  acceptance verdict is observable. Disarming tested that continuation
  hypothesis through the public DSH boundary; later exact hosted attempts
  showed that repeated Stops persisted and disproved it.

**Evidence**

- Test-first validation began at 21/22 because the disarm helper was absent.
  Focused coverage now passes 23/23, including exact interaction order and
  rejection of undefined or identity, revision, phase, and activation drift.
- The packed canary holds the real DSH goal-round driver's durability
  checkpoint, disarms the goal while that checkpoint is pending, then releases
  it and fails if any automatic goal followup is queued or admitted. The matrix
  rejects a repeated positive stop or any positive/upgrade goal followup.
- The complete Node 24 and Node 26 suites each pass 724/724 with strict
  typechecking. The local released-mode rehearsal passes all 10 authoritative
  real-process legs with
  zero acceptance operations, finish-line requests and reservations, and
  pending correlations after every leg. It remains correctly incomplete until
  hosted isolation and authorized delivery are proven.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted repeated-round failure](https://github.com/serenvia/sympoies-infra/actions/runs/33291423415)

## 2026-08-30 - Order hosted stop progress after runtime activation

**Result**

- Runtime-bearing authoritative canary phases now wait for the
  `dshRuntimeKit` service before registering their turn-stopping observers.
  Because runtime-kit provides that service only after its stop listener is
  registered, the existing callback and listener-tail milestones now have the
  ordering guarantee their names require.
- The intentionally runtime-free unpatched smoke and provider-mismatch probe
  keep their original core-service activation path. Runtime policy, verdict
  behavior, deadlines, successful receipts, and authenticated DSH patch bytes
  remain unchanged.

**Why / context**

- Hosted Gate 4 run `33286408577` reached the repeated canary-owned stop tail
  but no later settlement marker. Source inspection then showed that the
  canary could activate when only core DSH services were ready, while
  runtime-kit was still completing its asynchronous plugin setup.
- The prior listener-tail observation therefore did not prove that the later
  runtime listener had completed. Binding runtime-bearing phases to the
  runtime service narrows the next hosted result to the actual runtime
  listener or the following DSH turn-settlement boundary without changing the
  exercised behavior.

**Evidence**

- Test-first validation failed the new activation-order assertion before the
  dependency was added. Focused canary coverage passes 21/21, and an
  instrumented real-policy regression proves 2/2 that serial canary progress
  cannot pass the runtime stop listener before that listener completes.
- The complete Node 24 and Node 26 suites each pass 722/722, strict
  typechecking passes, and the 10-leg local authoritative matrix passes with
  zero acceptance, finish-line, reservation, and correlation resources after
  every leg.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted post-stop settlement failure](https://github.com/serenvia/sympoies-infra/actions/runs/33286408577)

## 2026-08-30 - Restore native DSH full-host authority at finish-line

**Result**

- `danger-full-access` is now an explicit native full-host authority profile:
  runtime-kit projects the strict typed runner, while nils retains transient-
  unit lifecycle supervision without adding private-user, address-family,
  localhost, or SUID restrictions.
- Confined runners retain those permission restrictions. DSH policy and
  approval still govern which actions may run, and credential scrubbing is
  unchanged; whole-runtime Docker remains the future isolation boundary.

**Why / context**

- Native DSH reported `danger-full-access`, but finish-line applied one fixed
  transient-unit sandbox to every runner. Commands could see host daemon and
  user-service sockets while AF_UNIX itself failed with
  `Address family not supported by protocol`.
- Lifecycle containment and permission isolation were separate contracts. The
  repair selects properties by the authenticated runner kind instead of
  weakening exact-command binding, cancellation, timeout, output bounds, or
  descendant cleanup.

**Evidence**

- The pre-fix full-host user-bus regression failed with the observed address-
  family error, then passed with localhost and supplementary-group probes.
- The nils finish-line integration suite passes 32/32. Its required local-fast
  lane passes documentation audits, formatting, Clippy, 372 package tests, and
  doc tests; a complementary confined regression proves user-manager
  delegation remains denied. The repair shipped in signed stable release
  `nils-cli` v1.27.27 with exact Linux and macOS package authentication.
- The runtime-kit focused finish-line suite passes 57/57, the complete package
  suite passes 722/722, strict typechecking passes, and policy parity remains
  bound to the reviewed 101-rule source.
- A pristine DSH 0.1.1-rc.2 checkout rebuilt with the reviewed patch and the
  released nils-cli v1.27.27 binaries passes the real tools pipeline with
  AF_UNIX, AF_NETLINK, localhost, user systemd, Docker, supplementary-group,
  and exact host network-namespace identity probes under `danger-full-access`.
- Reversing the patch and rebuilding reproduces the exact pristine 4,057-file
  build closure. A separate unpatched DSH tools canary succeeds with runtime-kit
  acceptance confirmed absent, proving rollback beyond a CLI startup check.

**Links**

- [Native full-host authority issue #133](https://github.com/sympoies/dsh-runtime-kit/issues/133)
- [nils-cli finish-line repair PR #1568](https://github.com/sympoies/nils-cli/pull/1568)
- [nils-cli v1.27.27](https://github.com/sympoies/nils-cli/releases/tag/v1.27.27)
- [Original nils finish-line containment PR #1468](https://github.com/sympoies/nils-cli/pull/1468)

## 2026-08-30 - Trace post-stop Agent settlement without session content

**Result**

- Positive and candidate-upgrade canaries now retain fixed milestones when the
  tracked turn commits `turn/end`, the Agent publishes idle status, an idle
  Agent restarts, or its next turn begins after a completed stop callback.
- The observers use only public `session/event` and `agent/status` boundaries;
  they retain no event payload, message, tool result, workspace value, or
  provider diagnostic. Successful receipts, policy behavior, deadlines, and
  authenticated DSH patch bytes remain unchanged.

**Why / context**

- Hosted Gate 4 run `33283080275` authenticated and sealed every immutable
  input, then reached the positive-canary deadline after a second stop request
  had reached the canary-owned listener tail.
- The repeated-stop marker could not distinguish failure to commit the turn,
  an unresolved idle transition, or a new wake-driven Agent activity. The new
  fixed milestones isolate those observable branches through four bounded
  diagnostic allowlist additions without changing verdict or successful-
  receipt behavior.

**Evidence**

- Test-first validation began at 60/62 because the new settlement observer and
  allowlisted code were absent, then the focused acceptance and canary suites
  passed 62/62.
- The settlement regression proves events before a completed stop are ignored
  and orders turn end, idle, restart, and next-turn markers explicitly.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted repeated-stop deadline](https://github.com/serenvia/sympoies-infra/actions/runs/33283080275)

## 2026-08-29 - Distinguish stop-listener tail from repeated stopping

**Result**

- Positive and candidate-upgrade canaries now retain a fixed milestone after
  their own stop callback has resolved and the following listener-tail
  observer has run.
- A second completed stopping boundary on the tracked Agent receives a
  distinct repeated-stop milestone. This separates a continued stop loop from
  a first stopping waterfall that has reached the canary-owned listener tail.
- The parent accepts both values only through its exact process- and
  phase-bound allowlist. Runtime policy, DSH patch bytes, successful receipts,
  and execution and process deadlines remain unchanged.

**Why / context**

- Hosted Gate 4 run `33242504804` authenticated and sealed every immutable
  input, then reached the positive-canary deadline after the canary's own async
  stop callback fulfilled but before the Agent reported idle.
- That callback marker alone could not distinguish another listener, a
  policy-driven continued step, or the synchronous tail of DSH turn
  settlement. The new milestones narrow those branches without exposing
  session content or changing acceptance behavior.

**Evidence**

- Test-first validation failed three focused assertions because the new fixed
  codes and listener-tail behavior were absent.
- The focused acceptance and canary contracts pass 61/61, the complete Node 24
  package suite passes 719/719, and strict typechecking passes.
- The held-callback regression proves the tail marker cannot advance while the
  callback is pending; a two-stop regression proves the second completed
  boundary is classified separately.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted post-callback deadline](https://github.com/serenvia/sympoies-infra/actions/runs/33242504804)

## 2026-08-29 - Bound hosted progress after the canary stop callback

**Result**

- Positive and candidate-upgrade canaries now advance to a distinct
  `CANARY_STOP_CALLBACK_COMPLETED` milestone only after their own
  turn-stopping callback returns or resolves successfully.
- The parent accepts the new process- and phase-bound deadline code through
  the same exact allowlist. A callback failure cannot advance the marker, and
  the milestone does not claim completion of the full listener waterfall,
  turn settlement, or agent idle.
- Runtime policy, successful receipts, execution and process deadlines, and
  the authenticated DSH patch remain unchanged.

**Why / context**

- Hosted Gate 4 run `33235197698` authenticated and sealed every immutable
  input, completed the already-registered runtime stop listeners, and then
  reached the positive-canary deadline.
- The prior marker was deliberately placed before the canary callback, so the
  typed failure could not distinguish callback completion from the remaining
  turn-to-idle boundary.

**Evidence**

- Test-first validation began at 15/16 because the new milestone was absent.
- The focused diagnostic contracts pass 60/60. Listener-waterfall and callback
  settlement tests prove the new marker follows success, remains at the prior
  boundary while pending, and cannot advance after rejection or a throw.
- The complete Node 24 suite passes 718/718, strict typechecking passes, and
  the 10-leg local authoritative matrix passes with zero acceptance,
  finish-line, reservation, and correlation resources after every leg.
- Two independent builders reproduced the exact reviewed package byte for byte
  with Node 24.16.0, npm 11.19.0, and zlib-ng compatibility gzip. The package
  digest remains in the delivery and Gate acceptance evidence so this packed
  devlog does not create a self-referential artifact claim.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted stop-listener completion failure](https://github.com/serenvia/sympoies-infra/actions/runs/33235197698)

## 2026-08-29 - Refine hosted canary progress to lifecycle sub-boundaries

**Result**

- Positive and candidate-upgrade canaries now retain fixed milestones for the
  validation request/result, host-validator request/result, requested stop,
  and entry to and completion of the runtime stop-policy listeners.
- A prepended content-free observer distinguishes entry to the public
  `agent/turn-stopping` boundary from completion of the already-registered
  runtime listeners. Its shared registration helper is exercised through a
  held listener waterfall, not inferred from source text. The parent accepts
  the new values only through its exact allowlist and process/phase binding.
- Successful receipts, execution and process deadlines, runtime policy, nils
  acceptance semantics, and authenticated DSH patch bytes are unchanged.

**Why / context**

- Hosted Gate 4 run `33229400323` authenticated every sealed input but reached
  its positive-canary deadline while waiting for the DSH Agent to become idle.
  The earlier coarse marker could not distinguish an unfinished validator from
  a stop-policy operation.
- An isolated replay with the exact Node 24.16.0, nils-cli 1.27.22, runtime
  package, and patched DSH rc.2 inputs completed all authoritative lifecycle,
  recovery, upgrade, and rollback legs. The remaining delta is confined to the
  hosted low-privilege user-manager and network-denied boundary.

**Evidence**

- Test-first validation began at 54 passing and three expected failures because
  the new marker names were absent from the canary and parent allowlists.
- The focused diagnostic contracts pass 58/58, the complete Node 24 package
  suite passes 716/716, and strict typechecking passes.
- Review regression coverage holds an already-registered runtime listener,
  observes the deadline at `TURN_STOPPING_ENTERED`, then releases it and proves
  the canary advances to `RUNTIME_STOP_LISTENERS_COMPLETED`. The narrower name
  does not claim completion of the canary callback or listeners registered
  later in the event waterfall.
- Marker tests continue to reject unknown, duplicated, mismatched, accessor-
  backed, or extended payloads without exposing child output.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted waiting-for-idle failure](https://github.com/serenvia/sympoies-infra/actions/runs/33229400323)

## 2026-08-29 - Expose bounded hosted canary deadline progress

**Result**

- A failed authoritative canary deadline now emits one allowlisted progress
  code bound to the exact phase and process identity before failure disposal
  and host exit.
- The parent acceptance process accepts only the exact failure schema, one
  marker, the expected phase and process, and a declared cause code. Unknown,
  duplicated, mismatched, accessor-backed, or extended inputs remain
  `UNKNOWN_FAILURE`.
- Marker flushing shares the receipt writer's awaited stream boundary, so
  backpressure cannot let disposal or host exit overtake the diagnostic.
- Successful receipts, the 120-second execution deadline, the 180-second
  process supervisor, DSH patch bytes, and runtime policy are unchanged.

**Why / context**

- Hosted Gate 4 runs `33222663532` and `33224027500` authenticated and sealed
  every immutable input, then failed the positive candidate phase after the
  canary deadline with an ordinary nonzero exit and `UNKNOWN_FAILURE`.
- A runner-equivalent clean-environment replay completed the positive phase in
  about six seconds and passed every later candidate leg through provider
  mismatch. That excluded Node, pnpm, environment cleaning, and executable
  ancestry, leaving the hosted lower-privilege execution boundary as the
  unresolved delta.
- Raw child output is intentionally excluded from public acceptance evidence;
  a fixed progress vocabulary provides the missing distinction without
  weakening that boundary or extending the timeout.

**Evidence**

- Test-first validation began RED because neither the packed progress reporter
  nor the parent marker recorder existed on the accepted baseline.
- Specialist review found that the first implementation did not await the
  failure marker write. A backpressured delayed-flush regression reproduced
  the ordering bug before the controller was repaired to claim one finalizer
  and await the marker before failure reporting, disposal, and exit.
- The canary suite passes 14/14, the acceptance contract suite passes 42/42,
  the complete Node 24 package suite passes 714/714, and strict typechecking
  passes. Security, testing, and maintainability review converged with no
  remaining material finding.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [First hosted unknown-failure run](https://github.com/serenvia/sympoies-infra/actions/runs/33222663532)
- [Confirmed hosted unknown-failure run](https://github.com/serenvia/sympoies-infra/actions/runs/33224027500)

## 2026-08-29 - Separate canary work and process-settlement deadlines

**Result**

- The packed authoritative canary now derives one canary-wide 120-second
  execution deadline from the DSH process launch, separately from a
  180-second process supervisor deadline.
- Every real canary phase consumes the shared process deadline, preserving a
  bounded 60-second window for failure or receipt flushing, resource disposal,
  and host exit after the canary-wide deadline.
- A canary loaded after startup receives only the remaining execution budget;
  it cannot move its deadline later by arming a new full-duration timer.
- Normal completion rechecks that same absolute deadline before clearing its
  timer, so a delayed callback cannot turn an elapsed deadline into success.
- The acceptance verdict, effective whole-phase bound, DSH patch, and nils policy are
  unchanged.

**Why / context**

- Hosted Gate 4 run `33212321687` authenticated every immutable input, then
  failed the positive candidate phase with `ETIMEDOUT`.
- The outer synchronous supervisor and the canary's final idle watchdog both
  expired after 120 seconds but started at different lifecycle points. Under
  hosted scheduling, the supervisor could terminate DSH before the canary had
  time to publish its own bounded outcome.
- Delivery security review then reproduced a second ordering edge: if the
  absolute deadline elapsed while its callback remained queued, normal
  completion could clear the timer and publish success. The repaired controller
  routes that state through the same single-flight deadline failure.

**Evidence**

- The new supervisor/deadline contract test began RED on the accepted merge
  tree because no explicit deadline relationship existed.
- The focused canary suite passes 13/13, the acceptance contract suite passes
  40/40, the complete package suite passes 711/711 on Node 22.19.0 with npm
  11.19.0, and strict typechecking passes.
- The contract tests verify both real consumers and use an injected late-start
  clock to prove that canary startup cannot reset the process-origin budget or
  clear an elapsed deadline into a success receipt.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted timeout failure 33212321687](https://github.com/serenvia/sympoies-infra/actions/runs/33212321687)

## 2026-08-29 - Make hosted acceptance startup and timeout reporting deterministic

**Result**

- Acceptance-required packed canaries now subscribe to both `dshRuntimeKit`
  and `dshAcceptance`, so staggered Cordis service activation cannot strand a
  scenario after observing only the runtime service.
- The authoritative scenario controller accepts a synchronous subprocess only
  when it has status zero, no spawn error, and no terminating signal. A DSH
  process that handles the timeout signal and exits zero can no longer mask
  the controller's `ETIMEDOUT` result as a missing receipt marker.
- Failed subprocess assertions now expose only a bounded executable label and
  the structured scenario diagnostic; child output, arguments, error messages,
  and stacks cannot enter the hosted failure log.

**Why / context**

- Hosted Gate 4 run `33197791532` authenticated every input but waited eight
  minutes without a positive receipt. The same immutable runtime and nils-cli
  artifacts completed the full matrix outside the supervised boundary.
- Bounded process inspection showed a staggered runtime/acceptance activation
  with no scenario start. The later controller deadline sent `SIGTERM`; DSH
  shut down cleanly, while the old status-only assertion discarded the native
  timeout fact.

**Evidence**

- Test-first validation began RED because neither the dual-service starter nor
  the complete subprocess-success predicate existed.
- The focused suites pass 49/49, the rebased complete package suite passes
  707/707, strict typechecking passes, and the package dry-run contains 1,206
  files.
- A sentinel regression proves that a zero-status `ETIMEDOUT` result cannot
  disclose child stdout, stderr, or error text through public diagnostics.
- The real packed DSH 0.1.1-rc.2 pipeline completes all ten authoritative
  lifecycle legs, including cancellation, restart, crash recovery, upgrade,
  and rollback, with the exact nils-cli 1.27.22 artifacts.
- Production acceptance policy, nils provider behavior, and authenticated DSH
  patch bytes are unchanged; this repair is confined to Gate 4 canary startup
  and scenario process classification.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted supervised failure 33197791532](https://github.com/serenvia/sympoies-infra/actions/runs/33197791532)

## 2026-08-29 - Flush the authoritative canary receipt before host exit

**Result**

- The packed authoritative canary now awaits the stdout write callback for its
  complete authenticated receipt line before requesting DSH host exit.
- The receipt marker and write boundary live in a packed fixture helper, with
  tests that prove host exit cannot advance before the callback and that write
  errors fail the canary closed.

**Why / context**

- Hosted Gate 4 run `33189519550` authenticated and sealed every input, then
  returned DSH status zero while the positive receipt marker was absent.
- The canary previously started an asynchronous stdout write and immediately
  entered its host-exit path. A pipe can therefore lose the marker even though
  all runtime acceptance work completed successfully.

**Evidence**

- Test-first validation began RED because the awaited receipt-output boundary
  did not exist. The focused canary suite passes 7/7, including delayed flush,
  write-failure, and unavailable or throwing host-exit coverage.
- The acceptance and diagnostic suites pass 38/38, strict typechecking passes,
  and the packed fixture includes the new helper.
- Acceptance policy, DSH patch bytes, nils integration, and receipt contents
  are unchanged; only delivery of the already-authenticated receipt is ordered
  before host exit.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted missing-marker failure 33189519550](https://github.com/serenvia/sympoies-infra/actions/runs/33189519550)

## 2026-08-29 - Add the governed workload manager boundary

**Result**

- The public `@sympoies/dsh-runtime-kit/manager` surface now owns strict v1
  workload admission, trust acceptance, lifecycle, control, host dispatch, and
  reconciliation contracts for coding, conversational, event-driven, and
  scheduled bot profiles.
- Lifecycle transitions preserve bounded session identity, exact receipts, and
  recoverable source state. Trust failures distinguish local validation from
  authority availability without allowing adapters to forge authority
  outcomes.
- Canonical request, document, result, and host-frame bounds fail closed before
  cloning or dispatch, and failure details expose only the documented
  health-probe field.
- Retained instance identity is revalidated before fresh or replayed host
  dispatch, and authority-failure payloads reject excessive size, nesting,
  cycles, and secret-shaped values before digest work.

**Why / context**

- Governed plugin composition needs a separate workload control boundary before
  private GitHub App credentials or deployment topology can safely drive bot
  instances.
- Public runtime policy belongs in this package, while private installation
  identities, credentials, routing, and operator state remain in private
  infrastructure repositories.

**Evidence**

- The focused suites pass 16/16 composition, 19/19 lifecycle, 4/4 admission,
  and 25/25 control-boundary cases; the complete package suite passes 703/703.
- Strict typechecking, diff checks, package dry-run, package self-import, and
  all 101 policy-source parity rules pass.
- The reviewed patch applies to pinned DSH 0.1.1-rc.2, the rebuilt patched host
  passes the real-tools smoke, and reversal is followed by a pristine rebuild,
  successful unpatched CLI startup, and clean compatibility attestation.
- API-contract, security, testing, maintainability, performance, and final
  red-team review converged with no remaining material finding before
  pre-merge review.

**Links**

- [Governed DSH applications plan #318](https://github.com/serenvia/sympoies-infra/issues/318)

## 2026-08-28 - Split hosted canary receipt failures into bounded stages

**Result**

- Packed authoritative acceptance now distinguishes canary receipt marker,
  JSON, schema, phase, process-instance, and the positive required-result
  boundary after a successful DSH subprocess exit.
- Receipt parsing accepts exactly one marker and exact schema, phase, and
  process bindings. Its errors never copy child output or untrusted receipt
  values into the diagnostic surface.
- The positive leg advances to a separate result-shape step after the receipt
  is authenticated, so a missing tool result no longer aliases receipt drift.

**Why / context**

- Hosted Gate 4 run `33176556010` authenticated and sealed every input, then
  failed at `candidate-positive` with no exit status, timeout, or signal. The
  earlier process classifier therefore proved that DSH returned success and a
  later receipt assertion failed, but marker absence and receipt-shape drift
  still collapsed into `UNKNOWN_FAILURE`.
- Raw process output is intentionally unavailable to public acceptance
  evidence. Fixed stage identifiers provide the missing distinction without
  weakening that privacy boundary or changing runtime behavior.

**Evidence**

- Test-first validation began RED because the bounded receipt parser did not
  exist. The new focused cases pass 3/3 and cover marker absence, duplicate
  markers, invalid JSON, schema/phase/process drift, exact success, and one
  sanitized uncaught-failure diagnostic.
- The acceptance contract suite passes 38/38, the complete package suite
  passes on the current rebased delivery head, strict typechecking passes, and
  `git diff --check` is clean.
- DSH patch bytes, nils policy, profile installation, production services, and
  no-provider behavior are unchanged.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted receipt-boundary failure 33176556010](https://github.com/serenvia/sympoies-infra/actions/runs/33176556010)

## 2026-08-28 - Freeze governed plugin composition contracts

**Result**

- The public `@sympoies/dsh-runtime-kit/composition` surface now defines strict
  v1 plugin descriptors, bot profiles, resolved compositions, and sibling lock
  receipts with canonical domain-separated digests.
- The manager-facing validate and resolve protocol authenticates validated
  document identities and a public-policy digest, emits exact success/failure
  unions, and keeps credentials and private deployment state outside public
  documents.
- Deterministic bounded backtracking selects globally valid versions, propagates
  singleton dependency closure incrementally, and fails closed on conflicts,
  cycles, missing health, authority widening, stale locks, oversized input, or
  exhausted search work.

**Why / context**

- Reusable plugins and coding, conversational, event, and scheduled bot
  profiles need one public composition contract before a separate application
  layer or private deployment controller can safely consume them.
- Discovery, installation, triggers, and package declarations must never grant
  runtime authority. The resolved ceiling is therefore derived only from the
  plugin requests, profile grants, and authenticated public policy.

**Evidence**

- The focused contract suite passes 16/16, including catalog-order invariance,
  invalid-newer-version fallback, bounded adversarial search, an oversized
  request, a pre-access 1,025-item catalog rejection, and forward/reverse
  1,024-plugin independent and dependency-chain resolutions.
- The complete package suite passes 641/641 with zero failures or cancellations;
  strict typechecking, package dry-run, and package self-import also pass.
- Policy-source parity verifies all 101 rules, and the pristine pinned DSH
  0.1.1-rc.2 compatibility check remains green.
- API-contract, testing, maintainability, security, red-team, and performance
  review converged with no remaining material finding.

**Links**

- [Composition governance issue #109](https://github.com/sympoies/dsh-runtime-kit/issues/109)
- [Governed DSH applications plan #318](https://github.com/serenvia/sympoies-infra/issues/318)

## 2026-08-28 - Make default-delivery inspection recovery executable

**Result**

- A lone `block-unsafe-default-delivery` denial now leads with an immediate,
  copyable recovery: retry one read-only command per Bash call, use the exact
  checkout as the Bash workdir, and keep inspection separate from delivery.
- The same first line explicitly says that no operator intervention is
  required. Governed commit and PR guidance remains on a separate follow-up
  line for calls that are genuinely delivering a change.
- Policy classification, command admission, and fail-closed behavior are
  unchanged; only the DSH-facing explanation is refined.

**Why / context**

- A fresh live DSH acceptance session correctly completed `runtime_context`,
  then combined several ordinary repository inspections into one Bash call.
  The policy denied that opaque shape before dispatch, but the prior generic
  instruction to "split" was not concrete enough: the agent treated it as a
  mandatory terminal failure instead of applying the safe retry itself.
- Agents should not need to infer whether an inspection denial needs operator
  action or how finely to split the next call when the runtime already knows
  the bounded recovery shape.

**Evidence**

- The regression began RED because the first visible denial line contained no
  `Retry now` instruction. It passes after asserting the immediate retry,
  one-command boundary, exact workdir, no-operator statement, and retained
  governed-delivery guidance.
- The complete package suite passes 634/634. Strict typechecking passes, and
  the policy benchmark passes 2,000 samples with a 1.14 ms p95, zero active
  operations, and zero retained-heap growth.

**Links**

- [Actionable shell denial recovery #118](https://github.com/sympoies/dsh-runtime-kit/pull/118)

## 2026-08-28 - Classify hosted process termination without exposing output

**Result**

- Authoritative acceptance now distinguishes a bounded native subprocess
  timeout or POSIX signal from a generic no-status failure.
- The shared result projector accepts only own data properties and fixed public
  error-code and signal allowlists. It never reads accessors, inherited fields,
  child output, messages, stacks, paths, or environment values.
- Each scenario leg clears prior process metadata, so a later failure cannot
  inherit a stale cause, signal, or exit status.
- The asynchronous crash leg stops waiting when its child exits, is signaled,
  or fails to spawn before the marker, while its deliberate post-marker kill
  remains outside failure classification.

**Why / context**

- The post-merge hosted Gate 4 run authenticated every input and reached the
  positive candidate leg, but its child returned without an exit status. The
  same immutable package completed all 12 functional legs and the patched and
  unpatched DSH lifecycle locally.
- The earlier bounded diagnostic correctly protected private runner state but
  could not separate deadline termination, a resource signal, and another
  spawn failure. That distinction is required before changing runtime behavior.

**Evidence**

- Regression tests began RED for the missing result projector, then exercise
  real Node subprocess timeout and `SIGKILL` results through the production
  recorder. Separate tests prove stale-state clearing and zero accessor use.
- The focused diagnostic checks pass 7/7, the acceptance contract suite passes
  36/36, and the complete package suite passes 633/633. Strict typechecking
  and 101-rule policy-source parity also pass.
- Testing, maintainability, and security follow-up reviews reported no
  remaining material finding. A red-team review found the asynchronous crash
  gap; its closed-set follow-up passed after real pre-marker `SIGKILL`, spawn
  failure, and post-marker termination coverage. DSH source patch bytes, nils
  policy behavior, package installation, and profile semantics are unchanged.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted failure 33166106735](https://github.com/serenvia/sympoies-infra/actions/runs/33166106735)

## 2026-08-28 - Validate nils-cli 1.27.22 for managed startup convergence

**Result**

- The exactly validated nils-cli release advances from 1.27.21 to 1.27.22,
  while the minimum supported release remains 1.27.17 and rollback validation
  remains unchanged.
- The compatibility manifest binds the signed tag commit, authenticated Linux
  and Darwin ARM64 archives, six Linux delivery binaries, and the two Darwin
  policy binaries used by runtime acceptance.
- DSH startup now consumes the bounded activity admission and actionable retry
  behavior that prevents helper-resolution faults from trapping read-only
  managed-session startup in a repeated hook cycle.

**Why / context**

- Managed startup could repeatedly fail before useful work when the activity
  helper could not classify a read-only hook command or resolve its runtime
  context. Restarting the agent only repeated the same prerequisite sequence.
- The new release keeps mutation admission fail closed while allowing audited
  read-only startup handling to converge and makes an unclassifiable shell
  retry explain the recovery boundary instead of expanding into another loop.

**Evidence**

- Contract expectations first failed against the retained 1.27.21 manifest
  and workflow mirrors, then passed after the complete release identity was
  updated.
- The verified annotated `v1.27.22` tag points to release commit
  `5ffcf3c01226fc2d1e513984210976bf00d2df08`.
- Both published checksum sidecars matched their downloaded archives, and all
  recorded binary digests were recomputed from those authenticated archives.

**Links**

- [nils-cli v1.27.22](https://github.com/sympoies/nils-cli/releases/tag/v1.27.22)
- [Managed startup activity convergence #1554](https://github.com/sympoies/nils-cli/pull/1554)
- [Unclassifiable shell retry diagnostics #1555](https://github.com/sympoies/nils-cli/pull/1555)
- [Cross-runtime startup convergence #330](https://github.com/serenvia/sympoies-infra/issues/330)

## 2026-08-28 - Bound authoritative failures to one safe inner-leg diagnostic

**Result**

- The packed authoritative scenario tracks each matrix leg and emits exactly
  one structured failure diagnostic with the bounded producer, current leg,
  and optional nonzero operation exit status.
- Fatal-path reporting never inspects the thrown value. Its cause is the fixed
  public `UNKNOWN_FAILURE` code, and parsed producer diagnostics normalize all
  non-allowlisted cause codes to that same value.

**Why / context**

- A hosted Gate 4 run authenticated and sealed every input, then reported only
  that the packed process matrix failed. The same immutable candidate passed
  all 12 functional matrix legs locally, so another blind retry could not
  isolate the runner-specific failure.
- Error messages, stacks, command output, paths, getters, and Proxy traps are
  inappropriate inputs to a durable acceptance record. The diagnostic needed
  to identify the failed leg without observing any of those values.

**Evidence**

- The initial regression failed because no diagnostic builder existed. Review
  follow-ups then began RED for untrusted accessor execution, arbitrary
  token-shaped cause codes, and missing later-leg operation status coverage.
- The focused diagnostic suite passes 5/5, including exact-one emission,
  later-leg status 17, stale-status clearing, zero getter or Proxy invocation,
  and cause-code normalization.
- The complete Node 22.19 package suite passes 623/623. Strict typechecking,
  the 101-rule policy-source verification, and pristine DSH 0.1.1-rc.2
  compatibility inspection also pass.
- Testing, maintainability, and security follow-up reviews reported no
  remaining material finding. The DSH patch, nils policy, profile behavior,
  and no-provider runtime path are unchanged.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Opaque hosted failure 33151327114](https://github.com/serenvia/sympoies-infra/actions/runs/33151327114)

## 2026-08-28 - Validate nils-cli 1.27.21 for DSH user namespaces

**Result**

- The exactly validated nils-cli release advances from 1.27.19 to 1.27.21,
  while the minimum supported release remains 1.27.17.
- The compatibility manifest binds the verified tag commit, Linux and Darwin
  ARM64 archives, six Linux delivery binaries, and the two Darwin policy
  binaries used by runtime acceptance.
- DSH can now use the fixed system Git executable when strict Linux user
  namespace evidence proves that host root appears as the unmapped overflow
  owner. Existing path, mode, and ambiguous-mapping refusals remain fail closed.

**Why / context**

- Live DSH finish-line validation found that a rootless user namespace exposes
  the trusted system Git file with the kernel overflow UID. The previous
  numeric-root-only check rejected it before dirty-checkout inspection.
- The same release makes runtime-handler failures actionable with sanitized
  handler identity, capability cause, doctor guidance, and the owning repair
  path. Agents no longer need to infer the next diagnostic from a generic
  fail-posture code.

**Evidence**

- The owner contract began RED because the manifest still validated 1.27.19.
  Both release archives match their published checksum sidecars, and every
  recorded binary digest was recomputed from the authenticated archives.
- The verified annotated `v1.27.21` tag points to release commit
  `2d077ec976e66be425e519222967cb1a5e9fed18`.

**Links**

- [nils-cli v1.27.21](https://github.com/sympoies/nils-cli/releases/tag/v1.27.21)
- [Runtime handler diagnostics #1550](https://github.com/sympoies/nils-cli/pull/1550)
- [User-namespace system Git trust #1552](https://github.com/sympoies/nils-cli/pull/1552)
- [Sympoies DSH fleet delivery issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-28 - Bound review discovery and repair convergence

**Result**

- Each delivery review generation now permits one broad discovery pass.
  Ordinary repair commits stay in that generation and receive closed-set
  follow-up over admitted findings, repair hunks, and their direct regression
  surface.
- A new concern extends the repair set only when the repair introduced a
  reachable, material correctness, security, data, migration, or public-contract
  regression. Raw diff size alone no longer activates red-team review, and low
  or informational observations do not block delivery.
- Reviewer personas and PR, tracking, and dispatch delivery owners now share
  the same admission and stopping rules.

**Why / context**

- Re-running full discovery after every repair allowed new architecture
  preferences, hypothetical hardening, and unrelated edge cases to turn review
  into an open-ended redesign loop. That made delivery difficult and could
  obscure the accepted observable outcome.
- The bounded model keeps material bugs actionable while separating possible
  improvements and newly discovered unrelated work from completion of the
  current change.

**Evidence**

- The regression contract first failed against the former broad-review and
  repeat-until-empty rules, then passed after the policy port.
- The focused skill and native reviewer suites pass 28/28, including read-only
  authority, parallel ordering, red-team sequencing, cancellation, and JSONL
  validation behavior.

**Links**

- [Bounded review convergence issue #113](https://github.com/sympoies/dsh-runtime-kit/issues/113)
- [Canonical runtime-kit implementation PR #72](https://github.com/sympoies/agent-runtime-kit/pull/72)

## 2026-08-28 - Exercise authenticated DSH clone trust through upload-pack

**Result**

- The authoritative clone boundary canonicalizes the DSH source once, then
  uses that exact identity for inspection, private trust configuration, and
  the fresh local clone.
- The clone and checkout now have one internal owner that derives its private
  Git config from the isolated environment instead of accepting a second
  configuration path.

**Why / context**

- Source assertions alone could remain green if exact trust was configured too
  late or never reached the local `git-upload-pack` child that rejected the
  distinct-UID hosted candidate.
- A symlinked source argument could also authenticate one path while recording
  trust for another unless the runner established one canonical identity
  before both operations.

**Evidence**

- The focused regression began RED because the production clone owner did not
  exist. It now makes local upload-pack simulate a different owner: two
  command-scoped values fail with Git's dubious-ownership rejection, the two
  private exact identities succeed, and caller plus system wildcard trust
  remain excluded.
- A symlinked input proves behaviorally that authentication and upload-pack use
  the same canonical repository identity; the resulting checkout is detached
  at the exact requested revision.
- The focused acceptance cases pass 3/3. The complete package suite passes
  619/619, strict typechecking passes, and policy-source parity verifies all
  101 rules.
- Testing and maintainability follow-ups closed both regression-design
  findings. Security, performance, and adversarial reviews reported no
  remaining material finding.

**Links**

- [Clone-trust repair PR #110](https://github.com/sympoies/dsh-runtime-kit/pull/110)
- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)

## 2026-08-28 - Validate nils-cli 1.27.19 without raising the compatibility floor

**Result**

- The exactly validated nils-cli release advances from 1.27.17 to 1.27.19,
  while the minimum supported release remains 1.27.17.
- The release identity binds the signed source commit, Linux and Darwin ARM64
  archives, six Linux delivery binaries, and the two Darwin policy binaries
  consumed by runtime acceptance.
- DSH agents can inspect Git remotes through the nils-owned read-only policy
  contract; mutating remote operations remain governed delivery actions.

**Why / context**

- Issue #213 live acceptance exposed that ordinary `git remote`, `git remote
  -v`, and constrained `git remote get-url` inspection was denied before
  dispatch. That forced an agent to guess whether repository identity could be
  checked safely.
- nils-cli 1.27.19 carries the bounded read-only admission repair without
  retiring the existing workspace, finish-line, signing, or provider
  governance protocols, so raising the minimum compatibility floor would be
  unnecessary.

**Evidence**

- The owner regression began RED because the manifest still validated
  1.27.17. The exact 1.27.19 archives match their published checksum sidecars,
  and every recorded binary digest was recomputed from those archives.
- The annotated `v1.27.19` tag is verified and points to signed commit
  `07969611cbf99ae6d07c20730e4e46e370bb25f3`.

**Links**

- [nils-cli v1.27.19](https://github.com/sympoies/nils-cli/releases/tag/v1.27.19)
- [Read-only remote query repair #1541](https://github.com/sympoies/nils-cli/pull/1541)
- [Release PR #1547](https://github.com/sympoies/nils-cli/pull/1547)
- [Sympoies DSH fleet delivery issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-28 - Persist exact DSH clone trust for Git subprocesses

**Result**

- The authoritative acceptance runner now writes only the authenticated DSH
  worktree and repository identities into its private global Git config before
  cloning the pristine source.
- System configuration remains disabled and no caller or wildcard trust is
  propagated into the credentialless candidate.

**Why / context**

- A local-path clone starts `git-upload-pack` as a child process. Command-scoped
  `safe.directory` values protected the outer `git clone` process but did not
  reach the child's repository ownership check.
- Persisting the same two exact identities in the already-private per-run
  config carries only the required trust across that process boundary.

**Evidence**

- Authoritative run 33125430377 authenticated and sealed every input, then
  failed closed at `fresh DSH source clone`.
- A distinct-UID reproduction failed with command-scoped trust and succeeded
  with the same exact values in the private global config.
- The focused regression passes 2/2, strict typechecking and the 101-rule
  policy-source verification pass, and the complete package suite passes
  618/618.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted failing run 33125430377](https://github.com/serenvia/sympoies-infra/actions/runs/33125430377)

## 2026-08-28 - Attempt command-scoped isolation for authenticated DSH cloning

**Result**

- The authoritative acceptance runner gave the fresh local DSH clone only
  two command-scoped Git trust identities: the authenticated source worktree
  and its exact `.git` repository path.
- The runner created a private empty global Git config and disabled system Git
  configuration, removing caller, XDG, global, and system wildcard trust from
  the isolated candidate process.

**Why / context**

- The distinct-UID hosted candidate reached the fresh DSH clone only after the
  outer runner trust boundary was corrected. The nested runner rebuilt its
  environment and therefore discarded that outer configuration; provider Git
  then resolved the local source through its `.git` identity and failed closed.
- Propagating arbitrary caller configuration would have widened trust. The
  command-scoped alternative preserved exact identities, but a later hosted
  run proved that local `git-upload-pack` did not inherit them.

**Evidence**

- The hosted regression failed at `fresh DSH source clone`; focused source
  contracts then failed before the nested identity and Git-isolation controls
  were implemented.
- The focused acceptance suite passes 22/22, including a behavioral wildcard
  control under hostile parent Git variables. Strict typechecking and the
  complete package suite pass, with 607/607 tests successful.
- A fresh managed DSH v0.1.1-rc.2 checkout built its host libraries, retained
  the exact pinned revision with a clean worktree, and passed public-export
  compatibility inspection.
- Later authoritative run 33125430377 failed at the same clone boundary,
  superseding the original conclusion and motivating the per-run config fix
  recorded above.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Hosted failing run 33109942791](https://github.com/serenvia/sympoies-infra/actions/runs/33109942791)
- [Distinct-UID runner trust PR #315](https://github.com/serenvia/sympoies-infra/pull/315)

## 2026-08-28 - Explain default-delivery denials without guesswork

**Result**

- A lone `block-unsafe-default-delivery` result now says that policy stopped
  the command before dispatch because it could not prove a read-only inspection
  or governed delivery shape.
- The denial tells agents to separate inspection from delivery and names the
  governed commit, worktree, and pull-request workflow for real delivery work.
- The handler code and nils-owned admission decision remain unchanged; the DSH
  transport only presents a bounded recovery path.

**Why / context**

- A live repository baseline combined several read-only Git queries in one
  shell call and surfaced only the handler ID. Although fail-closed behavior
  was correct, the agent could not tell whether to split inspection, choose a
  governed delivery tool, or retry the same command.
- The earlier opaque-shell guidance intentionally covered only the full
  classifier fan-out, so the single-handler boundary needed its own message.

**Evidence**

- The focused regression began at 0/1 because the denial contained only
  `agent-hook:block-unsafe-default-delivery`.
- The focused single-handler and opaque-fan-out contract tests pass together;
  strict typechecking and the complete repository suite also pass.

**Links**

- [Sympoies DSH fleet delivery issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-28 - Make opaque shell denials actionable in DSH

**Result**

- DSH policy denials now recognize the full opaque-shell classifier fan-out
  and explain that execution stopped before command dispatch.
- The error directs agents to invoke executable repository scripts directly,
  avoid an unnecessary `bash` or `sh` wrapper, and split compound operations
  into independently classifiable tool calls.
- Admission remains entirely owned by agent-hook; this change adds no command,
  path, environment, or repository allowlist.

**Why / context**

- A governed repository validation wrapped as `bash script.sh` was correctly
  denied by every shell classifier, but DSH rendered only a comma-separated
  list of handler IDs. That left the agent to guess at invocation shape and
  encouraged ineffective retries.
- Executing the same repository-owned executable directly reached the real
  validation, confirming that the missing behavior was diagnostic guidance,
  not an authorization exception.

**Evidence**

- The focused regression began at 0/1 because the previous denial contained
  neither the pre-dispatch boundary nor the direct-executable recovery path.
- The focused policy tests pass 2/2, strict typechecking passes, and the full
  repository suite passes 605/605.

**Links**

- [Sympoies DSH fleet delivery issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-28 - Unblock dsh-TUI 0.9.3 with an authenticated package patch

**Result**

- The 0.9.3 promotion no longer waits for an upstream release. Runtime-kit now
  owns a temporary installed-package patch that moves history-lock retries off
  the prompt submission stack.
- The patch lifecycle binds the exact package name and version, published
  `package.json` bytes, patch artifact digest, and target before/after hashes.
  It emits typed, idempotent check/apply/reverse receipts and rejects unknown,
  drifted, partially patched, or symlinked targets.
- The authenticated Agent Console smoke installs the exact registry artifact,
  applies the reviewed package patch, proves prompt history returns promptly
  while another process holds the lock, and only then boots the TUI.
- The upstream repair remains tracked for eventual removal of the downstream
  patch, but upstream merge and publication are no longer promotion blockers.

**Why / context**

- The published 0.9.3 history path performs up to 500 synchronous
  `Atomics.wait` retries before dispatching input. A contended history file can
  therefore make both typing and command submission appear frozen even though
  the Agent Console connection and DSH agent are healthy.
- The narrow package patch retains the existing best-effort history behavior,
  stale-lock handling, retry limit, and file format. Only lock acquisition and
  retry waits become asynchronous, and retry timers are unreferenced so they
  cannot delay process exit.

**Evidence**

- Against the authenticated 0.9.3 npm tarball, a fresh held lock made the
  unpatched `appendHistory()` return after 3,584 ms. The exact patched artifact
  returned in 0.33 ms under the same condition.
- The patch lifecycle regression first failed because no TUI patch owner
  existed. The focused lifecycle/profile suite now passes 15/15 and strict
  typechecking succeeds.
- The checked-in artifact is bound to the published tarball's exact
  `history.js` before hash and the reviewed nonblocking after hash; the smoke
  also rejects startup if the installed package cannot produce a patched
  receipt.

**Links**

- [dsh-TUI v0.9.3](https://github.com/ccch1mneyyy/dsh-TUI/releases/tag/v0.9.3)
- [Upstream history-lock follow-up #588](https://github.com/ccch1mneyyy/dsh-TUI/issues/588)
- [Runtime-kit promotion PR #99](https://github.com/sympoies/dsh-runtime-kit/pull/99)
- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-27 - Authenticate the dsh-TUI artifact and hold 0.9.3 promotion

**Result**

- The proposed Agent Console TUI boundary advances from
  `@deepseek-harness-tui/dsh-tui@0.9.2` to `0.9.3` without changing the pinned
  DSH release or the ordered three-bundle composition, but the promotion
  remains held in draft pending a corrected authenticated upstream release.
- The machine-readable contract now binds the upstream annotated tag object,
  source revision, npm tarball URL, SRI, and shasum in addition to the exact
  package name and version.
- The packed compatibility smoke fetches only the contract tarball, enforces
  both declared digests before exposing its bounded bytes, and installs that
  verified local archive instead of an unauthenticated registry specifier.

**Why / context**

- The 0.9.3 release carries the upstream resume-argument forwarding,
  suggestion-viewport re-anchoring, macOS Terminal input repair, settings
  auto-save, and installed-manifest version display needed by the Agent Console
  promotion. Host icon selection remains an infrastructure-owned acceptance.
- A version label alone would not prove which published bytes or source commit
  the compatibility path consumed, so promotion records both provider and npm
  identities.
- The reviewed 0.9.3 source retries a contended history-file lock with
  synchronous waits before prompt submission. A direct upstream repair now
  yields between retries and makes history persistence best-effort so input
  dispatch cannot be frozen by another process, but runtime-kit cannot promote
  it until that repair is published in a new authenticated release.

**Evidence**

- The `v0.9.3` annotated tag object
  `8c4e68461ef688528dbb22da566515044d2cf0ef` resolves to source revision
  `a3439a3c7d7e7b3c9cfc505e833525376e8558d0`; npm reports the same revision as
  the package `gitHead`.
- The downloaded tarball matches npm shasum
  `6a2925a4a3bfee39db897efe0ed6c04cab4d3931` and the recorded SHA-512 SRI.
- The owner contract test first failed against the old 0.9.2 manifest, then
  passed after the exact 0.9.3 provenance was promoted. The first public smoke
  exposed a separate stale 0.9.2 workflow input on both pinned rc.2 rows. The
  aligned follow-up run passed seven of eight jobs; the remaining rc.8 job hit
  an unrelated timeout flake, and that evidence is retained without merging.
- The artifact-enforcement regression then failed 2/11 because no digest owner
  existed and the smoke still installed the registry specifier. The focused
  suite passes 14/14 after repair, the real registry fetch returns exactly
  1,694,414 bytes matching both declared digests, strict typechecking succeeds,
  and the complete package suite passes 533/533.
- Upstream contention evidence reproduced a 3.6-second prompt-path stall. The
  focused history suite, full build verification, and package verification pass
  on signed repair commit `4d82713337701ac576a7353759cf7a10704580f9`.

**Links**

- [dsh-TUI v0.9.3](https://github.com/ccch1mneyyy/dsh-TUI/releases/tag/v0.9.3)
- [Upstream history-lock follow-up #588](https://github.com/ccch1mneyyy/dsh-TUI/issues/588)
- [Signed upstream repair commit](https://github.com/xsin4880/dsh-TUI/commit/4d82713337701ac576a7353759cf7a10704580f9)
- [Draft runtime-kit PR #99](https://github.com/sympoies/dsh-runtime-kit/pull/99)
- [Pinned rc.2 contract RED run](https://github.com/sympoies/dsh-runtime-kit/actions/runs/33025099526)
- [Aligned workflow run with retained rc.8 flake](https://github.com/sympoies/dsh-runtime-kit/actions/runs/33026649816)
- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-27 - Protect persistent integration branches during delivery

**Result**

- Worktree triage now accepts repeatable exact protected branch names, reports
  them in its structured envelope, and retains matching clean worktrees with a
  dedicated `protected` disposition.
- The DSH delivery skills and policy adopt released `git-cli sync-branch` for
  persistent non-default branches and bind PR delivery to the exact requested
  base through `forge-cli`.
- The nils-cli compatibility manifest records both released contracts against
  the existing authenticated v1.27.17 artifact set.

**Why / context**

- A fully merged persistent integration branch previously looked mechanically
  safe to prune even though it remained an intentional delivery target.
- Same-head PR discovery also needed to distinguish an exact requested base
  from a provider-default or otherwise unrelated target branch.

**Evidence**

- The executable scanner regression began at 0/1 because the previous CLI
  rejected every `--protect-branch` argument before classification.
- The focused scanner and skill contract suite passed 10/10, strict
  typechecking passed, and the complete repository suite passed 604/604.
- The scanner remains read-only; protection changes neither the selected base
  nor dirty, locked, primary, or foreign-owner handling.

**Links**

- [Protected integration branch issue #101](https://github.com/sympoies/dsh-runtime-kit/issues/101)
- [nils-cli exact-base and sync-branch PR #1533](https://github.com/sympoies/nils-cli/pull/1533)
- [Reference consumer PR #57](https://github.com/sympoies/agent-runtime-kit/pull/57)

## 2026-08-27 - Add fail-closed dirty-workspace bootstrap recovery

**Result**

- A fresh top-level DSH session whose checkout is already dirty now enters an
  explicit quarantine state instead of losing every bootstrap capability.
- Quarantine admits only exact registered definitions for bounded workspace
  metadata, authenticated nils inspection, and verification of a clean managed
  worktree handoff. It never transfers lease authority from the dirty checkout.
- Ordinary file, shell, Git, provider, and deployment mutations remain blocked
  until the selected clean workspace obtains a normal lease.
- The validated nils-cli floor advances to v1.27.17, which owns the typed
  inspection and handoff protocol used by this recovery path.

**Why / context**

- Dirty checkout takeover must stay fail-closed, but applying the bind denial to
  every tool left a fresh DSH session unable to inspect safe workspace facts or
  select a governed clean worktree.
- A dedicated quarantine surface breaks that bootstrap loop without weakening
  foreign-owner, dirty-worktree, signing, worktree, or delivery protections.

**Evidence**

- The live regression first failed before skill, runtime-context, or read-only
  Git dispatch and performed no repository or provider mutation.
- Focused recovery, lease, profile, compatibility, and release-floor suites
  pass, followed by the complete repository suite at 601/601 and strict
  typechecking.
- Packed native and keyless smoke passed through independently authenticated
  v1.27.17 binaries and DSH's real tool pipeline.
- The authenticated DSH patch applied and reversed in an isolated managed
  checkout; a clean pristine host rebuild and exact compatibility inspection
  then passed.

**Links**

- [Dirty bootstrap recovery issue #102](https://github.com/sympoies/dsh-runtime-kit/issues/102)
- [nils-cli recovery provider PR #1535](https://github.com/sympoies/nils-cli/pull/1535)
- [nils-cli v1.27.17](https://github.com/sympoies/nils-cli/releases/tag/v1.27.17)

## 2026-08-27 - Promote authoritative acceptance to released nils artifacts

**Result**

- The runtime compatibility floor and validated nils-cli release advance to
  v1.27.15, the first stable artifact set containing the durable finish-line
  acceptance provider.
- The completed reviewed-source candidate record is removed. Normal packed
  smoke and runtime health now authenticate the released identity unless a
  future cross-repository rehearsal explicitly supplies a new candidate.
- Linux CI, macOS runtime health, package benchmarks, and real DSH smoke all
  consume the new exact archive identities.

**Why / context**

- Runtime-kit first integrated the acceptance provider against the exact
  reviewed nils-cli PR head so DSH candidate testing could precede the
  cross-repository merge. Keeping that source-only identity active after a
  stable release would allow a rehearsal path to outlive the gate it served.
- Raising the floor is required because authoritative goal and turn completion
  depend on the register, admit, observe, and verdict contracts added by this
  release; v1.27.14 does not provide them.

**Evidence**

- The signed v1.27.15 tag resolves to commit
  037aea988e44b85008019504cec6e71b626dbedb.
- The official Linux and macOS ARM64 archives and all consumed binaries were
  independently rehashed before their SHA-256 identities were recorded.
- Test-first compatibility assertions failed against the v1.27.14 manifest.
  The focused release, operations, health, and acceptance suites then passed
  137/137; the complete repository suite passed 589/589.
- Strict typechecking, the 101-rule policy-source parity check, and pristine
  pinned DSH compatibility inspection all passed.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Runtime integration PR #100](https://github.com/sympoies/dsh-runtime-kit/pull/100)
- [nils-cli acceptance provider PR #1507](https://github.com/sympoies/nils-cli/pull/1507)
- [nils-cli v1.27.15](https://github.com/sympoies/nils-cli/releases/tag/v1.27.15)

## 2026-08-27 - Stabilize the default Bash cwd across policy lifecycle

**Result**

- Bash calls that omit the optional `workdir` now project the immutable
  session cwd into both pre- and post-execution policy ingress.
- The projection leaves the actual tool arguments unchanged and preserves
  every explicit `workdir`, including values that downstream policy must
  reject. Oversized argument objects are rejected before any projection copy.

**Why / context**

- A live external DSH call using the ordinary `pwd` command without an
  explicit workdir stopped before command dispatch as
  `policy-output-invalid`, while an explicit model-generated path typo was
  correctly rejected by the project gate.
- Using the already-authenticated session cwd gives prerequisite, policy, and
  completion one stable workspace identity without widening execution
  authority or introducing a path allowlist.

**Evidence**

- The primary regression first failed because neither policy ingress carried
  the default workdir. A review regression then proved the earlier projection
  enumerated an oversized Bash argument object before the existing input
  bound; the bounded implementation rejects it without starting policy
  dispatch.
- Focused default-workdir and high-cardinality tests pass. The complete plugin
  contract suite passes 83/83, the package suite passes 527/527, and strict
  typechecking succeeds.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-27 - Establish managed DSH work context without replacing user claims

**Result**

- Managed DSH authentication now establishes the project development work
  context atomically only when the session has no claim. An existing active,
  conflict-free claim is preserved instead of being overwritten.
- Startup applies one bounded authentication deadline across readiness, helper
  resolution, and claim establishment. Agent and plugin disposal cancel every
  in-flight stage.
- The validated nils-cli release advances to v1.27.14, which supplies the
  required `agent-session work-context set --if-absent` contract.

**Why / context**

- A fresh external DSH session could load project policy and use ordinary
  read-only host tools, but governed mutations and finish-line were blocked as
  `owner-unclaimed` because managed startup had not established a work-context
  claim.
- Establishing only a missing baseline avoids racing another owner and avoids
  replacing a richer claim selected by the user or an already-running agent.

**Evidence**

- The regression first failed with only readiness invoked and an invalid
  baseline operation entered. The final focused authentication suite passes 13
  scenarios covering claim validation, preserved richer claims, shared
  deadlines, helper-resolution stalls, and disposal cancellation.
- The complete package suite passes 526/526 with strict typechecking. The exact
  v1.27.14 source, Linux and macOS archives, and consumed binaries are pinned by
  published SHA-256 identities.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)
- [Atomic work-context PR #1528](https://github.com/sympoies/nils-cli/pull/1528)
- [nils-cli v1.27.14](https://github.com/sympoies/nils-cli/releases/tag/v1.27.14)

## 2026-08-27 - Normalize non-widening native sandbox echoes

**Result**

- The revision-authenticated DSH patch now normalizes an explicitly blank
  justification only when the accompanying native filesystem or Bash sandbox
  mode is already effective, or is the schema-floor `workspace-write` echo
  under an effective `danger-full-access` session.
- Missing pairs, nonblank requests, lower standing modes, and genuinely wider
  requests still follow DSH's native fail-closed validation and approval path.

**Why / context**

- The native filesystem and Bash tool schemas expose optional escalation
  fields even when the session already has full access. A live DSH model could
  therefore materialize the schema floor together with a blank justification,
  causing DSH to reject an ordinary call before filesystem or process
  dispatch despite no authority increase being requested.
- The mode classifier is confined to DSH's shared sandbox helper and is reused
  by runtime-kit's finish-line adapter. Their wrappers intentionally preserve
  the distinct native-request and revalidation pairing contracts. The repair
  adds no command, path, or environment allowlist and cannot widen authority.

**Evidence**

- Focused regressions first failed 0/2 before either mutation boundary, then
  passed 2/2 after the normalization. The complete affected upstream sandbox,
  filesystem, and Bash suites pass 174/174.
- A review-follow-up regression then failed because runtime-kit still owned a
  duplicate mode matrix; GREEN delegates classification to authenticated DSH
  while retaining explicit wrapper tests for missing and blank justification.
- The runtime-kit package passes strict typechecking and 517/517 tests. Patch
  check, apply, and reverse authenticate on all three supported DSH revisions.
- A patched host build succeeds; after reverse, the pristine host rebuild and
  pinned compatibility inspection also succeed with a clean upstream checkout.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-26 - Promote nils-cli v1.27.13 for locale-independent session liveness

**Result**

- The exact validated nils-cli release advances from v1.27.12 to v1.27.13
  while retaining v1.27.8 as the supported minimum.
- Linux x86-64 and macOS ARM64 archives, plus every Linux governance binary
  consumed by DSH delivery, remain bound to published SHA-256 identities.

**Why / context**

- Agent Console sessions launched without locale variables could receive tmux
  rows whose field separator was normalized by the C locale. The session
  projection then omitted live panes and reported Codex, Claude, and DSH
  sessions as stopped even though their processes were still running.
- v1.27.13 makes the tmux liveness projection locale-independent. The DSH
  compatibility promotion validates that correction without changing command
  authority, workspace leases, or the fail-closed version ceiling.

**Evidence**

- The release tag peels to source commit
  `3dee491576cbea2801dd066a911d76309d8836f2`; downloaded Linux and macOS
  archives and independently computed archive and consumed-binary hashes agree.
- The owner release-contract test first rejected the checked-in v1.27.12 pin,
  then the complete package suite passed 517/517 with strict typechecking.
- A patched real DSH rc.2 smoke passed all nine acceptance scenarios with the
  v1.27.13 binaries, after which the patch was reversed and the pristine host
  rebuilt. A C-locale projection also reported every existing session live.
- The in-process policy benchmark passed at 0.34 ms p95 with no retained
  growth, active resources, live handles, or teardown leakage.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)
- [Locale-independent session liveness PR #1523](https://github.com/sympoies/nils-cli/pull/1523)
- [nils-cli v1.27.13](https://github.com/sympoies/nils-cli/releases/tag/v1.27.13)

## 2026-08-26 - Promote nils-cli v1.27.12 for exact Git inspection

**Result**

- The exact validated nils-cli release advances from v1.27.11 to v1.27.12
  while retaining v1.27.8 as the supported minimum.
- Linux x86-64 and macOS ARM64 archives, plus every Linux governance binary
  consumed by DSH delivery, remain bound to published SHA-256 identities.

**Why / context**

- A fresh external DSH acceptance reached the real host CLI but the policy
  classified the two-token `git --version` inspection as an unknown Git
  delivery operation and blocked it before dispatch.
- v1.27.12 admits only that exact read-only inspection. Extra Git arguments
  remain governed, and the compatibility promotion does not weaken workspace,
  delivery, or finish-line enforcement.

**Evidence**

- The release tag peels to source commit
  `7662a44aae2ffc362fe41e7606a1106e21e70cdf`; downloaded release archives and
  independently computed archive and binary hashes agree.
- The release contract first failed with v1.27.11 still checked in, then the
  complete package suite passed 517/517 with strict typechecking.
- The in-process policy benchmark passed with a 0.48 ms p95 and no active
  resource or handle leakage after teardown.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)
- [Exact Git version inspection PR #1515](https://github.com/sympoies/nils-cli/pull/1515)
- [nils-cli v1.27.12](https://github.com/sympoies/nils-cli/releases/tag/v1.27.12)

## 2026-08-26 - Bind DSH completion to authoritative acceptance

**Result**

- Runtime-kit now provides an optional `dshAcceptance` Cordis service that
  registers exact DSH tool definitions, delegates durable generation and
  evidence policy to nils-cli, and gates both turn stop and synchronous goal
  completion on one detached provider verdict.
- The authenticated downstream DSH patch is consolidated as
  `native-execution-boundaries-v2`. Its only new source seam is the optional
  pre-mutation `GoalService.complete()` assertion; no-provider behavior remains
  unchanged. The patch SHA-256 is
  `5949f3cfafd5a8b928ef49671d5788a973a54425f1e5c25a564cb2a0bae509b1`.

**Why / context**

- The packed Gate 0 reproduction proved that direct goal completion could
  bypass the existing turn-stop finish line. Public DSH lifecycle, tool,
  session-projection, service, bundle, and profile interfaces covered the rest
  of the integration; goal completion required one synchronous host boundary.
- Nils owns canonical contracts, repository generations, exact validation
  evidence, recovery, and verdict precedence. Runtime-kit owns only DSH object
  identity, lifecycle correlation, capability custody, sanitized projection,
  and composition, avoiding a second JavaScript policy engine.
- A real packed smoke exposed a registration adapter bug where requirement names
  were sent as tool names. The repaired adapter binds the actual visible DSH
  definition name at registration and admission. The final audit also made
  replacement and in-place definition drift, malformed verdict sets, ambiguous
  projections, cancellation, and disposal explicitly fail closed.
- Specialist follow-up found that locale-sensitive identifier ordering, an
  unbounded default operation ID, and replacement-session disposal waits could
  still make legal contracts unusable or hide live work from cancellation and
  resource accounting. The repaired coordinator uses nils-compatible lexical
  ordering, fixed-length hashed operation identities, and caller/lifecycle-
  cancellable workspace readiness that is counted and joined during disposal.
- The cancellation canary initially treated the PID reported inside bwrap's
  private PID namespace as host-visible. A pinned rc.2 run failed safely with
  `EPERM`, revealing that this could collide with an unrelated host PID. The
  repaired proof resolves the one exact host-visible child from `/proc`, then
  independently requires both process death and a stopped heartbeat. Host
  process enumeration itself now fails closed instead of falling back to the
  namespace PID.
- A final rc.7 clean-profile rehearsal failed before session startup because
  the canary's inner npm allowlist omitted its new PID helper. The packaging
  contract now names that helper explicitly and tests the actual inner tarball;
  the failed patched checkout was reversed, rebuilt pristine, and smoke-tested
  unpatched before the three-version lifecycle restarted.

**Evidence**

- The acceptance, compatibility, authenticated-patch, replacement-readiness,
  and canary-packaging suites pass; the complete package suite, strict
  typechecking, and policy parity all pass, with all 101 policy rules verified
  against their exact source identity.
- Candidate hardening runs pass on pristine DSH 0.1.0-rc.7, 0.1.0-rc.8, and
  0.1.1-rc.2 with exact reviewed nils-cli candidate binaries. Every checkout
  completes authenticated apply, patched build, the ten-leg real-tools matrix,
  reverse, pristine rebuild, unpatched startup, and clean source attestation.
- The smoke proves pre-mutation goal denial, exact contained Bash and native
  validator evidence, an all-satisfied provider verdict, allowed completion,
  concurrent single-body execution, contained cancellation with no late
  success, restart and crash recovery, upgrade and rollback, zero residual
  acceptance operations, and unchanged no-provider behavior.

**Links**

- [Authoritative acceptance issue #59](https://github.com/sympoies/dsh-runtime-kit/issues/59)
- [Native convergence tracker #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)
- [nils-cli acceptance provider PR #1507](https://github.com/sympoies/nils-cli/pull/1507)

## 2026-08-26 - Promote nils-cli v1.27.11 for graceful DSH shutdown

**Result**

- The exact validated nils-cli release advances from v1.27.10 to v1.27.11
  while retaining v1.27.8 as the supported minimum.
- Linux x86-64 and macOS ARM64 release archives, plus every Linux governance
  binary consumed by DSH delivery, are bound to their published SHA-256
  identities. Runtime admission therefore remains exact-release and
  fail-closed rather than accepting a version string alone.

**Why / context**

- v1.27.11 carries the Agent Console profile contract for bounded graceful DSH
  shutdown. That shutdown lets the provider cancel and terminalize an active
  workspace operation before session deletion, preventing a known mutation
  from being stranded as an uncertain orphan.
- A deployment rehearsal correctly stopped with
  `DSH_RUNTIME_HEALTH_COMPANION_IDENTITY_INVALID` after the deployed companion
  binaries advanced to v1.27.11 while the runtime compatibility identity still
  named v1.27.10. The repair promotes the authenticated identity instead of
  weakening the health gate.

**Evidence**

- The release tag peels to source commit
  `fcec303ec3af4b7b4abd33557d201870bfdaea92`; downloaded sidecars and
  independently computed Linux and Darwin archive and binary hashes agree.
- Compatibility expectations failed before the manifest and workflow update,
  then the complete package suite passed 515/515 with strict typechecking.
- The in-process policy benchmark passed with a 0.71 ms p95 and no active
  resource or handle leakage after teardown.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)
- [Graceful Agent Console session deletion PR #1508](https://github.com/sympoies/nils-cli/pull/1508)
- [nils-cli v1.27.11](https://github.com/sympoies/nils-cli/releases/tag/v1.27.11)

## 2026-08-26 - Drain workspace lease completion before provider release

**Result**

- Workspace lease disposal now cancels the exact tool authority while keeping
  its terminal-completion channel live until the provider records the
  `cancelled` outcome. Durable binding release begins only after that drain.
- An expired operation left by a force-terminated Agent Console DSH session was
  reconciled through the existing exact-fence completion protocol as
  `cancelled`; it was not inferred to have succeeded.

**Why / context**

- Provider disposal previously aborted both the tool and its own completion
  request. That made a graceful cancellation unable to record its terminal
  outcome before release and correctly left later sessions fail-closed.
- The incident transcript retained one execution-bound call with no terminal
  tool result, while the managed-session broker durably proved that the owning
  runtime stopped and its capability was revoked. Those facts supported a
  conservative cancelled completion without weakening dirty-worktree or
  foreign-owner checks.

**Evidence**

- The focused disposal regression failed before the repair because the
  completion signal was already aborted, then passed with release still
  blocked until completion settled.
- The workspace lease suite passes 34/34, strict typechecking passes, and the
  complete package suite passes 515/515.
- The exact-fence completion result was `completed`; the durable workspace
  state then contained zero active operations and one completion digest for
  the reconciled record.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-26 - Validate nils-cli v1.27.10 in contained DSH delivery

**Result**

- The compatibility contract now validates nils-cli v1.27.10 while retaining
  v1.27.8 as the minimum supported release.
- The exact Linux x86-64 and macOS ARM64 release archives and the consumed
  governance binaries are bound by their published SHA-256 identities.

**Why / context**

- Linux `PrivateUsers=yes` containment renders an unmapped host-owned system
  Git executable with the kernel overflow UID. nils-cli v1.27.9 therefore
  rejected the otherwise immutable canonical system Git path before delivery
  could begin.
- v1.27.10 accepts that owner evidence only for canonical `/usr/bin/git` or
  `/bin/git`, with a valid UID map proving the overflow identity is genuinely
  unmapped. Other Git fallback paths retain the ordinary root/effective-owner
  trust rule.

**Evidence**

- The released Homebrew `agent-scope-lock 1.27.10` creates a scope lock inside
  a real `systemd-run --user` scope with `PrivateUsers=yes` and exits 0.
- GitHub release digests, downloaded sidecars, and independently computed
  archive hashes agree for Linux x86-64 and macOS ARM64.
- Compatibility contract tests pass 11/11. The two affected operations health
  tests pass after advancing both the accepted latest and rejected next-version
  fixtures.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)
- [System Git containment repair PR #1502](https://github.com/sympoies/nils-cli/pull/1502)
- [nils-cli v1.27.10 release PR #1504](https://github.com/sympoies/nils-cli/pull/1504)
- [nils-cli v1.27.10](https://github.com/sympoies/nils-cli/releases/tag/v1.27.10)

## 2026-08-25 - Rebase runtime health over the prerequisite mainline

**Result**

- The Issue 58 runtime-health candidate now has the current mainline as its
  direct ancestor while preserving the dsh-tui 0.9.2 composition and the
  post-execute finalization of pre-dispatch prerequisite failures.
- The combined version-scoped DSH patch remains one authenticated downstream
  artifact. Its SHA-256 is
  `8cf87b22f3adbd5fb24211e941223e69f29b961cc51a9c6b4f882c8bcd0f1141`,
  with release-specific before and after images for rc.7, rc.8, and rc.2.

**Why / context**

- The first disposable canary passed against an earlier base, but main then
  advanced through the TUI composition and prerequisite-finalization changes.
  That made the prospective merge tree materially different from the tree
  attested by the old exact-head CI and canary.
- Integration therefore stopped before merge, replayed the signed candidate
  over the new base, and recomputed the patch identity instead of treating the
  earlier acceptance as transferable evidence.

**Evidence**

- Runtime-health and compatibility-focused regressions pass 202/202, strict
  typechecking passes, and the complete package suite passes 515/515.
- Check/apply/check/reverse/check completes on pristine rc.7, rc.8, and rc.2
  checkouts, leaving each source checkout clean.
- Exact-head compatibility run 32853879965 passes all eight package, pinned,
  upstream-next, and macOS rows for the integrated candidate. The blocking
  reverse, pristine rebuild, unpatched startup, and closure comparison gates
  remain enabled.
- Independent maintainability, security, and testing review found no material
  issue in the integrated tree.

**Links**

- [Runtime health issue #58](https://github.com/sympoies/dsh-runtime-kit/issues/58)
- [Native convergence tracker #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)
- [Runtime health PR #84](https://github.com/sympoies/dsh-runtime-kit/pull/84)
- [Exact-head compatibility run 32853879965](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32853879965)
- [Managed authoring target issue #21](https://github.com/sympoies/agent-runtime-kit/issues/21)

## 2026-08-25 - Close pre-dispatch prerequisite failures through post-execute

**Result**

- The revision-authenticated DSH execution-prerequisite patch now routes a
  failed prerequisite verification through the ordinary post-execute failure
  boundary before publishing the final tool result.
- The tool body and prerequisite cache commit remain skipped, while policy,
  workspace-operation, and other post-execute owners receive one exact
  terminal failure event.

**Why / context**

- A fresh Agent Console DSH workload produced a durable failed tool result for
  `prerequisite-binding-invalid`, but DSH classified that pre-dispatch failure
  as a final scheduler result and skipped `tools/post-execute` entirely.
- The workspace lease had already admitted the mutation during ordered policy
  evaluation. Without the terminal event, its durable operation remained
  active and later fresh sessions correctly failed closed as uncertain.

**Evidence**

- A focused scheduler regression failed 0/1 before the repair because the
  post-execute observer saw zero failures, then passed 1/1 after the repair.
- The complete upstream tools test file passes 162/162 with the new terminal
  lifecycle assertion.
- The runtime-kit patch lifecycle suite passes 13/13, and an exact rc.2
  checkout successfully checks, reverses, and reapplies the updated artifact.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-25 - Validate the dsh-tui 0.9.2 Agent Console boundary

**Result**

- The exact Agent Console composition now pins
  `@deepseek-harness-tui/dsh-tui@0.9.2` beside DSH `0.1.1-rc.2`.
- A machine-readable profile workspace contract preserves DSH's hoisted linker
  and disabled peer auto-installation while explicitly disabling the four
  unneeded transitive lifecycle builds declared by the TUI release.
- The public compatibility matrix now runs the exact TUI composition on both
  pinned Node 22 and Node 24 rows, including a bounded enabled-TUI PTY startup.

**Why / context**

- A version-only bump failed under pnpm 11 because the TUI dependency chain
  requires explicit lifecycle decisions. Pre-creating only that block also
  discarded DSH's native profile settings and allowed the peer graph to drift.
- The earlier packed smoke intentionally disabled the interactive row while it
  drove tools, so installation and bundle inspection alone did not prove that
  the enabled TUI could import and stay live.

**Evidence**

- The compatibility regression failed against the prior `0.9.0` contract and
  passes 8/8 after the update.
- The real packed smoke installs `0.9.2`, verifies the profile workspace and
  three-bundle composition, keeps the enabled TUI live in a bounded PTY, and
  then passes runtime context, policy, native prerequisite, validation, review,
  resume, subagent, and finish-line scenarios.
- Clean public runners also proved the TUI's provider catalog is distinct from
  active route authority: catalog presence does not register Claude tools or a
  coauthor surface, and the selected controller and worker remain codex-proxy.
- Strict typechecking, the 465-test package suite, and diff checking pass.
  Focused review findings about contract ownership, enabled TUI startup, and
  Node 24 coverage were repaired without broadening agent host authority.

**Links**

- [dsh-TUI v0.9.2](https://github.com/ccch1mneyyy/dsh-TUI/releases/tag/v0.9.2)
- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-25 - Carry declared validation through native prerequisites

**Result**

- Native project-development prerequisite verification now recognizes the
  authenticated declared-validation reservation for the same exact execution.
  It preserves prerequisite receipt, context, and commit handling without
  re-running generic opaque-shell policy against a command already classified
  by the authoritative finish-line probe.
- Missing, stale, ordinary, rejected, or identity-mismatched reservations still
  use the generic policy path or fail closed. This adds no executable allowlist
  and does not broaden DSH host authority.

**Why / context**

- A fresh managed Agent Console DSH acceptance session passed runtime-context
  preparation and ordinary host CLI discovery, but its exact declared
  repository validation failed before dispatch with
  `prerequisite-binding-invalid`. The unused validation reservation then
  correctly poisoned the session ledger, so retries returned
  `finish-line-unavailable`.
- The existing declared-validation regression did not register a visible Bash
  definition and therefore did not traverse DSH's native prerequisite seam.

**Evidence**

- Registering the native Bash definition reproduced the defect before the
  repair; the focused declared-validation test failed 0/1.
- Declared and ordinary Bash regressions pass 2/2 after the repair. They prove
  prerequisite verification and commit remain active for declared validation,
  while an ordinary script remains subject to generic policy.
- The combined plugin and finish-line suite passes 126/126, the full package
  suite passes 464/464, strict typechecking passes, and focused independent
  review found no material issue.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-25 - Classify declared Bash validation before opaque-shell policy

**Result**

- Bash calls now receive a no-execution, correlation-bound finish-line probe
  before generic opaque-shell policy evaluation. Only a typed `ready` response
  bypasses that generic check; `ordinary-ready` commands remain subject to the
  existing policy transport without a command or executable allowlist.
- A prepared probe is rejected on every later denial, exception, cancellation,
  identity mismatch, or disposal path. Teardown retires its authenticated
  runner capability after closing admission and before clearing the ledger.

**Why / context**

- A live managed DSH workload reached its exact repository validation, but the
  generic policy had to judge an opaque Bash script before nils could identify
  it as the declared finish-line command. The script was therefore blocked
  even though the authoritative validation contract would have admitted it.
- Root ownership reported from a nested containment probe was expected user
  namespace remapping, not a host-binary trust defect. The repair consequently
  preserves systemd trust validation, full-host authority, and ordinary policy
  enforcement instead of weakening containment or adding a shell allowlist.

**Evidence**

- The focused regression failed 0/2 before the repair and passed 2/2 after it.
- Correlation mismatch, execution substitution, downstream denial and failure,
  caller cancellation, and pending-policy disposal regressions all pass. The
  combined finish-line and plugin-contract suite passes 102/102.
- The full package suite passes 464/464, strict typechecking passes, and
  `git diff --check` reports no errors.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-25 - Preserve the normal host CLI path for managed DSH turns

**Result**

- Managed nils and finish-line children no longer replace DSH's
  credential-scrubbed parent `PATH` with a fixed system or toolchain-only path.
- The exact `agent-hook` executable remains resolved before dispatch, while the
  existing credential scrub, managed-session isolation, canonical Linux user
  bus, sandbox, policy, and finish-line boundaries remain unchanged.

**Why / context**

- A live Agent Console workload could load project context and run a narrow Git
  probe, but ordinary host CLIs installed outside the system path were missing.
  The failed finish-line validation then correctly poisoned the disposable
  session, making every later foreground shell call fail closed.
- The fixed path recorded earlier in this log protected executable resolution,
  but it also acted as an unintended CLI allowlist. DSH already owns a normal,
  credential-scrubbed launch environment, so runtime-kit only needs to resolve
  its own helper exactly and project authenticated session fields.

**Evidence**

- The focused regression reported 6 failures before the repair and passed
  27/27 afterward. The extended policy, authentication, finish-line, and
  environment matrix passed 63/63.
- The full package suite passed 457/457, strict typechecking passed, and all 101
  projected policy rules matched the current public policy source.
- A clean build of the pinned DSH 0.1.1-rc.2 revision passed the machine-readable
  compatibility check with an unchanged upstream checkout.

**Links**

- [Fleet integration issue #213](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-25 - Replace Darwin directory projection with transient verified execution

**Result**

- Authenticated companion snapshots now retain read-only executable file
  handles plus private mode-`0500` self-resolution links beneath a mode-`0700`
  random root until every child scope settles. Runtime commands and all
  long-lived nils transports execute only through the retained file
  descriptions; inode-bound disposal preserves foreign replacements and warns
  about a relocated private root rather than deleting uncertain names.
- The version-scoped DSH patch now adds a fail-closed
  `SubprocessRuntime.spawnDescriptor` primitive. The local provider maps the
  caller-owned executable to child fd 3 and launches `/proc/self/fd/3` on
  Linux. On Darwin it copies exact descriptor bytes into a private mode-`0500`
  file beneath a fresh mode-`0700` directory, verifies source stability and
  target SHA-256, requires every temp-parent ancestor to be owned by root or
  the current UID, rejects an untrusted writable chain, revalidates both
  private identities immediately before spawn, and retains that path until
  synchronous spawn failure or complete process-tree settlement so descendant
  executable self-inspection remains valid. The requested `argv[0]` remains
  display identity only.
- Linux and macOS compatibility lanes run the native subprocess regression in
  addition to the tools and LLM boundary suites. Providers without descriptor
  execution cannot silently fall back to an ordinary path.

**Why / context**

- The preceding repair assumed Darwin could reopen a directory through
  `/dev/fd/<fd>` and traverse a child entry. The macOS provider lane disproved
  that assumption with `ENOTDIR`; Darwin's file-descriptor namespace is not a
  directory-projection equivalent of Linux procfs.
- DSH's public subprocess spec exposes only argv, cwd, environment, and the
  three standard streams. It cannot inherit an authenticated executable FD,
  so this missing native boundary belongs in the same hash-authenticated
  downstream patch as the tool and model admission seams.
- The first descriptor-execution provider run reached patched DSH typechecking
  and exposed that the local provider method also needed TypeScript's explicit
  `override` modifier. All affected Linux release rows reported the same
  `noImplicitOverride` failure, making this a patch contract defect rather than
  a platform execution failure.
- The replacement macOS run then reached the runtime-provider suite. Eleven of
  thirteen tests passed; the two project-audit fixtures returned `/var` paths
  after production had correctly canonicalized them to `/private/var`. The
  fixture now canonicalizes its temporary root instead of weakening strict
  production path matching.
- The following macOS run passed the provider suite and reached all three DSH
  native suites. Direct `spawn("/dev/fd/3")` then failed with `EACCES`, proving
  that Darwin fdesc entries are readable projections but not executable
  pathnames. A subsequent attempt to start `/usr/lib/dyld` as an executable
  bridge failed with `ENOEXEC`; dyld is not a supported descriptor-exec shim.
  The provider therefore keeps the descriptor as byte authority through
  per-spawn verified materialization. macOS still has no atomic fd-to-exec
  transition, so a malicious same-UID race remains an explicit platform risk.
- A packed-runtime RED proved that eager unlink breaks nils-cli self-inspection:
  `current_exe()` could not canonicalize the deleted executable and the
  finish-line runner exited 69. Private links are therefore retained as
  non-authoritative self-resolution aids. Replacing or relocating their root
  still cannot redirect Linux execution, and inode-bound disposal does not
  remove a foreign replacement.
- A later packed-runtime RED proved that Cordis intentionally returns a fresh
  trace wrapper for a service method on each access. Binding wrapper identity
  therefore rejected the unchanged real subprocess provider. Runtime-kit now
  authenticates Cordis' public original service identity, declared mode, and
  raw method while still invoking through the traced receiver; an in-place
  provider method replacement remains fail-closed.
- The first exact-head required-check run exposed two portability defects that
  local Linux had not represented. The CI Node executable exceeded the patch's
  original 64 MiB descriptor ceiling, and macOS canonicalized `/var` fixture
  paths to `/private/var` while a global one-shot unlink failure could be
  consumed by unrelated cleanup. The bounded executable ceiling is now 256
  MiB, the ownership fixture uses its canonical path, and cleanup injection is
  bound to the exact materialized executable. A separate Node 22 failure showed
  that a policy-timeout test released its child after a wall-clock sleep; it
  now waits for the actual abort event before releasing the process tree.
- The repaired macOS job then passed the Darwin provider and all selected DSH
  native suites before the generic packed smoke reached nils finish-line.
  Released nils-cli correctly returned `finish-line-unavailable`: its
  authoritative finish-line validation requires Linux systemd/cgroup
  containment. The macOS lane now runs a narrower packed runtime-health smoke
  instead of weakening that denial or claiming a cross-platform finish-line
  contract that nils does not provide.
- Independent acceptance review found that the first health-only harness could
  misclassify an unrelated early boot failure as companion-authentication
  success, then print an unchecked `ok: true` receipt. The negative boot now
  requires the exact typed companion-identity denial, and the receipt is
  asserted in full before publication, including parent/session/model
  inactivity and the exact patched checkout state.

**Evidence**

- The runtime-provider regressions now pass 23/23 while proving descriptor-only
  dispatch, stable Cordis wrapper handling, provider-mutation rejection,
  source replacement isolation, private-link lifetime, partial-creation
  cleanup, replacement preservation, and descriptor closure after a pre-spawn
  lease drains.
- The replacement materialization tests failed 2/80 against the dyld bridge,
  then passed with a new RED/GREEN delayed child self-resolution case while
  proving exact descriptor bytes, mode-`0500` permissions, the 256 MiB ceiling,
  nonexistent `argv[0]`, and cleanup after synchronous spawn failure or child
  process-tree settlement. The Linux descriptor path remains directly covered.
- Strict runtime-kit typechecking succeeds after the self-resolution and
  Cordis binding repairs. The rebased complete runtime-kit suite passes 514/514
  with no failures, skips, or cancellations. The policy performance gate
  passes at 0.345 ms p95, 201,584 retained bytes, zero retained growth, and no
  active resources or live handles after teardown.
- The authenticated patch manager accepts the expanded artifact and exact
  before/after target hashes on pristine rc.7, rc.8, and rc.2 checkouts. Each
  checkout completed a manager-owned pristine-to-patched-to-pristine lifecycle
  without source drift. The repaired patch is authenticated as
  `f9a06a14edf5ae9bf81665b5fca324034d73918067df23eafc4a338598a0409f`.
- Patched rc.8 passes the complete host/client typecheck and all 335 selected
  native tool, LLM, and subprocess tests. The packed Node 22.19.0 smoke using
  released nils-cli v1.27.9 artifacts passes every scenario and executes
  `runtime_kit_plus_one(41)` as 42 through the real DSH tools pipeline.
- After the exact-head CI repair, the timeout regression passed ten consecutive
  focused runs, strict runtime-kit typechecking passed, the complete package
  suite passed 514/514, package dry-run succeeded, and patched rc.8 again
  passed all 335 selected native tests before a clean manager-owned reversal.
- After reverse, rc.8 was cleaned, rebuilt from pristine sources, and started
  successfully through `pnpm dsh --help`; its checkout remained pristine. This
  process check is now a required Linux and macOS rollback gate rather than an
  inference from the source receipt.
- The platform-scoped packed smoke passed locally with authenticated released
  nils-cli v1.27.9 artifacts. It denied an unauthenticated companion before a
  model receipt, denied invalid project health before model middleware or the
  adapter, recovered the same session after repair, kept health/audit state out
  of model context, and returned 42 from `runtime_kit_plus_one(41)`. The patch
  was then reversed, the pristine rc.8 host rebuilt and started, and the DSH
  checkout verified clean.
- The review repair passed the same packed smoke while matching
  `DSH_RUNTIME_HEALTH_COMPANION_IDENTITY_INVALID` and validating every receipt
  claim before output. A second patch reverse, pristine host rebuild/start,
  manager check, and clean-checkout proof also passed.
- Released-mode candidate rehearsal `issue-58-v1279-rc2-candidate-r1` bound
  package SHA-256
  `60f552bb0124ec3bf119cce07ef9b588ebed5bbc5b600af24bc65083b17dc3c7`,
  pristine DSH rc.2, and the six released nils-cli v1.27.9 binaries. All 11
  functional scenarios passed with zero failures; only the two live-delivery
  records remained pending authorization. Fresh operations and runtime package
  extractions proved setup, update, doctor, rollback, removal, packed session
  health denial/recovery, reviewer, resume, subagent, and finish-line behavior.
  The receipt remains honestly `incomplete` because this local rehearsal is not
  the separate disposable OS-isolated promotion environment, and no primary
  profile was targeted.
- The failed macOS run is retained as the negative platform proof; pristine
  build, reverse, restored host closure, and checkout cleanup still passed in
  that job, isolating the failure to directory-FD traversal.

**Links**

- [Failed macOS provider run](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32805896377)
- [First native descriptor provider run](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32808546897)
- [Rejected dyld provider run](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32810291857)
- [Exact-head CI portability failures](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32828183161)
- [Platform-scope CI finding](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32831297400)
- [PR #84](https://github.com/sympoies/dsh-runtime-kit/pull/84)
- [Capability issue #58](https://github.com/sympoies/dsh-runtime-kit/issues/58)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

**Follow-ups**

- Require the macOS lane to prove per-spawn verified materialization, cleanup,
  packed runtime-health behavior, patch reversal, and restored closure before
  merge. Keep complete authoritative finish-line acceptance on Linux. Keep #58
  open through the clean-main canary acceptance and rollback rehearsal; do not
  start #59.

## 2026-08-25 - Harden runtime snapshot teardown and compatibility restore

**Result**

- Authenticated nils snapshot teardown now removes executable entries through
  the retained directory descriptor. It removes the directory pathname only
  while that name still resolves to the opened inode, so a replacement at the
  former path is not chmodded or recursively deleted.
- Darwin descriptor authentication now compares the original directory handle
  with a handle opened through `/dev/fd`; it does not assume the fdesc
  filesystem reports the underlying device and inode through pathname `stat`.
- Pristine and post-reverse DSH closure checks now run the upstream bounded
  clean owner before an exact host build, which is the face changed by the
  downstream patch. The patched lane still performs the separate full
  host-and-client build required by packed runtime smoke.

**Why / context**

- The execution path was already descriptor-bound, but disposal closed that
  descriptor before reopening a replaceable pathname. A same-UID rename could
  therefore make cleanup delete a foreign replacement and retain the displaced
  authenticated executable copies.
- The first provider matrix after adding host-closure authentication
  exposed two portability assumptions that Linux-only focused validation could
  not prove: Darwin fdesc metadata differs from Linux procfs metadata, and the
  retained rc.7 client face is not byte-reproducible even across clean builds.
  Comparing that unrelated face cannot establish whether the host patch was
  reversed, so pristine and restored attestations now bind the exact host face.

**Evidence**

- The security regression failed first because disposal removed the replacement
  path and left the displaced executables. It now passes while proving the
  replacement bytes survive and both authenticated copies are unlinked from
  the displaced directory.
- The focused runtime-health provider test and strict typecheck pass after the
  descriptor-relative cleanup and open-handle identity changes.
- Two clean rc.7 host builds produced the same 3,834-file, 28,122,037-byte
  closure digest. In contrast, two clean full builds changed 28 unrelated
  client bundle files while preserving their paths, sizes, modes, maps, and
  total byte count; this grounds the host-face attestation boundary.
- The failed compatibility run is retained as negative evidence: macOS rejected
  the old pathname-stat identity check, while every Linux DSH row reached the
  final closure comparison and detected stale output after reverse. The next
  provider run must pass all eight jobs before merge.

**Links**

- [PR #84 security review](https://github.com/sympoies/dsh-runtime-kit/pull/84#pullrequestreview-5014562499)
- [Failed compatibility run](https://github.com/sympoies/dsh-runtime-kit/actions/runs/32803093275)
- [Capability issue #58](https://github.com/sympoies/dsh-runtime-kit/issues/58)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

**Follow-ups**

- Re-run the complete local suite and all Linux/macOS compatibility rows on the
  repaired head. Keep #58 open through clean-main canary acceptance and the
  rollback rehearsal; do not start #59 before those records exist.

## 2026-08-25 - Move runtime health admission out of model prompts

**Result**

- The issue #58 candidate adds a native `RuntimeHealth` Cordis service with
  frozen typed snapshots, scoped probes, recovery, provider retirement, and
  model-hidden admission. Project health is refreshed by DSH's pre-waterfall
  model guard; capability-dependent tools are fenced by the native tool guard.
- Companion authentication now retains immutable executable snapshots across
  every long-lived nils transport. Scoped descriptor leases drain executable
  resolution, spawn, response validation, process-tree quiescence, cleanup,
  and HMR disposal before the snapshot can close or be reused.
- The downstream DSH patch is authenticated by release revision, patch digest,
  and before/after hashes. It extends only the native tools and LLM public
  boundaries required for prerequisite and health admission, remains owned by
  runtime-kit, and has apply, reverse, and complete build-closure checks.
- Compatibility validation targets Linux descriptor paths and a dedicated
  macOS arm64 lane with released nils-cli artifacts and `/dev/fd`. The former
  prompt health rule is retired instead of being duplicated in policy text.

**Why / context**

- Prompt reminders make the model reconstruct environment health and delivery
  evidence, can leak operational diagnostics into model context, and cannot
  guarantee denial before middleware, adapters, or model work begins. Health
  admission belongs in deterministic harness lifecycle owners.
- A descriptor path authenticates bytes only while its owner remains alive.
  Policy, context, finish-line, and workspace-lease consumers therefore need
  transport-scoped ownership rather than agent-authored path evidence or a
  lease covering only the final spawn call.
- DSH does not currently expose every required native interception point. The
  version-scoped patch is maintained out of tree in this repository; no DSH
  fork, vendored source, or upstream contribution is introduced.

**Evidence**

- The exact final runtime suite passes 496/496 with strict typechecking,
  packaging, and all 101 projected policy rules verified against their frozen
  source. Focused regressions cover descriptor replacement and reuse,
  cross-consumer HMR disposal, delayed workspace-lease resolution, child
  lifecycle disposal, and transactional admission rollback.
- Packed smoke on DSH 0.1.1-rc.2 installs into a clean profile and proves that
  an invalid project is blocked before session middleware, adapters, and model
  dispatch; health codes and audit sentinels stay outside model context; the
  same session recovers and returns the expected tool result after repair.
- Native patched tools and LLM suites pass 247/247 on DSH 0.1.0-rc.7 and rc.8,
  and 249/249 on DSH 0.1.1-rc.2. A local rc.2 reverse rebuild restores the
  authenticated complete generated `lib` closure; the provider matrix remains
  the authority for the retained rc.7/rc.8 and Darwin rows.
- Maintainability, testing, and adversarial reviewers converged with no open
  findings after lifecycle, rollback, platform, and lease-order regressions
  were strengthened.

**Links**

- [Capability issue #58](https://github.com/sympoies/dsh-runtime-kit/issues/58)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)
- [nils-cli v1.27.9](https://github.com/sympoies/nils-cli/releases/tag/v1.27.9)

**Follow-ups**

- This remains candidate evidence until its PR and all Linux/macOS
  compatibility jobs pass, followed by a clean post-merge canary deployment,
  acceptance run, and rollback rehearsal. Do not begin issue #59 before those
  gates are recorded on #58 and #66.
## 2026-08-25 - Validate nils-cli v1.27.9 without retiring v1.27.8

**Result**

- The exact v1.27.9 source, Linux release archive, and six consumed governance
  binaries are authenticated in the machine-readable compatibility manifest.
- The minimum supported release remains v1.27.8; this promotion adds the latest
  validated release without unnecessarily retiring the previous protocol floor.

**Why / context**

- Agent Console deployment needs the v1.27.9 macOS advisory-coordination repair
  together with the runtime-kit's native execution-prerequisite bridge.

## 2026-08-25 - Bind automatic prerequisites at the native DSH execution boundary

**Result**

- The issue #57 candidate adds an exact-definition prerequisite seam to the
  DSH tools pipeline and implements automatic project-development context on
  that seam. Admission begins before any execution wrapper or tool body, while
  idempotent cache completion runs only after DSH has finalized and linearized
  the successful result.
- One version-scoped downstream patch is authenticated by revision, patch
  digest, and before/after source hashes. Runtime-kit owns idempotent
  check/apply/reverse commands; it does not fork, vendor, or propose the patch
  upstream.
- The runtime compatibility floor now uses the released nils-cli v1.27.8
  binaries required by the execution-prerequisite protocol.

**Why / context**

- Public registry and event wrappers cannot prove that the definition admitted
  before execution is the exact definition dispatched after HMR or a
  `tools/execute` short circuit. They also cannot place durable prerequisite
  context after the entire native post waterfall without duplicating DSH's
  dispatcher.
- The native seam keeps caller cancellation, tamper-evident agent and call
  identity, HMR replacement checks, result finalization, and completion
  linearization inside the owner of tool execution. Runtime-kit supplies policy
  behavior and bounded idempotent cache reconciliation; agents do not
  reconstruct this evidence or retry shell commands themselves.

**Evidence**

- The patched DSH tools suite passes 161/161, including exact agent scope, HMR
  substitution, identity mutate-and-restore, successful execute short
  circuits, late cancellation, one-shot scheduler replay, prerequisite binding
  closed before approval can suspend admission, finalizer failure, and
  uncertain cache completion.
- Packed runtime smoke passes independently on DSH 0.1.0-rc.7,
  0.1.0-rc.8, and 0.1.1-rc.2 with automatic and Code Mode nested
  prerequisites. Reversing the patch returns every checkout to an authenticated
  pristine state.
- The complete runtime suite passes 455/455, the focused patch-manager suite
  passes 13/13, and strict typechecking passes. Policy-source parity verifies
  all 101 projected rules. The benchmark records 0.366 ms p95 and the
  released-agent-hook benchmark records 5.916 ms p95, both with clean teardown
  and within budget.
- Released-mode source acceptance on DSH 0.1.1-rc.2 binds the exact patch,
  package, and six nils-cli v1.27.8 artifacts. It passes all 11 local functional
  scenarios with 2 delivery steps pending authorization and no failures. The
  operations leg preserves the exact authenticated patched baseline rather than
  falsely requiring a pristine status inside the patched rehearsal clone.

**Links**

- [Capability issue #57](https://github.com/sympoies/dsh-runtime-kit/issues/57)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)
- [nils-cli v1.27.8](https://github.com/sympoies/nils-cli/releases/tag/v1.27.8)

**Follow-ups**

- This is candidate evidence, not deployment. Complete independent review,
  merge, clean post-merge canary deployment, and rollback rehearsal before
  closing #57 or promoting the next #66 item.

## 2026-08-24 - Adopt the released governed-commit policy candidate

**Result**

- The issue #55 runtime candidate now pins the stable nils-cli 1.27.7 release,
  its exact source commit, Linux archive, and all six consumed binary digests.
- Package CI downloads that same authenticated release, and the operations
  rehearsal derives its expected DSH version from the compatibility manifest
  instead of retaining an rc.7 literal after the pinned channel moved to rc.2.

**Why / context**

- The native DSH tool depends on the topology and trusted-executable policy
  delivered by nils-cli #1491. Candidate evidence built from an unreleased or
  older installed helper would not prove the completion path users receive.
- Final package validation exposed two independent stale assumptions: the CI
  workflow still fetched nils-cli 1.27.5, and the operations scenario expected
  rc.7 even though the pinned DSH channel is rc.2. Both now follow the
  machine-readable compatibility owners.

**Evidence**

- The stable v1.27.7 release tag peels to the reviewed release merge, its
  source workflow and exact-SHA CI passed, and the Homebrew-installed binaries
  match the official archive digests recorded in the compatibility manifest.
- Released-mode source acceptance passed 10 functional scenarios with zero
  failures against exact DSH 0.1.1-rc.2; live delivery remained explicitly
  pending rather than being synthesized by the candidate.
- Provider-free delivery rehearsals passed on DSH 0.1.0-rc.7, 0.1.0-rc.8,
  and 0.1.1-rc.2. Every row created one signed commit in the leased feature
  worktree, rejected stale-head and foreign-session attempts, and preserved the
  primary checkout and remote-default ref.
- The corrected operations suite passes 82/82, the focused release and
  compatibility contracts pass 29/29, the rebased Node 24 suite passes
  415/415, strict typechecking passes, and policy source parity verifies all
  101 projected rules.

**Links**

- [Tracking issue #55](https://github.com/sympoies/dsh-runtime-kit/issues/55)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)
- [nils-cli policy PR #1491](https://github.com/sympoies/nils-cli/pull/1491)
- [nils-cli v1.27.7](https://github.com/sympoies/nils-cli/releases/tag/v1.27.7)
- [Release wait follow-up #1496](https://github.com/sympoies/nils-cli/issues/1496)

**Follow-ups**

- This is candidate evidence, not deployment. Bind final validation and review
  to the rebased runtime head, then merge, deploy, run post-update acceptance,
  and roll back successfully before closing #55 or starting the next #66 item.

## 2026-08-24 - Validate nils-cli 1.27.6 for every supported DSH row

**Result**

- The nils-cli compatibility ceiling now admits the authenticated 1.27.6
  release while retaining 1.27.1 as the general minimum and 1.27.5 as the
  DSH rc.2 minimum.
- The release manifest, CI benchmark input, public operating documents, and
  doctor regression coverage share the same exact release identity.

**Why / context**

- A consumer image could install and activate the latest runtime package but
  its startup doctor rejected agent-docs 1.27.6 because the validated upper
  bound still ended at 1.27.5. The failure was a compatibility-ledger gap, not
  a DSH subprocess or process-tree failure.
- Promoting only the consumer would have left future startup and deployment
  checks fail-closed, so the owning runtime contract needed the release proof
  before downstream images could adopt 1.27.6.

**Evidence**

- The focused release-contract test failed first on the retained 1.27.5
  manifest, then passed with exact source, archive, and six binary identities.
- Doctor coverage proves 1.27.6 healthy on general and rc.2 paths, preserves
  the rc.2 lower bound, and rejects unvalidated 1.27.7.
- The complete suite passed 402/402 tests and strict typechecking. The policy
  benchmark passed 2,000 samples with 0.347 ms p95, no active or live handles,
  and retained heap below budget.

**Links**

- [nils-cli v1.27.6](https://github.com/sympoies/nils-cli/releases/tag/v1.27.6)

## 2026-08-24 - Prove native governed commits without checkout retargeting

**Result**

- The issue #55 candidate adds a structured DSH governed-commit tool whose
  target is derived only from the live session workspace. Model-authored
  repository routing, workdir overrides, message files, and raw argv are not
  part of the surface.
- Nils policy now treats the remote default, primary checkout branch, and
  linked session worktree as independent facts. It admits the exact leased
  non-default linked worktree while keeping default, primary, foreign-session,
  stale-head, retargeted, missing-evidence, and drifted cases fail-closed.

**Why / context**

- The legacy completion path could require agents to retry with `--repo` or
  probe Git state because default-branch classification was coupled to the
  primary checkout's current branch. That coupling is invalid when a maintainer
  uses an integration branch in the primary checkout.
- DSH already exposes the authenticated session workspace, tool lifecycle,
  subprocess, and lease surfaces required for this operation. Keeping the
  orchestration in runtime-kit and deterministic Git policy in nils-cli avoids
  both an agent shell workaround and a maintained DSH fork.

**Evidence**

- The runtime suite passed 411/411 tests, typechecking, policy-source parity,
  pinned and upstream-next compatibility checks, and package inspection.
- Inline specialist and red-team review added regressions proving runtime
  disposal drains active commit children, transport errors disclose no local
  paths, and the end-to-end deadline includes executable resolution without a
  late subprocess spawn.
- Nils passed all 42 DSH policy tests and warning-free clippy validation.
- Packed provider-free smokes passed on DSH 0.1.0-rc.7, 0.1.0-rc.8, and
  0.1.1-rc.2. Each created one signed feature-worktree commit, verified its
  exact parent and staged file, kept the primary/default checkout unchanged,
  rejected a stale expected HEAD without a commit, and denied a live foreign
  session before the tool body.

**Links**

- [Tracking issue #55](https://github.com/sympoies/dsh-runtime-kit/issues/55)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

**Follow-ups**

- This entry describes candidate evidence, not deployment. Release and pin the
  nils change first, then rerun the exact compatibility, update/rollback, and
  post-update smoke gates before closing #55.

## 2026-08-24 - Promote the merged workspace-lease baseline

**Result**

- The protected nils-cli and runtime-kit merges were rebuilt and redeployed as
  one matched nils-cli 1.27.5 stack, then accepted across every pinned DSH row.
- The workspace capability remains an out-of-tree runtime integration. Public
  DSH and Cordis lifecycle surfaces were sufficient, so no DSH patch, fork, or
  vendored source became part of the delivery.

**Why / context**

- The serial convergence tracker requires the merged identities themselves to
  pass deployment and rollback before the current child can close or the next
  child can begin. PR-head CI and candidate rehearsal alone are insufficient
  because another compatibility change may enter the target branch first.
- Runtime-kit main had already promoted the released nils-cli 1.27.5 boundary.
  Post-merge acceptance therefore used matching 1.27.5 hook, documentation,
  and review binaries instead of preserving the earlier temporary mixed-version
  plan.

**Evidence**

- The exact runtime merge passed 402/402 tests and strict typechecking. Packed
  full and native two-session smokes passed on clean DSH rc.7, rc.8, and rc.2
  sources; same-worktree peers were denied before mutation and linked
  worktrees overlapped.
- Setup, update, rollback, and removal passed from the frozen Task 1.1 baseline
  to the merged runtime package while unrelated bundles, user patches, private
  skills, provider homes, and DSH source identities remained unchanged.

**Links**

- [Tracking issue #56](https://github.com/sympoies/dsh-runtime-kit/issues/56)
- [Runtime delivery PR #72](https://github.com/sympoies/dsh-runtime-kit/pull/72)
- [nils-cli delivery PR #1488](https://github.com/sympoies/nils-cli/pull/1488)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

## 2026-08-24 - Accept the workspace-lease candidate before merge

**Result**

- The reviewed runtime-kit and nils-cli workspace-lease heads passed their
  refreshed CI, exact packed compatibility matrix, real two-session canary,
  and baseline-to-candidate upgrade and rollback rehearsal.
- Official DSH rc.7, rc.8, and rc.2 sources remained unmodified. Git identity,
  durable ownership, recovery, and fencing remain in nils-cli; runtime-kit
  continues to project only public DSH lifecycle facts.

**Why / context**

- The serial tracker requires one child to be deployed, exercised, and rolled
  back before merge and before the next convergence issue can begin. Candidate
  acceptance therefore had to cover both the runtime adapter and its canonical
  nils authority at immutable reviewed heads.
- The operations rehearsal also confirmed the executable trust boundary: tools
  beneath writable ancestors were rejected, while owner-only immutable
  snapshots passed. The acceptance environment was corrected without weakening
  the runtime guard.

**Evidence**

- Full packed and native two-session smokes passed on all three DSH rows. A
  same-worktree peer was denied before mutation, linked worktrees overlapped,
  and cancellation, resume, review, subagent, finish-line, and negative policy
  paths passed.
- The Task 1.1 baseline installed successfully, updated to the candidate,
  rolled back, and was removed without changing unrelated bundles, user patch
  state, private skills, provider homes, or the DSH source checkout.
- Runtime provider checks passed Node 22, Node 24, pinned DSH, and
  upstream-next. Nils provider checks passed Linux, macOS, coverage,
  cargo-deny, CodeQL, and JUnit reporting with no failure or pending job.

**Links**

- [Tracking issue #56](https://github.com/sympoies/dsh-runtime-kit/issues/56)
- [Runtime delivery PR #72](https://github.com/sympoies/dsh-runtime-kit/pull/72)
- [nils-cli delivery PR #1488](https://github.com/sympoies/nils-cli/pull/1488)
- [Serial umbrella #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

## 2026-08-24 - Keep active workspace renewal observable on Node 22

**Result**

- Workspace lease renewal deadlines remain referenced while an admission or
  acquired operation is active, so lease loss can cancel and complete pending
  tools even when no unrelated event-loop handle exists.
- Idle bindings still use unreferenced renewal timers and therefore do not keep
  DSH alive by themselves.

**Why / context**

- PR validation exposed a Node 22 lifecycle difference that newer Node versions
  did not reproduce: a tool waiting only for its authority signal could leave
  no referenced handle, allowing the process to exit before the renewal timer
  fired. This made correctness depend on incidental runtime activity.
- A separate idle-renewal test intentionally exercises an unreferenced timer;
  its harness now owns a bounded referenced deadline instead of changing the
  production idle-exit contract.

**Evidence**

- The focused Node 22 renewal-loss test first failed with a pending-promise
  cancellation, then both active-operation and pending-admission variants
  passed after the lifecycle fix.
- The final package suites pass 402/402 on Node 22 and 402/402 on the current
  Node runtime. The workspace-lease suite passes 34/34 on both, and strict
  typechecking passes.

**Links**

- [Runtime delivery PR #72](https://github.com/sympoies/dsh-runtime-kit/pull/72)
- [Tracking issue #56](https://github.com/sympoies/dsh-runtime-kit/issues/56)

## 2026-08-24 - Authenticate ordinary managed sessions outside optional orchestration

**Result**

- A top-level Agent Console DSH session now authenticates its exact private
  session principal through an always-on pre-policy boundary.
- Ordinary shell, selective context, and finish-line admission no longer wait
  for the optional Main Agent child plugin to activate. Unmanaged sessions,
  foreign children, mismatched readiness, and substituted helper identities
  remain fail-closed.

**Why / context**

- The earlier single-agent bridge lived inside the optional Main Agent child
  plugin. A prompt submitted immediately after dsh-tui became interactive could
  reach policy while that child was still pending, so the first shell call was
  rejected as `owner-unclaimed`; the same process worked after the child
  activated a few seconds later.
- This was an activation-order race, not a missing capability: the live DSH
  process carried all six private `AGENT_SESSION_*` principal fields, and a
  later turn in the same launch authenticated successfully.

**Evidence**

- The live RED submitted a prompt as soon as dsh-tui became interactive and
  reproduced `owner-unclaimed` on the first `pwd`; a later diagnostic turn in
  the same launch observed the Main Agent tools and prepared project context,
  proving the activation race. GREEN covers exact producer readiness, direct
  in-memory bridge binding, unmanaged and foreign isolation, and mismatched
  readiness denial.
- The focused composition RED proved the optional Main Agent child repeated
  readiness even after the always-on bridge had bound the session. GREEN makes
  that child reuse the binding, removing ordinary admission from its activation
  and transient-failure path.
- The initial full package suite passed 357/357. Focused review then closed one
  disposal-during-readiness race; the final authentication suite passes 4/4,
  the affected policy and Main Agent suites pass 90/90, and strict
  typechecking passes without repeating the broad package run.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-24 - Ignore non-escalating sandbox mode echoes

**Result**

- Finish-line shell preparation now treats an unpaired permission value as a
  no-op only when it exactly matches the already-effective sandbox policy.
- Different requested modes, malformed pairs, unavailable policy, and genuine
  widening requests retain the native fail-closed validation and approval
  path.

**Why / context**

- DSH 0.1.1-rc.2 can materialize the current sandbox mode in a Bash call even
  when the caller did not request wider authority. Validating that current-mode
  echo as a new escalation rejected the call for lacking a justification
  before nils could execute it, and the unfinished operation then blocked the
  turn's finish line.

**Evidence**

- The focused RED had no normalization contract for the effective-mode echo.
  GREEN covers both the exact no-op and a different-mode request that remains
  rejected without justification.
- The finish-line and policy integration suite passed 72/72. The full package
  suite passed 353/353 with strict typechecking.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-24 - Enforce workspace ownership through native DSH lifecycle

**Result**

- The shipped bundle now activates WorkspaceLease before policy and registers
  a strict nils-cli provider for bind, begin, complete, renew, and release.
  DSH supplies authenticated Agent, Session, tool, cancellation, and disposal
  facts; nils-cli remains the sole owner of Git identity, durable exclusion,
  operation fencing, and recovery policy.
- A live child with an exact immutable parent lineage and the same checkout may
  share its ancestor's provider binding with a distinct process-local opaque
  reference. Independent sessions still contend, while linked worktrees remain
  separate mutation domains.
- Explicit same-session `resume` and `compact` can recover released or expired
  dirty work only after every operation is terminal. Startup, clear, foreign
  lineage, and uncertain operations remain fail-closed.

**Why / context**

- The previous hook experience forced agents to reconstruct repository and
  branch evidence in shell commands, sometimes blocking a completed task at
  commit time. Moving admission into the existing public DSH lifecycle makes
  the safety rule automatic without modifying, vendoring, or forking DSH and
  without duplicating Git policy in JavaScript.
- Recovery must preserve interrupted user work without turning a known session
  identifier into transferable authority. The host-authenticated session and
  parent digests therefore gate recovery, and every successful recovery mints
  a new binding and generation.

**Evidence**

- Runtime-kit passed 393/393 tests, typechecking, the exact 101-rule policy
  parity check, and the complete packed rc.7 acceptance smoke.
- Clean packed profiles on DSH 0.1.0-rc.7, 0.1.0-rc.8, and 0.1.1-rc.2 denied
  a second session in the same worktree before its mutation body, while root
  and linked-worktree mutation bodies overlapped successfully. Each DSH source
  checkout retained its exact initial identity.
- The nils-cli workspace suite passed 13/13, including same-session dirty
  resume, startup denial, foreign takeover denial, and uncertain-operation
  denial. Formatting, clippy, and the repository local-fast workspace gate
  passed on the final source.
- Rebasing onto the always-on managed-session authentication change exposed a
  clean-profile regression: partial `AGENT_SESSION_*` isolation sentinels were
  mistaken for a complete principal. A focused regression failed before the
  fix; both always-on and optional authentication paths now require the full
  producer principal, and the packed rc.7 acceptance matrix passes again.

**Links**

- [Workspace identity and leases](../workspace-leases.md)
- [Tracking issue #56](https://github.com/sympoies/dsh-runtime-kit/issues/56)
- [Native convergence tracker #66](https://github.com/sympoies/dsh-runtime-kit/issues/66)

## 2026-08-24 - Bind workspace authority at DSH's final tool guard

**Result**

- Runtime-kit now exports a versioned Cordis workspace service that binds an
  opaque reference to one exact live Agent and Session generation, classifies
  tool calls through a trusted same-process provider, renews authority, and
  drains exact operations before release.
- Provider decisions for both `allow` and approval-pending `ask` calls are
  sealed to the DSH execution identity and consumed by the public monotonic
  guard immediately before dispatch. The capability remains dormant in the
  default bundle until its nils-cli provider is implemented.

**Why / context**

- Worktree safety had accumulated as shell-hook evidence choreography. DSH
  already owns authenticated Agent, Session, approval, tool, cancellation, and
  disposal lifecycles, so runtime-kit can enforce admission at those native
  boundaries without modifying DSH or asking a model to reconstruct identity.
- Git/worktree canonicalization, durable cross-process leases, fencing, and
  recovery remain nils-cli responsibilities. Keeping that split avoids a
  second JavaScript policy engine and preserves an unmodified pinned DSH.

**Evidence**

- Focused lifecycle coverage passed 32/32, including stale refs, Session
  replacement during admission, approved `ask`, post-classification execution
  replacement, renewal loss, concurrent teardown, and durable release failure.
- The full repository suite passed 383/383 with typechecking and the exact
  101-rule policy parity check.
- Clean profiles installed the packed candidate and exercised DSH's real tool
  pipeline on 0.1.0-rc.7, 0.1.0-rc.8, and 0.1.1-rc.2. Every row observed
  `bind -> begin -> body -> complete:succeeded -> release`, an `owned` state,
  and a non-transferable opaque reference. The smoke also caught and fixed an
  inherited-TTY dependency in rc.8 profile installation.

**Links**

- [Workspace identity and leases](../workspace-leases.md)
- [Tracking issue #56](https://github.com/sympoies/dsh-runtime-kit/issues/56)
- [Runtime contract commit](https://github.com/sympoies/dsh-runtime-kit/commit/54fe5b51e77cb50144d080ba3ffbc2419dcd5633)

**Follow-ups**

- Add the version-matched nils-cli canonical identity and durable lease
  provider, then compose and deploy the capability through the remaining #56
  gates before activating it by default.

## 2026-08-24 - Authenticate managed single-agent sessions before policy

**Result**

- A top-level DSH process launched as an authenticated managed session now
  binds its exact Agent Console principal at the first pre-step, before policy,
  selective context, shell, or finish-line subprocesses run.
- Single-agent admission does not initialize a Main Agent run. Unmanaged DSH
  sessions and foreign children retain the scrubbed identity boundary.

**Why / context**

- The bridge previously bound a top-level principal only after native Main
  Agent initialization. A correctly profiled ordinary Agent Console session
  therefore reached its first shell call without an owner and was rejected as
  `owner-unclaimed` plus `coordination-unmanaged`.

**Evidence**

- The focused RED proved that the first managed pre-step performed no
  readiness call and left the bridge empty.
- GREEN performs the producer-owned self-readiness check, validates the exact
  session, incarnation, checkpoint, and trusted activity helper, reuses the
  binding on later steps, and proves no run initialization occurred.
- Typechecking and the focused Main Agent bridge suite pass; the latter covers
  managed, repeated, foreign-child, and unmanaged boundaries.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-24 - Require the nils-cli 1.27.5 advisory checkout repair for DSH rc.2

**Result**

- The validated nils-cli release advanced to v1.27.5 with its exact signed
  source, Linux archive, and six consumed binary digests. The global minimum
  remains v1.27.1 for the retained DSH rc.7 and rc.8 paths.
- The reviewed DSH rc.2 composition now requires v1.27.5 so an authenticated
  advisory session cannot be denied by the enforce-only physical checkout
  lease guard.

**Why / context**

- An ordinary Agent Console DSH session could authenticate its owner and load
  policy successfully, then still block its first shell tool on a clean
  checkout lease left by another session. The nils-cli v1.27.5 repair makes
  authenticated advisory coordination observe rather than acquire that lease;
  enforce, missing, and forged coordination remain fail closed.
- Keeping the older global minimum avoids retiring working rc.7 and rc.8
  compositions while preventing rc.2 from claiming compatibility without the
  exact session-start repair used by the fleet.

**Evidence**

- The focused doctor regression rejected the pre-change rc.2 range, then proves
  v1.27.4 needs attention, v1.27.5 is healthy, and rc.8 with v1.27.1 remains
  healthy.
- Package tests, strict typechecking, and the real policy benchmark authenticate
  the same release manifest used by deployment.

**Links**

- [nils-cli v1.27.5](https://github.com/sympoies/nils-cli/releases/tag/v1.27.5)
- [nils-cli advisory checkout repair](https://github.com/sympoies/nils-cli/pull/1486)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-24 - Validate nils-cli 1.27.4 without retiring 1.27.1

**Result**

- The validated nils-cli release advanced to v1.27.4 with its signed source
  revision, Linux archive digest, and exact digests for the six consumed
  binaries. The global minimum remains v1.27.1 for DSH rc.7 and rc.8, while
  the exact rc.2 path requires v1.27.4 lifecycle fixes.
- Current compatibility documentation and CI now fetch and authenticate the
  same v1.27.4 archive recorded by the machine-readable manifest.

**Why / context**

- DSH 0.1.1-rc.2 support depends on the policy projection and finish-line
  repairs released after v1.27.2, so validating only the older nils bundle
  would leave the promoted DSH compatibility claim incomplete.
- Advancing validation does not itself require retiring previously supported
  binaries; the runtime doctor keeps their inclusive range and raises only the
  exact rc.2 floor.

**Evidence**

- Focused regressions failed against the v1.27.2 manifest, then passed for the
  exact v1.27.4 source, archive, binary digests, and conditional version range.
- The doctor regression proves rc.2 with v1.27.1 needs attention, rc.2 with
  v1.27.4 is healthy, and retained rc.8 with v1.27.1 remains healthy.
- The full package suite passed 349/349 with strict typechecking. Both selected
  DSH source channels passed exact clean-checkout inspection, deterministic and
  released-agent-hook policy benchmarks retained no active work, and the packed
  runtime passed all eight non-delivery scenarios using v1.27.4 binaries.
- The standalone delivery rehearsal was not counted because it has no
  authenticated session owner and correctly fails closed. Agent Console owns
  the separate session-bound delivery acceptance.

**Links**

- [nils-cli v1.27.4](https://github.com/sympoies/nils-cli/releases/tag/v1.27.4)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-24 - Validate the latest DSH and close finish-line turns durably

**Result**

- The compatibility matrix now authenticates DSH 0.1.1-rc.2 at its exact
  upstream revision and Agent Console TUI 0.9.0 while retaining the reviewed
  rc.7 and rc.8 compatibility rows.
- A successful finish-line stop now releases its exact turn capability before
  the agent becomes idle. Teardown drain preserves an already authenticated
  release for one bounded teardown grace while continuing to cancel ordinary
  in-flight requests.

**Why / context**

- DSH 0.1.1-rc.2 makes agent disposal explicit and does not await asynchronous
  disposal listeners. Depending on process teardown for the durable release
  could therefore race subprocess resolution, leave the prior session active,
  and make a later resume fail closed.
- The allow decision is the last awaited boundary that still owns the complete
  turn identity and capability, so release belongs there; disposal remains the
  fallback for incomplete turns.

**Evidence**

- Focused regressions reproduced drain cancelling an authenticated release,
  a late release escaping the first drain snapshot, and an unbounded direct
  release; all three repaired lifecycle paths passed with bounded cleanup.
- The full local suite passed 348/348 with strict typechecking. A packed DSH
  0.1.1-rc.2 plus TUI 0.9.0 smoke passed runtime context, route and effort,
  tools, result-driven finish-line, resume, reviewer, and failure-path checks.

**Links**

- [DSH 0.1.1-rc.2 source revision](https://github.com/deepseek-ai/DeepSeek-Harness/commit/b150a551b8d465e31e418e1b2eaf5e79bbb7d28e)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Keep lane visibility filters registry-valid

**Result**

- Main Agent worker launch now gives rc.7 a visibility deny list containing
  only the controller tools this bundle guarantees are globally registered.
- The per-child monotonic execution guard still denies delegation, legacy,
  controller, and configured extra names, including names absent at launch.

**Why / context**

- A real Agent Console Main Agent controller reached worker launch after the
  provider-identity repair, but rc.7 rejected four absent cross-product names
  in `tools.restrict()` before the worker could bootstrap.

**Evidence**

- Focused RED reproduced the invalid visibility contract with the legacy
  `subagent` name present in the filter.
- GREEN keeps bundle-owned controller tools hidden while proving legacy and
  configured names remain denied by execution authority.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Keep provider activity identity separate from its owner

**Result**

- Authenticated Main Agent policy calls retain the Agent Console session as
  their coordination owner while sending the original DSH session identity to
  `agent-hook` on one private subprocess-only metadata field.
- The public DSH ingress schema and controller principal allowlist are
  unchanged.

**Why / context**

- Once native initialization bound a controller, the ingress subject correctly
  changed from the DSH session UUID to the Agent Console owner. The activity
  capability reused that owner field as a provider correlation, so
  `agent-session` rejected the first authenticated post-tool event with
  `provider-session-id-mismatch`.

**Evidence**

- Focused REDs proved that the runtime omitted the stable provider identity and
  that `agent-hook` rendered the owner subject into the activity event.
- GREEN preserves the owner subject for policy while activity renders the
  separately transported DSH provider session; downstream runtime binding
  remains the final fail-closed identity check.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Resolve the trusted activity helper before identity checks

**Result**

- Portable Main Agent configuration now resolves `agent-session` through the
  DSH host executable seam before controller-principal and worker-launch
  identity checks compare real filesystem targets.
- Absolute configuration remains unchanged, and arbitrary absolute helper
  paths still fail closed.

**Why / context**

- Agent Console correctly pinned `AGENT_SESSION_BIN` to the installed helper,
  while the runtime bundle intentionally configured its released CLIs by bare
  name. `realpathSync('agent-session')` does not search `PATH`, so the exact
  installed helper was incorrectly rejected as an invalid principal.

**Evidence**

- The focused RED reproduced `main-agent-controller-principal-invalid` with
  portable runtime configuration and a resolver-pinned helper.
- GREEN covers controller initialization, worker launch, and rejection of an
  unrelated absolute helper without widening the principal environment.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Preserve the pinned activity helper after controller init

**Result**

- The authenticated DSH controller principal now carries the absolute
  `AGENT_SESSION_BIN` path that Agent Console pinned for its runtime.
- Ordinary DSH sessions remain isolated, while managed policy and finish-line
  subprocesses can resolve the exact session-activity helper after native
  controller initialization.

**Why / context**

- Native initialization binds the controller only after the tool body
  succeeds. Its post-tool policy check therefore becomes the first check to
  use the authenticated principal.
- The bridge previously retained the session identity and capability files but
  dropped the pinned helper path. The intentionally minimal subprocess `PATH`
  could not resolve `agent-session`, so the activity capability failed closed
  even though initialization itself had succeeded.

**Evidence**

- The focused regression failed because the bound controller environment
  omitted `AGENT_SESSION_BIN`, then passed after the helper path joined the
  principal allowlist and was pinned to the trusted CLI companion by real path.
- The Main Agent suite passed 35/35, the authenticated policy-environment
  integration passed, and strict typechecking remained green.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Preserve the authenticated DSH controller after native init

**Result**

- Successful `main_agent_run_initialize` now binds the exact top-level DSH
  controller id to its readiness-authenticated managed-session principal.
- The bridge restores only five `AGENT_SESSION_*` fields owned by session
  admission. Foreign subagents, readiness identity mismatches, and failed init
  attempts remain unbound; plugin teardown clears every controller binding.

**Why / context**

- Native readiness/init could create a durable run, but the bridge previously
  resolved only managed lane children. The controller therefore became
  ownerless again on its next policy, context, shell, or finish-line subprocess.

**Evidence**

- The focused RED showed a successful native init followed by an undefined
  bridge principal for the exact DSH controller id.
- GREEN binds only that id. Focused coverage also proves an incarnation
  mismatch stops before store init and a non-top-level foreign subagent cannot
  claim the controller principal.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Initialize DSH Main Agent runs through a native tool

**Result**

- A DSH controller now performs fixed compatibility and authenticated
  self-readiness checks, then initializes its durable run through the
  controller-only `main_agent_run_initialize` tool.
- Managed lanes deny that controller surface, while ordinary DSH shell and nils
  subprocesses remain ownerless and scrubbed by design.

**Why / context**

- Live Agent Console acceptance reached `runtime_context(project-dev)` but the
  documented shell readiness/init path was rejected as `owner-unclaimed` and
  degraded to `finish-line-unavailable` before a native lane could exist.
- Lane bootstrap already had a native authenticated tool; controller init
  required the same fixed-argv boundary to reach that lane safely.

**Evidence**

- The focused regression failed because no controller initialization tool was
  registered, then passed with exact `capabilities → self readiness → init`
  argv and controller-cwd assertions.
- The Agent Console contract now requires native controller initialization and
  lane bootstrap surfaces in addition to the existing lane checkpoint.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Authenticate Main Agent lanes across DSH subprocesses

**Result**

- A managed DSH lane now bootstraps through a lane-scoped native tool and binds
  its authenticated worker principal to policy, selective context, and
  finish-line subprocesses for the lifetime of that lane.
- Ordinary DSH sessions still scrub all ambient provider identity, and the
  private bridge is removed when the lane runtime closes.

**Why / context**

- The external launch payload supplied a valid worker capability, but the DSH
  child received it only as prompt text. Each shell tool runs in a fresh
  process, so a standalone export could not authenticate the required first
  bootstrap; hook admission consequently observed an ownerless DSH session.
- Keeping the bridge inside the lane registry avoids broadening environment,
  credential, or repository permissions while making the runtime-issued
  worker identity available at the exact nils subprocess boundaries.

**Evidence**

- The regression first failed because the child prompt still required an
  unauthenticated shell bootstrap and no lane bootstrap tool existed.
- The focused Main Agent, policy, and session-environment matrix passed 79/79,
  followed by strict typechecking. Foreign-child and ambient-identity cases
  remain rejected or scrubbed.

**Links**

- [Main Agent Mode contract](../main-agent-mode.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Resolve nils helpers against the DSH host PATH

**Result**

- Portable `agent-hook` and `agent-docs` lookup now uses the DSH subprocess
  service's host execution PATH, while the resolved child still receives the
  existing isolated spawn environment.
- Timeout, exit, output, correlation, and process-tree quiescence checks remain
  unchanged and fail closed.

**Why / context**

- The first portable-command repair supplied the isolated child environment to
  `resolveExecutable`. Its intentionally minimal PATH excluded the private
  nils-cli directory, so live Agent Console turns still failed before policy or
  context dispatch even though managed spawn now received an absolute command.
- The rc.7 subprocess contract uses an omitted environment for portable helper
  lookup and applies the isolated environment only when spawning the resolved
  executable.

**Evidence**

- The regression observed the isolated PATH in the resolver call and failed
  until that argument became `undefined`.
- The focused context, policy, and finish-line matrix passed 96/96, followed by
  strict typechecking and package preview.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Resolve portable nils commands before managed spawn

**Result**

- Policy, selective context, and finish-line transports now resolve portable
  `agent-hook` and `agent-docs` names through the DSH subprocess service before
  passing an absolute executable to managed spawn.
- Explicit absolute executable configuration remains unchanged, and every
  existing fail-closed timeout, output, exit, and process-tree check remains in
  force.

**Why / context**

- The runtime passed a bare executable name directly to a subprocess API whose
  spawn contract requires a fully resolved `argv[0]`. A live rc.7 turn could
  therefore load the plugin but fail policy admission, selective context, and
  finish-line completion before the nils child executed.
- Using the subprocess service's resolver keeps executable lookup aligned with
  its scrubbed child environment instead of duplicating host PATH logic in the
  runtime kit.

**Evidence**

- The regression first failed as `runtime-context-unavailable` when a fake
  subprocess rejected an unresolved command, then passed after resolution.
- The focused context, policy, and finish-line matrix passed 74/74, including
  bare-name coverage for all three transports and a pre-spawn deadline check.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Converge the Agent Console and Workbench runtime

**Result**

- One runtime-kit candidate now retains the authenticated Agent Console rc.7
  profile and high-effort Sol route while also accepting the reviewed DSH
  rc.8 Workbench release.
- The packed smoke derives the exact source revision from the selected release
  contract, so rc.7 and rc.8 cannot be confused during validation.

**Why / context**

- The Agent Console fleet work and the Workbench rc.8 compatibility work had
  landed on divergent reviewed histories. Binding both products to the
  Agent Console head therefore made the Workbench fail closed at startup.
- Converging those histories keeps one compatibility authority and preserves
  the exact-version checks for both products.

**Evidence**

- The combined compatibility, Agent Console, context, finish-line, lifecycle,
  Main Agent, operations, and session-environment matrix passed 171/171.
- Typechecking, package preview, and the downstream Workbench container smoke
  are the release gates for this integration candidate.

**Links**

- [Fleet integration tracking issue](https://github.com/serenvia/sympoies-infra/issues/213)
- [Compatibility contract](../compatibility.md)

## 2026-08-23 - Empty profiles accept an owned generated lock

**Result**

- Setup now accepts pnpm creating a new lockfile only when its sole importer,
  package, and empty snapshot root bind the reviewed runtime-kit artifact and
  removing that root leaves the exact default lock structure.
- Any unrelated importer, dependency, setting, package, snapshot, or metadata
  still counts as collateral and is restored and rejected.

**Why / context**

- DSH rc.8 creates `pnpm-lock.yaml` during the first plugin installation even
  when the profile previously had no lockfile. The operations projection
  treated every absent-to-present lock transition as unrelated mutation, so a
  valid first Workbench activation rolled back.
- The exception is deliberately structural rather than version-wide: package
  and activation digests remain authoritative, runtime-kit-prefixed keys are
  not ownership evidence, and only an otherwise empty pnpm 9 lock is
  equivalent to the absent pre-install state.

**Evidence**

- The regression first failed with `native-dsh-collateral-mutation`, matching
  the live rc.8 profile, then passed together with the existing unrelated-lock
  rejection case.
- The final focused generated-lock matrix passed 9/9; the complete affected
  operations suite passed 81/81 and strict typechecking
  passed.

**Links**

- [Operations guide](../operations.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Operations now bind every reviewed DSH release

**Result**

- Setup, doctor, replay, and recovery accept exact DSH rc.7 or rc.8
  toolchains and bind each version to its reviewed source revision.
- Unknown releases and mismatched version-to-revision state remain
  fail-closed.

**Why / context**

- Runtime compatibility already covered both Agent Console on rc.7 and Web
  Workbench on rc.8, but the out-of-process operations plane still encoded
  only the rc.7 executable identity. That prevented the reviewed Workbench
  composition from reaching setup.
- Reading the same compatibility manifest avoids a second release allowlist
  drifting away from the package compatibility contract.

**Evidence**

- The rc.8 setup regression first failed with status 70 at the exact
  toolchain boundary, then passed with the rc.8 source revision in its plan;
  an rc.9 fixture remains rejected.
- The affected operations and compatibility matrix passed 91/91, and strict
  typechecking passed.

**Links**

- [Operations guide](../operations.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Runtime compatibility added the Workbench rc.8 release

**Result**

- The runtime package now accepts one homogeneous DSH `0.1.0-rc.7` or
  `0.1.0-rc.8` peer set while continuing to reject mixed and unknown versions
  before importing DSH modules.
- The compatibility manifest binds both official release tags to exact source
  revisions, without retiring the rc.7 Agent Console lane.

**Why / context**

- Agent Console still composes the reviewed rc.7 profile, while the Web
  Workbench has advanced to rc.8. A single runtime package must therefore
  preserve both exact release contracts rather than use an open-ended
  prerelease range.
- Keeping the public surface check shared is justified only by executable
  evidence; it is not inferred from semver compatibility.

**Evidence**

- The owner test first failed because rc.8 was absent from the compatibility
  manifest and the loader rejected every rc.8 peer.
- The focused compatibility suite passed 18/18 after the fix, retained the v1
  diagnostic shape, and rejects mixed versions plus tracked or untracked
  checkout drift.
- Packed composition smokes passed independently on clean, unmodified rc.7 and
  rc.8 source checkouts before and after execution with released nils-cli
  v1.27.2, including policy, Main Agent, native subagent, finish-line, resume,
  and isolation scenarios. A focused API-contract review passed after those
  two contract repairs.

**Links**

- [Compatibility guide](../compatibility.md)
- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Validated nils-cli advanced to 1.27.2

**Result**

- The validated nils-cli release advanced to v1.27.2 with exact signed source,
  Linux archive, and six consumed binary digests.
- The runtime doctor now accepts stable `agent-docs` releases from the retained
  v1.27.1 minimum through the v1.27.2 validated release, while rejecting newer,
  older, or prerelease identities.

**Why / context**

- Host deployments had already upgraded their governed helper toolchains, but
  the runtime doctor still encoded v1.27.1 as its only valid identity. That
  correctly stopped activation, yet prevented an independently released and
  fully compatible helper bundle from being adopted.
- Keeping minimum and validated releases distinct preserves the supported
  v1.27.1 lane instead of turning a routine validation advance into an
  unreviewed compatibility retirement.

**Evidence**

- The compatibility regression and runtime doctor range test failed before the
  manifest and runtime change, then passed with v1.27.1 and v1.27.2 accepted
  and v1.27.3 rejected.
- The full package suite passed 309/309 tests, strict typechecking passed, and
  the exact DSH rc.7 checkout passed its source compatibility check.
- The published v1.27.2 Linux `agent-hook` and `agent-docs` binaries completed
  the packed DSH smoke; both deterministic and real-process policy benchmarks
  passed without retained active work.

**Links**

- [nils-cli v1.27.2](https://github.com/sympoies/nils-cli/releases/tag/v1.27.2)
- [Compatibility guide](../compatibility.md)

## 2026-08-22 - POSIX command containment became host-tool independent

**Result**

- Governed operations now create their isolated session and process group
  through Node's POSIX `detached` child-process contract instead of resolving
  a host-provided `setsid` executable.
- The supervisor reports its command pgid over the private control descriptor,
  so timeout, output-limit, supervisor-loss, and descendant cleanup retain the
  same fail-closed quiescence proof on Linux and macOS.

**Why / context**

- macOS provides POSIX process groups but does not ship the Linux `setsid`
  command. Treating that executable as a prerequisite prevented an otherwise
  compatible DSH profile from reaching even a setup preview.
- Keeping process-group creation inside the existing Node supervisor removes a
  host package dependency without weakening the negative-pgid cleanup boundary.

**Evidence**

- The new no-`setsid` regression failed with typed `command-unavailable` before
  the fix and passed afterward.
- Focused timeout, live-descendant, supervisor-loss, and owner-repair scenarios
  passed, along with strict typechecking.
- A real macOS Node 26 / DSH rc.7 setup preview completed with `ok: true` while
  the host provided no external `setsid` executable.

**Links**

- [Node.js child process `detached` contract](https://nodejs.org/api/child_process.html#optionsdetached)
## 2026-08-23 - Bind Agent Console to high-effort Sol routes

**Result**

- The authenticated Agent Console rc.7 contract now requires `high` reasoning
  effort for its default GPT-5.6 Sol route.
- Controller and worker observations must both carry the exact effort, and the
  sanitized inspection result preserves it for downstream provisioning checks.
- Main Agent lane anchors inherit the controller's effective request-header
  effort instead of narrowing the route to provider and model.

**Why / context**

- Provider and model identity alone could not distinguish the intended default
  from a profile that silently selected another supported effort level.
- Keeping effort in the same closed route boundary lets deployment code fail
  before activation when controller and worker defaults drift.

**Evidence**

- The pre-change contract test failed because the default omitted effort and
  the inspector rejected the new observation field.
- The focused Agent Console, rc.7 compatibility, context, finish-line,
  session-environment, and Main Agent suites pass 72/72 with strict
  typechecking and diff checks green.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Nils children retain the authenticated Linux user route

**Result**

- Context and finish-line subprocesses now retain only the canonical Linux
  per-user runtime directory and its derived D-Bus address, alongside a fixed
  system executable path.
- Ambient or caller-supplied home, path, session token, and D-Bus values remain
  excluded from the replacement environment.

**Why / context**

- A real Agent Console turn loaded its project context but then lost the
  finish-line capability because the replacement child environment disconnected
  `agent-hook` from the authenticated systemd user manager.
- The required route is host runtime state rather than provider credential
  state. Binding it to `/run/user/<uid>` restores the supported nils transport
  without forwarding an ambient home or arbitrary bus endpoint.

**Evidence**

- The regression first showed that the isolated child lacked every user-runtime
  field; the repaired focused context and finish-line suite passed 27/27 with
  caller attempts to override the protected fields rejected.
- Released nils-cli v1.27.2 completed an exact finish-line open and release with
  only the canonical runtime directory, derived user-bus address, and fixed
  system path present.

**Links**

- [Fleet integration issue](https://github.com/serenvia/sympoies-infra/issues/213)

## 2026-08-23 - Validate nils-cli 1.27.2 on the Agent Console integration line

**Result**

- The Agent Console UI integration line now validates released nils-cli 1.27.2
  while retaining 1.27.1 as its minimum supported release.
- Agent Docs health derives the inclusive supported range from the packaged
  compatibility manifest instead of hard-coding one version.

**Why / context**

- Default-branch runtime-kit validation alone did not include the UI line's
  `./agent-console-profile` export. Live provisioning correctly stopped before
  service restart when the incomplete source identity was selected.
- Applying the same reviewed compatibility delta to the integration line keeps
  the TUI profile and nils release proof on one exact source commit.

**Evidence**

- Focused compatibility, operations, skills, and Agent Console profile tests
  passed 90/90; typecheck passed.
- The exact DSH rc.7 source plus dsh-tui 0.8.1 and released nils-cli 1.27.2
  binaries completed the packed runtime smoke with profile inspection, scoped
  tool authority, and Sol worker inheritance all verified.
- The equivalent compatibility delta already passed the full 309-test package
  suite and both policy benchmarks on default main; that unchanged full suite
  was not repeated for this integration-only transfer.

**Links**

- [Fleet integration tracking issue](https://github.com/serenvia/sympoies-infra/issues/213)
- [Default-main compatibility PR](https://github.com/sympoies/dsh-runtime-kit/pull/35)

## 2026-08-22 - Integrate macOS containment with the Agent Console profile

**Result**

- The Agent Console integration line now contains both the rc.7/TUI profile
  inspection export and the reviewed POSIX command-gate containment
  implementation. macOS no longer requires a host `setsid` executable.

**Why / context**

- The first m4 apply selected the containment-only `main` merge, which fixed
  process groups but did not contain the still-unintegrated
  `./agent-console-profile` export. Verification stopped before LaunchAgent
  reload, leaving the running service available.

**Evidence**

- The three containment source/test files are byte-identical to the reviewed
  `345724c6...` merge. Typecheck, six focused containment scenarios, and all
  seven Agent Console profile contract tests pass together on this integration
  line.

**Follow-ups**

- Bind sympoies-infra to this combined signed commit, prove a real m4 setup
  dry-run, then resume the local apply without GitHub Actions.

## 2026-08-21 - Agent Console rc.7 profile authenticated

**Result**

- The runtime kit now publishes an exact `dsh-tui` profile contract for DSH
  rc.7 and the authenticated TUI release, alongside the retained native
  `headless` profile.
- The profile preserves the official interaction surface, runtime-kit tools
  and skills, and Main Agent Mode service while workers inherit the controller's
  `codex-proxy` / `gpt-5.6-sol` route.

**Why / context**

- A successful headless activation did not prove that Agent Console's
  agent-preset composition retained the same runtime-kit and orchestration
  capabilities. The TUI moves model-facing rows onto the agent plane, so a raw
  uncomposed session can otherwise look like missing runtime functionality.
- The compatibility boundary accepts sanitized observations only. Sandbox,
  approval, credential, state, and worker authority remain with their existing
  owners rather than moving into runtime-kit profile inspection.

**Evidence**

- The pre-change profile suite failed 5/5 because no non-headless composition
  boundary existed; focused profile and Main Agent route coverage then passed
  33/33.
- A packed runtime-kit candidate completed the full real DSH rc.7 smoke with
  `@deepseek-harness-tui/dsh-tui@0.8.1`, including project-skill precedence,
  policy guards, resume, native reviewer, and governed-delivery rehearsal.

**Links**

- [Fleet integration tracking issue](https://github.com/serenvia/sympoies-infra/issues/213)
- [Compatibility contract](../compatibility.md)
- [Operations guide](../operations.md)

## 2026-08-21 - Main Agent Mode completed on nils-cli 1.27.1

**Result**

- DSH Main Agent Mode now owns controller-only lane launch, authenticated
  readiness, per-lane checkpoints, supervision, request-changes and acceptance
  decisions, and run closeout through the nils-cli coordination store.
- The compatibility boundary now requires and validates the released nils-cli
  v1.27.1 bundle, including the lane-orchestration command family and live
  agent-docs health check.

**Why / context**

- The earlier runtime exposed lane start and stop primitives, but a usable main
  agent also needs one authoritative path from assignment through review and
  closeout. Keeping that state in nils-cli prevents the DSH adapter from
  inventing a second coordination model.
- The completed integration keeps lane authority local to each child while the
  controller retains orchestration tools and bounded lifecycle ownership.

**Evidence**

- The exact released v1.27.1 Linux bundle completed the real-store Main Agent
  E2E scenarios for two-lane lifecycle, overlapping-scope refusal, and stopped
  lane reconciliation.
- Focused Main Agent, orchestration, lifecycle, and released-binary scenarios
  passed 48/48; the compatibility owner suite passed 17/17, alongside strict
  typechecking and package inspection.
- The isolated local `headless` profile reported healthy with agent-docs
  v1.27.1 after the exact release binaries were staged into its toolchain.

**Links**

- [Tracking issue #6](https://github.com/sympoies/dsh-runtime-kit/issues/6)
- [Tracking issue #8](https://github.com/sympoies/dsh-runtime-kit/issues/8)
- [nils-cli v1.27.1](https://github.com/sympoies/nils-cli/releases/tag/v1.27.1)
- [Main Agent Mode contract](../main-agent-mode.md)

## 2026-08-21 - Product and contributor documentation separated

**Result**

- The root README was reduced from 799 to 127 lines and now serves DSH users
  with the product purpose, supported runtime, activation path, capabilities,
  and documentation map.
- Routine contributor setup and validation moved to `DEVELOPMENT.md`; focused
  operations, compatibility, and acceptance guides plus a docs index now own
  the corresponding long-form material.
- Architecture gained navigable section headings, and coexistence contract
  coverage now checks the canonical operations guide instead of requiring
  internal activation detail in the user-facing README.

**Why / context**

- The README had accumulated runtime internals, contributor commands,
  operations procedures, promotion evidence, and compatibility detail. That
  made the package difficult for a non-contributor to evaluate and try.
- Separating documents by audience follows the repository convention that
  everyday development and test guidance belongs in `DEVELOPMENT.md`, while
  agent-docs remains a compact DSH runtime context asset rather than a human
  documentation store.

**Evidence**

- `npm test` passed 291/291 tests and `npm run typecheck` passed.
- `npm run benchmark:policy` passed 2,000 samples with p95 0.133 ms and no
  active operations or live handles after teardown.
- A local Markdown scan resolved links across 90 files, `git diff --check`
  passed, and `npm pack --dry-run --json` included every new documentation
  entrypoint.

**Links**

- [Product README](../../README.md)
- [Contributor guide](../../DEVELOPMENT.md)
- [Documentation index](../README.md)
- [Operations guide](../operations.md)

## 2026-08-21 - Project devlog workflow established

**Result**

- The repository gained a monthly, newest-first development log, a search
  helper, a project-local `project-devlog` skill, and discoverable entrypoints
  in `README.md` and `AGENTS.md`.
- The initial August log backfills the four durable milestones from the public
  repository's creation through the completed native DSH migration.

**Why / context**

- The migration plan retains detailed execution evidence, but it is too dense
  for routine historical lookup. A curated devlog preserves milestone context
  and links without turning commit history or the plan ledger into a changelog.
- The structure follows the signal-oriented convention already used by the
  public `sympoies/symphony-board` project, adapted to this runtime package's
  privacy and documentation boundaries.

**Evidence**

- `node --test test/devlog-search.test.mjs` passed the helper's success,
  no-match, usage, malformed-month, and path-traversal cases.
- `bash -n scripts/devlog-search.sh` passed, and searches by term and month
  returned the expected August entries.
- `npm pack --dry-run --json` included both devlog documents in the published
  package, and `git diff --check` passed.

**Links**

- [Devlog policy and template](README.md)
- [Project devlog skill](../../.agents/skills/project-devlog/SKILL.md)
- [Repository policy](../../AGENTS.md)

## 2026-08-21 - Native DSH runtime migration completed

**Result**

- The public out-of-tree runtime kit reached `main` through PR #26, with the
  final parity evidence and migration state repair following in PR #27.
- DSH now uses dsh-runtime-kit plus nils-cli while Codex and Claude Code retain
  agent-runtime-kit plus nils-cli; the profiles coexist without cross-loading.

**Why / context**

- The terminal architecture is additive, not a provider replacement. DSH owns
  its native agent, tool, session, sandbox, skill, and subagent surfaces while
  nils-cli remains the shared deterministic policy and workflow boundary.
- Keeping the two runtime layers isolated makes DSH activation reversible and
  avoids coupling this package to the existing provider runtime.

**Evidence**

- Hosted acceptance run `32447387497` completed 12 scenarios with 12 passed,
  0 pending, and 0 failed against released nils-cli v1.27.0 and DSH rc.7.
- Compatibility run `32447803170` completed four green jobs before PR #26
  merged. A local profile check reported DSH doctor healthy and the real
  `runtime_kit_plus_one` tool returned `42` for `41`.

**Links**

- [Tracking issue #1](https://github.com/sympoies/dsh-runtime-kit/issues/1)
- [Delivery PR #26](https://github.com/sympoies/dsh-runtime-kit/pull/26)
- [Final parity repair PR #27](https://github.com/sympoies/dsh-runtime-kit/pull/27)
- [Retained migration state](../plans/2026-08-18-dsh-runtime-kit-migration/dsh-runtime-kit-migration-execution-state.md)

## 2026-08-20 - Acceptance became hermetic and profile-safe

**Result**

- The acceptance path preserved the hosted tool runtime, reused only an
  authenticated pnpm store and DSH lockfile, tolerated executable-mode
  normalization, and installed into native or clean DSH profiles offline.
- Operations recovery was hardened around canonical profile roots, atomic
  collateral restoration and removal, bounded activation assets, and exact
  ownership adoption.

**Why / context**

- A green source-level test was insufficient for the final coexistence claim.
  Acceptance needed to prove that a packed candidate could run without network
  resolution, mutate neither the upstream checkout nor unrelated provider
  state, and recover safely across interrupted profile operations.

**Evidence**

- The final local package suite reached 254/254 tests with typechecking, both
  compatibility benchmarks, package inspection, and packed rc.7 smoke green.
- The hosted-rehearsal sequence progressed from authorization-pending runs to
  the terminal credentialless 12/12 acceptance recorded in the migration
  state, while the upstream DSH checkout remained clean.

**Links**

- [Hosted runtime repair PR #14](https://github.com/sympoies/dsh-runtime-kit/pull/14)
- [Authenticated pnpm store PR #15](https://github.com/sympoies/dsh-runtime-kit/pull/15)
- [Authenticated lockfile PR #16](https://github.com/sympoies/dsh-runtime-kit/pull/16)
- [Executable-mode normalization PR #17](https://github.com/sympoies/dsh-runtime-kit/pull/17)
- [Offline native profile install PR #18](https://github.com/sympoies/dsh-runtime-kit/pull/18)
- [Clean-profile initialization PR #21](https://github.com/sympoies/dsh-runtime-kit/pull/21)

## 2026-08-19 - Native policy, review, and lane orchestration integrated

**Result**

- The runtime integrated the nils-backed lifecycle and finish-line bridge,
  selective project context, complete rule-parity projection, eight native
  read-only reviewer personas, Main Agent Mode, and digest-bound operations.
- All 101 legacy source rules resolved to 25 implemented groups or one
  provider-obsolete retirement; no retired handler executable remained in the
  production package.

**Why / context**

- DSH integration had to use public Cordis and harness services while keeping
  deterministic policy out of JavaScript. Exact runtime identities, bounded
  transport, and server-owned reviewer personas preserve authority across the
  native lifecycle without trusting prompt text or caller claims.

**Evidence**

- The integrated package passed 164/164 tests, strict typechecking, source and
  runtime parity verification, two compatibility rows, and a packed smoke on
  unmodified DSH rc.7.
- Reviewer acceptance blocked an attempted write before the tool body,
  validated structured JSONL through nils-cli, and left no live child.

**Links**

- [Main Agent Mode PR #7](https://github.com/sympoies/dsh-runtime-kit/pull/7)
- [Native runtime integration PR #10](https://github.com/sympoies/dsh-runtime-kit/pull/10)
- [Architecture](../architecture.md)
- [Main Agent Mode contract](../main-agent-mode.md)

## 2026-08-18 - Public out-of-tree baseline established

**Result**

- A public repository, approved migration plan, and first external DSH bundle
  baseline were created without forking or patching DeepSeek Harness.
- The baseline registered a Cordis plugin, public and optional private skill
  providers with explicit precedence, a native probe tool, and the first
  bounded nils-cli lifecycle bridge.

**Why / context**

- The runtime needed to remain independently installable and publishable while
  private skills stayed owner-controlled and outside the package. Versioned
  compatibility adapters were preferred over vendoring upstream DSH behavior.

**Evidence**

- Baseline PR #2 passed 26/26 tests and a packed real DSH rc.7 smoke. The next
  lifecycle layer passed 53/53 tests, strict typechecking, package inspection,
  and real allow, deny, cancellation, disposal, and skill-precedence checks.
- The real native probe returned `42` for `41`, and an authoritative policy
  denial occurred before the tool body ran.

**Links**

- [Migration source](../plans/2026-08-18-dsh-runtime-kit-migration/dsh-runtime-kit-migration-discussion-source.md)
- [Migration plan](../plans/2026-08-18-dsh-runtime-kit-migration/dsh-runtime-kit-migration-plan.md)
- [Baseline PR #2](https://github.com/sympoies/dsh-runtime-kit/pull/2)
- [Lifecycle bridge PR #3](https://github.com/sympoies/dsh-runtime-kit/pull/3)
